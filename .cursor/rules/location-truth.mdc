---
description: CCC 位置真相 — M1 是仓库，Mac2017 是工作台
globs: 
alwaysApply: true
---

# CCC 位置真相

> 硬规则。不讨论，不商量。认错位置 = 做错事。

## 一句话

**M1 是代码仓库 + Desktop 控制面。Mac2017 才是 Hub/Board/Engine 的开发和生产工作台。**

## 具体

| | M1（当前机器） | Mac2017（产线节点） |
|---|---|---|
| 角色 | 源码仓库 + Desktop 控制面 | 开发/生产工作台 |
| 访问方式 | 当前终端 | `ssh mac2017`（~/.ssh/config 已配） |
| CCC 源码 | `/Users/apple/program/CCC/`（只读副本） | `/Users/fan/program/CCC/`（可读写，git push 从这里发） |
| 跑什么 | CCCDesktop.app + relay.m1 + hub-tunnel | Hub(:7777) + Board(:7775) + Engine(PID 62332) + Relay + Chat-server |
| 你怎么改 | 改完 push 到 GitHub | 开发工具（Claude/OpenCode）在那边合入后才生效 |
| 你怎么查 | `ssh mac2017` 连过去看 | 日志、看板、Engine 状态全在那 |

## 你每次启动要做的事

```bash
# 1. 确认 M1 上的本地版本
cd /Users/apple/program/CCC && cat VERSION && git log -3 --oneline

# 2. SSH 确认 Mac2017 服务正常
ssh mac2017 "cd /Users/fan/program/CCC && cat VERSION && launchctl list | grep ccc"

# 3. 如果两边 VERSION 不一致，原因是什么？→ 问我，不是自己 merge

# 4. 所有 Engine/Board 行为排查一律走 ssh mac2017，不在本地臆测
```

## 禁止

- ❌ 说「M1 上跑 Engine」— M1 不跑 Engine
- ❌ 说「本地改了等同步」— 改完 push，开发工具（Claude/OpenCode）在 2017 合入
- ❌ 臆测 Mac2017 上的代码状态 — ssh 看，或查 git log
- ❌ 在 M1 上改完直接 ssh 到 2017 执行 —— 合入走开发工具（Claude/OpenCode），你只改 M1 副本
- ❌ 往 Mac2017 的 `/Users/fan/program/CCC/` 直接推 commit（那是开发工具（Claude/OpenCode）的合入目标）
- ❌ 说「两棵树自动同步」— 没有自动同步，M1 push → 开发工具（Claude/OpenCode） pull → 合入

## 你改代码的流向

```
M1 改代码 → git add + git commit + git push
   → 开发工具（Claude/OpenCode）在 Mac2017 上 git pull → 合入 → 跑测试 → 验证
   → 生效
```

你的 commit 到达 Mac2017 需要经过开发工具（Claude/OpenCode）合入，不是直通。
