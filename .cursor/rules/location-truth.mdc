---
description: CCC 位置真相 — 2017 = 执行写码节点（engine worktree）+ 生产，M1 = 中枢
globs:
alwaysApply: true
---

# CCC 位置真相（新栈 · 2026-08-05）

> 硬规则。不讨论，不商量。认错位置 = 做错事。

## 一句话

**2017 = 执行写码节点（engine worktree）+ 生产 :7788；M1 = 中枢出卡/验收/合入/看板 + 轻量开发；业务仓（qb 等）本体机器写码。**

## 具体

| | M1（中枢） | Mac2017（生产与执行节点） |
|---|---|---|
| 角色 | 中枢出卡/验收/合入/看板 + 轻量开发 | 生产运行 + Engine 派发 + 机审 + 执行写码（engine worktree） |
| 访问方式 | M1 终端 | `ssh fan@192.168.3.116` / 2017 终端 |
| CCC 源码 | `/Users/apple/program/CCC/`（git 主仓，push 到 GitHub） | `/Users/fan/program/CCC/`（生产/执行副本，只 pull --ff-only） |
| 跑什么 | M1 IDE（Claude/OpenCode）+ 浏览器看 2017 看板；Desktop 暂缓 | web-server :7788 + engine + board-scheduler（三 launchd）+ 中继 6100/6102 |
| 开发怎么走 | ccc-plan → plan-to-cards → push | 2017 Engine 自动 pull 派发 OpenCode 在 worktree 内写码 → 机械门禁 → 自动机审 → 准备合入 |
| 你怎么查 | M1 浏览器看看板/运维 | 日志 `/Users/fan/.ccc/logs/`、看板 :7788 |

## 你每次启动要做的事

```bash
# 1. 确认 M1 本地版本
cd /Users/apple/program/CCC && cat VERSION && git log -3 --oneline

# 2. SSH 确认 2017 服务正常（不再有 Hub :7777 / Board :7775 / relay.m1）
ssh fan@192.168.3.116 "cd /Users/fan/program/CCC && git log --oneline -1 && launchctl list | grep com.ccc"

# 3. 两边 HEAD 不一致 → 以 main 为准，M1 push → 2017 pull（部署流程 release.sh 负责）

# 4. Engine/看板行为排查一律走 ssh 2017（日志 /Users/fan/.ccc/logs/），不在本地臆测
```

## 禁止

- ❌ 说「M1 上跑 7788/Engine」— 生产服务只在 2017
- ❌ 说「2017 直接手改」— 2017 生产副本只 pull；开发走 Codex 出卡 → Claude Code worktree
- ❌ 臆测 2017 代码状态 — ssh 看，或查 git log（`ssh fan@192.168.3.116 "git -C /Users/fan/program/CCC log --oneline -1"`）
- ❌ 提旧拓扑（Hub :7777 / Board :7775 / relay.m1 / hub-tunnel / **Hub 写码槽**）— 已全部退役（**OpenCode CLI 执行体仍可用**，勿与旧 Hub「OpenCode 槽」混称）

## 改代码的流向

```
ccc-plan → plan-to-cards → push → 2017 自动 pull（CCC_AUTO_PULL）
  → Engine 派发 OpenCode → 机械门禁 → 已回写 → Claude 机审 → ready_for_merge
  → 老板「合入批准」→ approve-merge → 2017 pull + 重启部署
```

你的改动到达 2017 生产必须经过「拆卡 → 执行 → 机审 → 合入批准 → 部署」全链，不是直通。

**合入批准**：`scripts/approve-merge.sh` / [`docs/product/north-star-slice.md`](docs/product/north-star-slice.md)；机审只在 2017 Engine。
