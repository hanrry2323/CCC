---
description: CCC 开发共识（2026-08-05 新栈重写）——当前架构、开发通道、任务卡体系、红线（始终应用）
alwaysApply: true
---

# CCC 开发共识（新栈 · 2026-08-05）

> 旧版（v0.65/v0.66 Hub 口径）已归档：`docs/archive/ccc-cursor-legacy-2026-08-05/`。本文为当前基线；冲突一律以本文 + `docs/INDEX.md` §0 + `docs/DOC-PROTOCOL.md` + `docs/roadmap.md` 为准。

## 项目一句话

CCC = 自动化任务编排平台：薄驱动 Engine + Markdown 任务卡（唯一事实源）+ HTTP 看板/运维实时面 + 2017 单端生产。M1 IDE = 开发中枢（老板只聊意图+出卡，只看板/中继/Δ）；中间 pull/派发默认自动（`CCC_AUTO_PULL`）。Desktop 壳暂缓。开仓 SSOT：`CLAUDE.md`「开仓作战卡片」。

## 文档与项目注册（硬）

- 读写规范：`docs/DOC-PROTOCOL.md`（落点表外禁止新建）
- 项目真值：`docs/projects/registry.yaml` + `docs/projects/<prefix>/README.md`
- 禁止双写 PREFIXES / `_is_taskable` / `docs/kb-seed`；禁止口头注册项目
- 详见始终规则 `.cursor/rules/doc-protocol.mdc`

## 当前架构（2026-08-06 基线）

- **版本**：v0.70.0（根目录 VERSION；版本号只在 CHANGELOG 补丁）
- **代码分布**：M1 `/Users/apple/program/CCC` = 开发副本（git → GitHub main）；2017 `/Users/fan/program/CCC` = 生产副本（只 pull、不手改）
- **单端生产**：2017 `:7788`（web-server / engine / board-scheduler 三 launchd 服务）；大脑对话走 2017 中继 6100/6102；**M1 不跑 7788，无 Hub :7777、无 sidecar**
- **任务卡 = 唯一事实源**：`docs/dispatch/TNN-*.md`；卡头五态：待分派 / 执行中 / 已回写 / 已关闭 / 打回；看板是派生视图（loader 扫描卡 → /board API），不存数据不做决策
- **北星流程**：`ccc-plan` → plan-to-cards → Engine+机审静默 → ready_for_merge → 人审 diff →「合入批准」→ 合入部署
- **人机实时面**：HTTP `:7788` 看板 + 运维（主路径）；**Desktop 暂缓、非主路径**
- **cwd 铁律（M1）**：做 CCC 必须打开 `/Users/apple/program/CCC`；禁止把 `qx-map` 等其它仓静默当写源
- **免登录**：`CCC_WEB_AUTH_REQUIRED=0`（单用户局域网直连，全端点免鉴权）
- **合入硬路由**：「合入批准」（旧称「验收看板」）→ `scripts/approve-merge.sh` / [`docs/product/north-star-slice.md`](docs/product/north-star-slice.md)；质量靠机审，禁止代写机审区。进度认 2017 API。

## 席位与工具（硬 · 唯一事实源 /Users/apple/qx-map/ide/tool-roles.md）

- **2017 开发** = **OpenCode**（默认）；点名 Claude 可开发
- **2017 机审** = 卡头验收方（默认 Claude）写 `## 机审区`（Engine 回写后自动拉起）= **质量门**
- **合入批准** = 人审 diff 后 `approve-merge.sh`（旧称验收看板）
- **难度突击** = `Cursor`：写码；不代关卡
- **Codex** = 出卡/裁决
- **主 IDE** = 中枢（plan-to-cards + 合入批准）
- **Trae** = 停用；**Desktop** = 暂缓
- **双轨独立**：QuantHive 与 CCC 完全独立
- **出卡前了解**：[`docs/product/hub-context-sop.md`](docs/product/hub-context-sop.md)
- **冻结**：禁止新增 Agent 心智补丁 SOP（INDEX §0）
## 任务卡体系（T54+ 规则，本仓代码实现在 server/board/）

- 命名：`<前缀><三位序号>-<slug>.md`（前缀以 `docs/projects/registry.yaml` 为准；代码 `PREFIXES` 派生）；旧卡保持原名不迁移
- 卡头必含：关联 / 执行体 / 验收 / 状态 / 派发 / 项目 / 日期；**新卡验收须与执行体交叉（OpenCode↔Claude Code）**
- **已验收卡门禁（T67）**：卡含 `## 验收区` 且 20 行内有 `✅` / `判定：通过` → 卡头状态必须 = 已关闭
- 交叉验收门禁：`server/board/roles.py` + `validate.py`；出卡：`scripts/new-card.sh`（默认 OpenCode/Claude Code）

## 硬红线

1. **不写 QuantHive 业务代码**；**禁止**经 CCC 为 QuantHive 出卡/Engine 派发（`qh` 已移出 PREFIXES）
2. 2017 生产副本只 pull 不手改；远程 worktree 不随意删
3. 中继只走 6100/6102；不碰旧端口/旧服务（7777/7775/7788-M1/4000/4100）
4. 回写前 push 成功；机械门禁卡 commit/diff；终验独立取证；Codex/Cursor 不验收
5. **旧栈已退役**——不要按旧文档/旧规则行事：`scripts/`（已归档 docs/archive/ccc-legacy-2026-08-02/scripts/）、Hub :7777、6+1 列 jsonl、能力包、角色分层
6. 无授权不删改归档目录；历史文档带「待核/历史归档」标记的按红线 #4 不扩写

## 工作方式

- 结论先行、人话汇报；改代码前先读 `docs/INDEX.md` §0；读写文档先读 `docs/DOC-PROTOCOL.md`
- 分步提交（每块 commit+push），禁止 `git add -A` 全量提交；提交前 `git status` 核对无他人改动混入
- 本机 Cursor = **难度突击**（不代关卡）；合入批准走 `approve-merge.sh` / north-star-slice
