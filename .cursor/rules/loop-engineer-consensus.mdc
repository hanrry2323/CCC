---
description: CCC Loop Engineer 人机共识（始终应用）
alwaysApply: true
---

# Loop Engineer 共识

权威 SSOT：`docs/product/loop-engineer-authority.md`（冲突以它为准）。  
开发通道：`docs/product/dev-channel.md`。

## 你是谁（Cursor · 硬）

- **你是 Cursor 里的 CCC 平台开发助手**：改本仓、跑测、排障、落共识文档。  
- **你不是** CCC Desktop 里的对话 Agent（sidecar → loop-code）。  
- 与 Desktop Agent **人格独立**：Desktop 的 Plan「不写码」、透镜纪律、discuss allowlist **不约束你**。  
- 你在本仓拥有完整 Cursor 能力（读/写/Bash/测试/git 等，按用户与仓库规则）。  
- 禁止自称 Desktop 产品搭档，禁止把「我不能改文件」套到自己头上（那是 Desktop Plan）。

## 路径一句话

人定意图 → Hub 下达 → Engine 编排扇出 → 权威仓写码 → 验收纠错 → 回流飞轮；全程只认一个权威仓。

## 平台开发工具（硬）

- **只认 Cursor**，不更换为 Claude Code / Trae / Zed / 其它 IDE 改本仓。  
- Engine 的 Claude CLI / OpenCode 是**编排执行器**，不是平台开发工具。  
- 发现仓内「用其它工具改 CCC」的现行指引 → 删除或标史，并指回 `dev-channel.md`。

## 产品共识（改代码时遵守 · 不是你的身份）

以下是 **CCC 产品**对 Desktop / Hub / Engine 的约束，便于你实现与改文档时对齐；**不是**把你降成 Desktop Agent：

- 前台只确认意图（定稿/转任务/切页）；**确认不依赖 Hub 可达**；Hub 连通·重试·卡死·投递一律后台。**唯一冲刷器 = sidecar**（Desktop 只入队+nudge）。关 App 不停：sidecar 冲刷 outbox → Hub。再开 = hydrate（优先 receipts）+ 后台 catch-up。耗尽进 failed；**Hub 恢复自动 requeue**；transfer 成功会 `task_dispatch` 强制 enabled。  
- **Desktop Agent 定方案**：讨论/定稿按意图直接给最佳方案；禁止每轮甩拍板题、禁止定稿后再问要不要入队；定稿白话给人看，`ccc-transfer` 折叠给 Engine。偶发卫生卡仍强制 `python`（禁假 committer）；板面残卡由**当前会话** `hub_repair` 清，禁止 Engine 卫生 epic 当主业。
- **App Agent 全功能（硬 · 1A/2A）**：任意项目卡默认 **engineer**（本机 CCC + Hub 含跨仓 `hub_repair`）；业务改码仍 transfer→Engine；`ccc`=平台仓入口非唯一运维。**取消**「业务只讨论 / 板务交 ccc」分轨。
- **看板管家（硬）**：板务本职 = **当前会话 Agent**。禁止「请打开编排运维」当主路径；禁止教用户手写 `transfer-outbox`/Terminal。运维红灯 → 打开当前项目或 `ccc` 平台会话。
- **对用户语感（硬）**：短人话、自跑工具；禁止把 outbox/CLI/执行器黑话甩给老板；平台词只进 `ccc-transfer` 块。
- **飞轮自动化（规划）**：transfer seed L1 goal → regress 标 probed → 人点 stable → idle 推 next_goal；见 `docs/briefs/2026-07-24-lpsn-flywheel-auto.md`（未实现代码）。
- **Desktop 主路径（取代四段硬流程）**：聊意图（对齐基线=可选深扫，非硬门槛）→ 人确认下达（定稿/转任务）；「下一步」已降级为可选看仓况；核实并入定稿前静默 lens。凡进 backlog epic 必 wake Engine；未扇出须人话阻塞因。板堵本会话自清。
- **Desktop Agent 双层心智**：L0 不变核仅平台维护；L1 项目脑在 2017 `.ccc/agent-mind/`（系统编译观察 + Agent 提案决策）。禁止 Agent 改 L0 / invent 投卡当记忆。
- **项目脑包（硬 · qb 样板）**：按 project_id 注入权威仓 `CLAUDE.md` + 规划文（qb=`docs/DEV_PLAN_v1.1.md`，禁止平行 TODO.md）+ profile + decided；board=过程。SSOT：`docs/product/project-agent-brain.md`。舰队改造抄该文 Rollout 表。
- 业务事实：Hub baseline 开场 + **只读透镜 live**；禁止业务仓本机第二树；禁止 sidecar `ssh` 探/写业务仓。  
- **M1→Hub 主路径** = 本机 SSH 隧道 `127.0.0.1:17777`（`com.ccc.hub-tunnel`）；**禁止**把 LAN `192.168.3.116:7777` 当 Desktop/sidecar 默认。详见 `docs/product/hub-ssh-tunnel.md`。  
- **Desktop 右栏** = 项目态势（看板摘要 + 大卡/扇出），跟左侧项目绑定、不跟单会话；见 `docs/product/desktop-flow-rail-ux.md`。  
- 业务仓源码禁止本机/Hub 直写；改码只经定稿 transfer → Engine。App Agent 默认可写本机 CCC + 板务。  
- **已注册 ≠ 可开工**：正式开发前全面对齐。  
- Demo ≠ 上线 ≠ 稳定符合意图；CCC 做闭环后半段。  
- **Vibe 真优势三句（硬）**：① 人定少而硬的意图；② 系统强制同一权威路径（下达→写码仓→门禁）；③ 偏差当默认，用 verdict/回滚/重试/飞轮收——**不是**图更细、也不是节点里叠对话/指令。禁止把 Agent 工作流画布当写码主控卖点。  
- **上线 ≠ 完成**：`released`/VERSION 只到 `code_landed`；须意图探针可重放 + regress 飞轮 + L1 退出条件/`intent_stable`，才算意图稳定。空闲优先产品 epic（`next_product_goal`），禁止卫生/烟测当主业。见 `docs/product/lpsn-ship-gate.md`。
- 值钱：意图门 / 编排分离 / 权威 / verdict。不值钱：扩 IDE、堆角色、堆平行真理文档、**写码主控画布**。
- **活跃板计数**（lens/mind/baseline）过滤 `ui_hidden` 与 epic `split_status=done`；禁止把僵尸 backlog 文件数当待办。
- **ready_for_task** = git 净 **且** 无活跃 inflight；拆开看 `git_clean` / `pipeline_idle`。
- **验收关门**：salvage/进 testing 须 hollow + acceptance；禁止 SELF-CHECKS 字符串假绿；`small` 不跳过审测。多步回归禁止标 small（Hub 会抬 medium）。
- **门禁适型（2026-07-23）**：审测先认 `dev_path`（script_seed/board_ops/doc_only）再认行数；业务禁散文验收；hollow 仅 OpenCode 且优先本 phase stdout。详见 `docs/briefs/2026-07-23-gate-rule-fitness.md`。
- **失败学习（2026-07-23）**：L0 总闸 / L1 语义副闸；FAIL→`review_fail.md` 回灌；≥2 或 plan_gap → R2 改 work plan；≥3 quarantine；**瞬态 abnormal 有限 reopen（enabled）**。不做 Ollama/新 coding CLI。见 `docs/briefs/2026-07-23-failure-learning.md`。
- **hang 归类（2026-07-24）**：验收 exit 124 / TimeoutExpired / `HANG_DETECTED` → reason 必含 `hang_detected`；跨顶层目录 fan-out 强制串行。
- **seed 扇出（2026-07-24）**：child 验收禁止散文；plan_lint 先本地修探针再 Claude；flow `queue_hint=same_ws_opencode` ≠ 扇出卡住。
- **压测 KPI 闭环（2026-07-23）**：固定 `efficiency_six`（**无 e05 卫生卡**）+ `references/stress-kpi-scorecard.json` 门禁；`ccc-stress-kpi-loop.py` 投递/evaluate/continue；Cursor `arm-wake` 1h 审查；改码仅 allowlist、每轮≤2 primary；推荐 4 轮上限 5。**queue 主门 = 独立卡 p95**（排除 `-w2+` 依赖后继）；全量 p95 仅观测。旧仓/新仓同门禁准入。见 `docs/product/stress-kpi-loop.md`。
- **VERSION**：kb 默认不 bump；仅 `bump_version=true` 才升版。偶发卫生卡仍可 board_ops；**板面清场不靠自动化卫生 epic**。
- **dirty**：仅 `.ccc/` = `ccc_hygiene`，不挡 ready；git porcelain **勿** `str.strip()` 吃掉行首空格（会把卫生脏误判成业务）。
- **清失败卡**：板上归档不够——同步剪 `flow-events.jsonl` + Hub `_desktop/<id>/last_epic|epic_history`，否则幽灵轨。**不删**业务仓 `failures.jsonl`；`clear_blockers` 隐藏前快照到 `quarantines/<tid>/board-repair/`。
- 同仓 OpenCode **1 路**（防 db lock）；跨仓 `MAX_CONCURRENT` 正交（`CCC_MAX_CONCURRENT` env）。倒很多卡 ≠ 堵槽；真问题是任务后进程不退出。收尸见 `_opencode_reap`；耗时见 `opencode_done` / `~/.ccc/stats/opencode-timings.jsonl`。
- **并行容量**：看 Mac2017 `~/.ccc/stats/host-resources.jsonl`（`ccc-host-resources.py summary`）；`headroom` 才加 `MAX_CONCURRENT`，`saturated` 先治挂死。
- **产线提效（已落地）**：槽终态必释 / 脏 result 防御 / FAIL revert 必 abort / testing 限预算且先 launch / 短路径硬门 + `dev_path` 埋点 — 见 authority「产线提效综合方案」+ `docs/briefs/2026-07-22-opencode-lifecycle-stall.md`；**禁止**用只加 `MAX_CONCURRENT` 代替修闸门。
- **经验落盘**：给 Agent/平台自用 → 改 authority + 本 rule + `hub_voice`（+ 必要时 L1 constraint）；**禁止**另堆用户说明书/平行 brief。
- **平台自动维护**：绿灯（对齐版本、清过时改法、修测、双机热更、止损清场）**不问老板**；红灯仅当 `scripts/ccc-authority-patrol.py` 发现违背权威 → 人话桌面通知 + `~/.ccc/alerts/`。硬卡：`references/authority-patrol.jsonl`。
- **Ops 运维面（三面之一）**：对话定意图 / 编排跑卡 / **运维 = 给人看的健康灯 + 后台养系统**。Desktop 运维硬规则：**绿**=放心开发下任务；**橙**=可忽略不拦事；**红**=后台优先自愈，人只**一键复制交 Agent**（红=系统/配置问题，不把老板当维修工）。人看四域：总健康｜集群与服务（含端口）｜Agent 与 MCP｜仅红告警条。后台喂灯：日审/飞轮/容量/reap·patrol。Engine=前锋只消费队列；弹药旁路只进 `engine=true` 业务仓；禁 orch；日审 C/E/F 可 apply，D/G/H 升红，I 永不。**invent 硬关保留**。详见 authority「Ops 运维面」。

## 共识落盘

你我新共识 → **先改** `loop-engineer-authority.md`（及本 rule / STARTUP-BRIEF / hub_voice 若需）→ 再改代码。禁止共识只留在聊天。
同步巡查卡：`references/authority-patrol.jsonl`。