---
description: 文档与项目注册硬规范——读写项目文档必须按 DOC-PROTOCOL / registry
alwaysApply: true
---

# 文档与项目注册（硬 · 始终应用）

读写项目文档、注册/改项目、改出卡前缀或路径时，**必须**遵守：

1. `docs/DOC-PROTOCOL.md` — 写哪里 / 怎么写 / 禁写哪里  
2. `docs/projects/registry.yaml` — 项目唯一事实源  
3. `docs/projects/<prefix>/README.md` — 每项目一页（五节封顶）

## 落点（摘要）

| 意图 | 只写这里 |
|------|----------|
| 共识/权威 | 先 `docs/INDEX.md` §0 |
| 下一程 | `docs/roadmap.md`「下一程挂账」一行 |
| 注册项目 | `registry.yaml` + 对应 README |
| 开发任务 | `docs/dispatch/<prefix>/` 出卡 |
| 临时 | `docs/notes/`（7 天内处理） |
| 史 | `docs/archive/` |

## 禁止

- 落点表外新建说明/设计文或 `docs/qb/` 类业务深文档树  
- 只改 `PREFIXES` / `_is_taskable` / `kb-seed` 而不改 registry  
- 口头注册项目；把 Hub 史文、`docs/kb-seed/` 当现行  

最短路径：`INDEX §0` → `DOC-PROTOCOL` → 档案或出卡。
