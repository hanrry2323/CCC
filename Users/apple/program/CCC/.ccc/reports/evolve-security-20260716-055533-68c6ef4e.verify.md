# Phase 1: 验证 weak SHA1 修复 (B324)

## 验证结果

### 1. `usedforsecurity=False` 已到位 ✅
```bash
grep -n "hashlib.sha1" scripts/_evolve.py
```
输出：`205:    short = hashlib.sha1(fp.encode("utf-8"), usedforsecurity=False).hexdigest()[:8]`
确认参数正确传递。

### 2. bandit 不再报 B324 ✅
```bash
bandit -q scripts/_evolve.py 2>&1 | grep -c "B324" || echo "0 B324"
```
输出：`0 B324` ✅

### 3. Python 3.14 编译通过 ✅
```bash
python3 -m compileall -q scripts/_evolve.py
```
无输出（编译通过） ✅

### 4. import 正常 ✅
```bash
python3 -c "import sys; sys.path.insert(0,'scripts'); from _evolve import _post_finding; print('import OK')"
```
输出：`import OK` ✅

## 语义正确性确认

- **用途**：SHA1 仅用于生成 task ID 的 8 字符短指纹（line 205）
- **安全标签**：已显式标注 `usedforsecurity=False`
- **分类**：非安全用途（task ID 索引，非密码学哈希签名）

## 验收清单完成度

- [✅] `usedforsecurity=False` 已到位
- [✅] bandit 不再报 B324
- [✅] 语义正确：SHA1 仅用于 task ID 短指纹，非安全用途
- [✅] Python 3.14 编译通过
- [✅] 无其他 SHA1 调用点受影响（仅此一处）

## Commit

```bash
git add scripts/_evolve.py
git commit -m "verify(security): confirm usedforsecurity=False on SHA1 in _post_finding (B324) [evolve-security-20260716-055533-68c6ef4e]"
```
