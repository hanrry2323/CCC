# DEPRECATED — 免费模型目录（档案）

> **现行**：付费-only Go 单活跃钥。见 [`docs/relay/KEY-POOL.md`](../../docs/relay/KEY-POOL.md)。  
> 本目录 `free-models.json` / `code-pool.curated.json` **仅档案**，禁止再注入启用池。  
> `catalog.ts` 保留加载能力但不参与热路径配置。
