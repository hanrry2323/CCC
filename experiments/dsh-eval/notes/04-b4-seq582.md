# 实验 B4 · seq582 矛盾单点收口

- **状态**：✅ 完成
- **批次**：B2 链路
- **环境**：会话日志（只读分析）
- **日期**：2026-08-16

## 结论

**seq582「带 description 却报缺」是误读，不存在矛盾**。该调用的 `run_code` 顶层 arguments 只有 `['code']`，**没有顶层 description**；raw 文本里可见的 `description: "Fetch origin..."` 是 **code 内部 bash 内层工具的 description**。报 `missing required property "description"` 完全正确。

## 方法

解析 `session-5a3a8ff3` 的 seq582（tool/call）、seq581（assistant 生成）、seq583（tool/result），对比三层。

## 证据

```
assistant/generated@581 arguments 顶层 keys: ['code']      ← 模型生成时就没顶层 description
tool/call@582         arguments 顶层 keys: ['code']      ← agent loop 收到的一致（零传输丢失）
tool/result@583       → "missing required property description"
```
- raw 里出现的 `description: "Fetch origin 并对比本地与远端提交差异"` 在 code 字符串内（是 `tools.bash({...})` 的子参数）。

## 结论细节

- 这正是一个**完美的混淆机制实例**：模型写 code 时给 bash 工具填了 description，看到自己代码里有 description 就以为 run_code 的也齐了 → 顶层忘写 → 校验器拒绝。
- 与全量统计一致：116 次调用，66 次顶层无 description（全失败）、50 次有（全成功），零例外。

## 风险 / 对 CCC 借鉴的影响

- 报告维度十 B4 项是基于对 raw 文本的误读产生的"疑点"，现关闭。description 根因结论无异常点，可放心作为已解决项。
- 对 CCC 出卡/审卡的启示：看 raw 工具参数时，区分「顶层参数」与「code 字符串内嵌文本」需要 JSON 解析而非肉眼——文档/审计工具应强调结构化解析。
