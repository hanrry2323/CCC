你是 CCC Verifier(独立 claude -p session)。

# 验证 cluster-bus-bugfixes

跑这 4 个命令,把输出收集起来:

```bash
# Probe 1: JSONL bug 修了
cat > /tmp/t.json <<'EOF'
{"phase":1,"status":"pending","files":["a"],"commit":null,"commit_message":"t"}
{"phase":2,"status":"pending","files":["b"],"commit":null,"commit_message":"t"}
EOF
bash /Users/apple/program/CCC/scripts/ccc-exec-commit.sh /tmp ccc-vfy 2>&1 | tail -3

# Probe 2: GC 代码存在
grep -c "GC_THRESHOLD" /Users/apple/program/CCC/scripts/cluster-bus.py

# Probe 3: include_stale 参数
grep -c "include_stale" /Users/apple/program/CCC/scripts/ccc-dispatch.py

# Probe 4: sidecar task_id 文件
ls /Users/apple/program/CCC/.ccc/phases/cluster-bus-bugfixes.phases.json.task_id 2>&1 | head -1
```

红线 6 验证:
```bash
EXEC=$(cat /Users/apple/program/CCC/.ccc/plans/cluster-bus-bugfixes-executor-session-id.txt)
MY=$(cat /Users/apple/program/CCC/.ccc/plans/cluster-bus-bugfixes-verifier-session-id.txt)
[ "$EXEC" != "$MY" ] && echo "ISOLATION_OK" || echo "ISOLATION_FAIL"
```

写 verdict.md 到 `/Users/apple/program/CCC/.ccc/verdicts/cluster-bus-bugfixes.verdict.md`,格式:

```
# Verdict — cluster-bus-bugfixes

## ISOLATION_OK (or FAIL)

## VERDICT: PASS|CONDITIONAL_PASS|FAIL

## Probe 1 — JSONL bug
- 结果:
- 证据: (上面输出)

## Probe 2 — GC 机制
- 结果:
- 证据:

## Probe 3 — include_stale
- 结果:
- 证据:

## Probe 4 — sidecar task_id
- 结果:
- 证据:

## 总结
- X/4 通过
- 最终 VERDICT
```

完成后 echo:
```
VERIFIER_RESULT: success
VERDICT_FILE: /Users/apple/program/CCC/.ccc/verdicts/cluster-bus-bugfixes.verdict.md
PROBES_PASSED: <N>/4
```