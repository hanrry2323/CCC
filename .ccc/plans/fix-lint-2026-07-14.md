# Plan: fix-lint-2026-07-14 — 修复 ruff 扫描发现的 32 处 lint 问题

> 撰写：20-min patrol (ccc-product 自动化) | 执行：ccc-dev（auto）
> 触发：20 分钟轮询 — ruff 自动扫描 `scripts/` 发现 32 处可自动修复的 lint 问题
> 复杂度：small（机械操作，无行为变更）

---

## 当前代码状态

- 入口：`scripts/`
- ruff 扫描：`ruff check scripts/ --select F401,F841,F811`
- 共 32 处问题，分布：

| 文件 | 处数 | 问题 |
|------|------|------|
| `scripts/ccc-board-server.py` | 10 | 多为 F401（unused import） |
| `scripts/ccc-cockpit.py` | 4 | F841（unused variable） |
| `scripts/_board_store.py` | 2 | F401 |
| `scripts/phase_lint.py` | 2 | F841 |
| `scripts/_stats_aggregator.py` | 2 | F841 |
| `scripts/ccc-board.py` | 2 | F841 |
| `scripts/_config.py` | 1 | F841 |
| `scripts/ccc-engine.py` | 1 | F841 |
| `scripts/opencode-exec.py` | 2 | F841 |
| `scripts/gen-tauri-icons.py` | 1 | F401 |
| `scripts/tests/test_quarantine_archive.py` | 3 | F401/F841 |

19 处可由 ruff `--fix` 自动修（[*] 标记），其余 13 处需手工判断（变量是否真未用）。

---

## 范围

- **目标**：消除 `ruff check scripts/ --select F401,F841,F811` 的所有 32 处告警
- **只改文件**：上述 11 个文件
- **不改文件**：`templates/`、`docs/`、`.ccc/` 配置
- **执行方式**：`auto`（机械 fix）

## Phase 1：执行 ruff --fix 自动化修复（19 处）

### 做什么

```bash
ruff check scripts/ --select F401,F841,F811 --fix
```

### 验收清单

- [ ] ruff 自动修复后剩余 ≤ 13 处
- [ ] 语法检查：所有改动文件 `python3 -c "import ast; ast.parse(...)"` 通过
- [ ] 测试通过：`uv run pytest tests/scripts/ -q --ignore=tests/e2e` ≥ 240 passed

## Phase 2：手工修复剩余 13 处 F841

### 做什么

逐处检查每个 F841 告警：变量是否真的没用？是删除赋值还是加 `_` 前缀？

### 验收清单

- [ ] `ruff check scripts/ --select F401,F841,F811` 输出 0 errors
- [ ] 所有测试通过

---

## 全局验收

- [ ] `ruff check scripts/ --select F401,F841,F811` 0 errors
- [ ] `uv run pytest tests/scripts/ -q --ignore=tests/e2e` ≥ 240 passed
- [ ] 每个 phase 一个 commit，commit msg 包含 `phase=N`
- [ ] diff 仅触及 11 个目标文件