# Plan: [v0.7-slim — CCC 瘦身 80→15]

> 撰写：ccc-planner | 执行：Claude（manual）

---

## 范围

- **目标**：把 CCC 从 80+ 文件瘦身到 15 个核心文件，砍掉为"路线预留"而存在的过度工程化代码
- **只改文件**：
  - 删除：`scripts/cluster-bus.py` + `scripts/ccc-znode-register.py` + `scripts/ccc-zcode-bridge.sh` + `scripts/ccc-zcode-orchestrate.sh` + `tools/cluster-doctor.sh`
  - 删除：`references/cluster-protocol.md` + 整套适配器 md（保留 `runtime-opencode.md`，其余删除）
  - 删除：`scripts/ccc-dispatch.py` + `scripts/ccc-hook.sh` + `scripts/ccc-scheduler.sh` + `scripts/hello-ccc.sh`
  - 删除：`.ccc/dispatches/` 整个目录
  - 删除：`scripts/flywheel-scan.py` + `scripts/ccc-cost-report.sh` + `scripts/ccc-cost.sh`
  - 删除：`tests/cluster/*` + 派单/飞轮/集群相关测试
  - 删除：`scripts/precommit-bash-quality.sh` + `scripts/precommit-verdict-length.sh`
  - 删除：`.claude/worktrees/oral-calc-commit/` 整个 worktree
  - 修改：`CLAUDE.md` + `README.md` + `profile.md` + `state.md`（更新资产清单）
  - 修改：`scripts/ccc` wrapper（删掉对已删脚本的引用）
  - 保留：`SKILL.md` + `templates/`（6 个） + `references/red-lines.md` + `docs/lessons.md` + `scripts/ccc-precheck.sh` + `scripts/ccc-finish.sh` + `scripts/executor-watchdog.sh` + `scripts/ccc-exec-commit.sh` + `scripts/ccc-init.py` + `scripts/ccc-search.py` + `scripts/ccc-status.sh` + `scripts/ccc-task-done.sh` + `tests/scripts/` 中保留 4 个核心测试
- **不改文件**：`SKILL.md`（入口不动）/ `references/red-lines.md`（宪法不动）/ `.ccc/profile.md` 已读但仅追加新状态
- **执行方式**：`manual`
- **Phase 数**：4

---

## 改动 1：删 cluster 总线整套（A 类）

### 做什么
砍掉"跨设备 SSH 调度"路线相关全部文件。当前本地 4 窗口用不上，未来要做就从头重写更简单的版本。

### 怎么做
- 删除 6 个脚本：`scripts/cluster-bus.py` + `scripts/ccc-znode-register.py` + `scripts/ccc-zcode-bridge.sh` + `scripts/ccc-zcode-orchestrate.sh` + `tools/cluster-doctor.sh` + `tests/cluster/` 整个目录
- 删除 1 个文档：`references/cluster-protocol.md`
- 删除 `scripts/cluster-bus.py.md` + `tools/cluster-doctor.sh.md` + `scripts/ccc-znode-register.py.md`（这些是 .md 文档化版，CCC 没这约定，不留）
- 验证 `scripts/ccc` wrapper 不引用上述命令

### 验收
- `ls scripts/ | grep -E "(cluster|znode|zcode|doctor)"` 返回空
- `ls tests/cluster/ 2>/dev/null` 返回 No such file or directory
- `git status` 仅显示本 phase 范围内的文件被删

---

## 改动 2：删多 IDE 适配器整套（B 类）

### 做什么
10 个适配器 md 砍到 1 个（只留 `runtime-opencode.md`），剩下的就是这一份"opencode 怎么用 CCC"的文档。

### 怎么做
- 删除：`references/adapters/runtime-cursor.md` + `runtime-claude-p.md` + `runtime-zcode.md` + `runtime-claude-code.md` + `scheduler-launchd.md` + `scheduler-mavis-cron.md`
- 保留：`references/adapters/runtime-opencode.md`（如不存在则创建一份精简版）
- 处理 `.claude/worktrees/oral-calc-commit/references/adapters/`（worktree 副本整个 worktree 在改动 4 删）

### 验收
- `ls references/adapters/ | wc -l` 等于 1
- `git status` 仅显示本 phase 范围内文件被删

---

## 改动 3：删派单 + 编排 + 飞轮 + 成本 + 异常报告（C/D 类）

### 做什么
把"启动 Executor"退化为"直接 spawn 子进程 + 喂 stdin"。杀鸡用牛刀的派单系统、UUID 命名、状态文件全部砍掉。飞轮和成本报告是 v0.5 路线，没跑过不维护。

### 怎么做
- 删除 4 个脚本：`scripts/ccc-dispatch.py` + `scripts/ccc-hook.sh` + `scripts/ccc-scheduler.sh` + `scripts/hello-ccc.sh`
- 删除整个目录：`.ccc/dispatches/`
- 删除 3 个脚本：`scripts/flywheel-scan.py` + `scripts/ccc-cost-report.sh` + `scripts/ccc-cost.sh`
- 删除 5 份异常报告 `.ccc/abnormal-reports/*.md`（保留目录，里面清空）
- 删除对应 .md 文档化版本：`scripts/ccc-dispatch.py.md` + `scripts/ccc-hook.sh.md` + `scripts/ccc-scheduler.sh.md` + `scripts/hello-ccc.sh.md` + `scripts/flywheel-scan.py.md`
- 删除测试：`tests/scripts/test_ccc_dispatch_smoke.py` + `test_ccc_zcode_*`（4 个）+ `test_flywheel_scan_smoke.py` + `test_ccc_zcode_orchestrate_smoke.py` + `test_ccc_zcode_bridge_smoke.py` + `test_ccc_znode_register_smoke.py` + `test_cluster_bus_smoke.py` + `test_cluster_bus_benchmark.py`
- 保留的 4 个核心测试：`test_ccc_precheck_finish_smoke.py` + `test_ccc_exec_commit_smoke.py` + `test_ccc_exec_commit_idempotency.py` + `test_ccc_exec_commit_jsonl_smoke.py` + `test_executor_watchdog_smoke.py` + `test_ccc_init_search_smoke.py` + `test_ccc_status_smoke.py` + `test_ccc_tooling_hooks_smoke.py` + `test_integration_business_flows.py`
- 删除 2 个 precommit 钩子：`scripts/precommit-bash-quality.sh` + `scripts/precommit-verdict-length.sh`
- 删除对应 .md 文档：`scripts/ccc-cost-report.sh.md` + `scripts/install-ccc-as-skill.sh.md`（如不再用）

### 验收
- `ls scripts/ | wc -l` 在 12-15 之间（精简后）
- `ls .ccc/dispatches/ 2>/dev/null` 返回 No such file or directory
- `ls .ccc/abnormal-reports/` 返回空
- `pytest tests/scripts/ -k "not cluster and not zcode and not dispatch and not flywheel"` 全绿

---

## 改动 4：清理 worktree + 文档同步

### 做什么
清掉 `.claude/worktrees/oral-calc-commit/` 完整副本；同时更新 CLAUDE.md / README.md / profile.md / state.md 的资产清单反映精简后状态。

### 怎么做
- 验证 worktree 是否还有未提交内容（`git -C .claude/worktrees/oral-calc-commit status`）
- 验证后删除整个 `.claude/worktrees/oral-calc-commit/` 目录
- 更新 `CLAUDE.md`：删除"配套扩展"中 v0.5+ 路线部分关于 IDE 定时任务、知识飞轮、跨设备集群的描述
- 更新 `README.md`：精简资产清单和路线图
- 更新 `.ccc/profile.md`：精简"关键资产清单"表格
- 更新 `.ccc/state.md`：在"关键历史决策"追加第 5 条"v0.7 精简决策"
- 更新 `docs/lessons.md`：追加 lesson 29（"v0.7 精简：路线图当现实做 = 过度工程化"）
- 更新 `scripts/ccc` wrapper：删掉对已删脚本的 case 分支
- 更新 `tests/scripts/test_ccc_tooling_hooks_smoke.py`：如有引用已删脚本则修复
- 更新 `tests/scripts/test_integration_business_flows.py`：同上
- 更新 `references/red-lines.md`：添加红线 13"禁止未使用路线代码"（v0.7 配套）

### 验收
- `ls .claude/worktrees/ 2>/dev/null` 返回 No such file or directory
- **精简命中对照（按 sections 分项实绩,不分全局数字）**：scripts/ 30+ → 8 核心,tests/ 21 → 8 核心,adapters/ 7 → 1（详见 v0.7-slim report）
  > ⚠️ 原全局数字(单一总数区间)为 Planner 拍脑袋,未含 .ccc/ 31 历史 + docs/lessons.md 1571 行 + 归档目录,已废除。
- `pytest tests/scripts/ -q` 全绿
- 跑一遍 `bash scripts/ccc-precheck.sh` 不报错
- `bash scripts/ccc-status.sh` 输出的脚本列表与保留清单一致
- `CLAUDE.md` / `README.md` / `profile.md` 三处的"关键资产清单"表格行数一致

---

## Commit 计划

| Phase | 改动 | Commit message 草稿 |
|-------|------|---------------------|
| 1 | 删 cluster 总线整套 | `chore(slim): remove cluster-bus route (cluster/znode/zcode/doctor) (phase 1/4)` |
| 2 | 删多 IDE 适配器整套 | `chore(slim): trim adapters to opencode-only (phase 2/4)` |
| 3 | 删派单/飞轮/成本/precommit | `chore(slim): remove dispatch/flywheel/cost/precommit (phase 3/4)` |
| 4 | 清理 worktree + 文档同步 | `chore(slim): cleanup worktree and sync docs (phase 4/4)` |

规则：每个 phase 一个独立 commit，message 含 phase 编号。

---

## 全局验收清单

- [ ] 4 phase 全部完成
- [ ] `pytest tests/scripts/ -q` 全绿
- [ ] 精简命中对照达成：scripts/ 30+ → 8、tests/ 21 → 8、adapters/ 7 → 1（原单一全局数字区间已废除，详见 Lesson 30）
- [ ] `CLAUDE.md` / `README.md` / `profile.md` 三处资产清单一致
- [ ] `scripts/ccc-precheck.sh` + `scripts/ccc-finish.sh` + `scripts/executor-watchdog.sh` 三个核心门控脚本可用
- [ ] `SKILL.md` 入口文件未动
- [ ] diff 范围仅限"只改文件"列表
- [ ] 每个 phase 对应一个 commit
- [ ] phases.json 与 plan phase 数一致

---

## 后续步骤（可选 — Planner 兜底）

- 精简后，CCC 真正变成"一个 50 行 SKILL.md + 5 个核心 bash 脚本"的小型框架
- 后续如需做跨设备调度或定时任务，按需从头写更简单的版本
- 飞轮 + 异常报告的教训沉淀机制用 `docs/lessons.md` 手动维护即可，不做自动扫描
