# Plan: [V0.7e-fix — 修 SKILL.md 配套扩展段（Critical #1）]

> 撰写：ccc-planner | 执行：Claude（manual）

---

## 范围

- **目标**：修 V0.7-slim Verifier 找出的 Critical #1：SKILL.md L218-222 "配套扩展（v0.5+ 路线）" 段仍列已删功能
- **只改文件**：
  - `SKILL.md` L218-222
- **不改文件**：其他任何文件
- **执行方式**：`manual`
- **Phase 数**：1

---

## 改动 1：删 SKILL.md 配套扩展段过时描述

### 做什么
原 L218-222 列了 cron / quality_flywheel.py / cluster bus 三个**已删功能**。需要改成"路线挂起，需要时按需重写"措辞，与 v0.7-slim 精简精神一致。

### 怎么做
原段：
```
## 配套扩展（v0.5+ 路线）

- **IDE 定时任务**：cron / launchd 自动唤起 CCC 跑下一阶段
- **知识飞轮**：`quality_flywheel.py` 对接报告，沉淀 lessons 自动丰富红线
- **跨设备**：CCC cluster bus / ssh 调用 qb / feiniu / M1 集群（v1.0 路线）
- **跨工具**：SKILL 不绑 IDE，可在 Trae / Cursor / Zed 间移植
- **模型路由**：通过 `ANTHROPIC_BASE_URL=http://127.0.0.1:4000` 选模型
```

改为：
```
## 配套扩展（按需）

> v0.7-slim 精简：原路线图（IDE 定时任务 / 知识飞轮 / 跨设备集群）代码已删。
> 如需这些功能，按需从头重写更简单的版本，不预留。

- **跨工具**：SKILL 不绑 IDE，可在 Trae / Cursor / Zed 间移植
- **模型路由**：通过 `ANTHROPIC_BASE_URL=http://127.0.0.1:4000` 选模型
```

### 验收
- SKILL.md 不再包含 "cron" / "quality_flywheel" / "cluster bus" 字样（grep 验证）
- L218-222 段已修改
- pytest 还绿
- 1 commit

---

## Commit 计划

| Phase | 改动 | Commit message |
|-------|------|---------------|
| 1 | 修 SKILL.md | `docs(slim): prune SKILL.md stale route mentions (post-v0.7slim)` |

---

## 全局验收清单

- [ ] SKILL.md L218-222 已改
- [ ] grep 验证 cron/quality_flywheel/cluster bus 不再出现
- [ ] pytest 还绿
- [ ] 1 commit