# Plan: [V0.7d-prime — 红线 14+15 工程化（monitor + 5min 轮询）]

> 撰写：ccc-planner | 执行：Claude（manual）

---

## 范围

- **目标**：把"自动开 monitor + 5 分钟轮询 + 完成自动终止"沉淀为 CCC 工具链，未来所有 Executor 任务自动启用
- **只改文件**：
  - `scripts/ccc-monitor.sh`（新增，开 monitor 窗口）
  - `scripts/ccc-poll.sh`（新增，5 分钟轮询 + 完成检测 + 自动终止）
  - `references/red-lines.md`（追加红线 14+15）
  - `scripts/ccc-exec-launcher.sh`（新增，整合 monitor + poll + Executor 三件套）
  - `docs/engineer-flow.md`（新增，文档化流程）
  - `CHANGELOG.md`
- **不改文件**：`SKILL.md`（不引入新触发词）/ `.ccc/profile.md`
- **执行方式**：`manual`
- **Phase 数**：2

---

## 改动 1：ccc-monitor.sh + ccc-poll.sh 工具脚本

### 做什么
两个独立工具脚本，未来手动或自动调起。

### 怎么做
**scripts/ccc-monitor.sh**：
```bash
#!/bin/bash
# 自动开 tmux monitor 窗口（如果已开就跳过）
SESSION="${1:-claude-code}"
if tmux list-windows -t "$SESSION" 2>/dev/null | grep -q "monitor"; then
  echo "[monitor] 已存在"
  exit 0
fi
tmux new-window -t "$SESSION" -n monitor "bash -c 'while true; do clear; echo \"=== \$(date) monitor ===\"; tmux list-windows -t $SESSION; echo; for w in \$(tmux list-windows -t $SESSION -F \"#I\" 2>/dev/null); do echo \"--- window \$w ---\"; tmux capture-pane -t $SESSION:\$w -p 2>/dev/null | tail -5; done; sleep 10; done'"
echo "[monitor] 已开"
```

**scripts/ccc-poll.sh**：
```bash
#!/bin/bash
# 5 分钟轮询指定窗口 + 完成检测 + 自动终止
WINDOW="${1:-1}"
SESSION="${2:-claude-code}"
INTERVAL="${3:-300}"
LOG="/tmp/$(basename "$0" 2>/dev/null)-$(date +%s).log"

START=$(date +%s)
echo "[poll] start window=$SESSION:$WINDOW interval=${INTERVAL}s"

while true; do
  sleep "$INTERVAL"
  PANE=$(tmux capture-pane -t "$SESSION:$WINDOW" -p 2>/dev/null | tail -5)
  NOW=$(date +%s)
  ELAPSED=$((NOW - START))
  
  # 完成信号：单独 prompt "❯" + 无 "esc to interrupt"
  if echo "$PANE" | grep -q "❯" && ! echo "$PANE" | grep -q "esc to interrupt"; then
    echo "[poll] 完成检测 elapsed=${ELAPSED}s"
    echo "$PANE" > "/tmp/poll-final-$(date +%s).txt"
    break
  fi
  echo "[poll] elapsed=${ELAPSED}s"
done
echo "[poll] 退出"
```

**scripts/ccc-exec-launcher.sh**：
```bash
#!/bin/bash
# 三件套：开 monitor + 启动 Executor + 启动 poll
SESSION="claude-code"
WINDOW="${1:?usage: ccc-exec-launcher.sh <window> <prompt-file>}"
PROMPT_FILE="${2:?usage: ccc-exec-launcher.sh <window> <prompt-file>}"

# 1. 开 monitor（如果没开）
bash "$(dirname "$0")/ccc-monitor.sh" "$SESSION"

# 2. 启动 Executor
tmux send-keys -t "$SESSION:$WINDOW" "cd /Users/apple/program/CCC && cat $PROMPT_FILE | claude --bare --model deepseek-v4-flash" Enter

# 3. 后台启动 poll（自动检测完成）
nohup bash "$(dirname "$0")/ccc-poll.sh" "$WINDOW" "$SESSION" 300 > "/tmp/poll-$WINDOW-$(date +%s).log" 2>&1 &
POLL_PID=$!
echo "[launcher] poll PID=$POLL_PID window=$WINDOW"
echo "$POLL_PID" > "/tmp/poll-$WINDOW.pid"
```

### 验收
- 3 个脚本存在且 `bash -n` 语法过
- 手工跑 `bash scripts/ccc-monitor.sh` 不报错
- 手工跑 `bash scripts/ccc-poll.sh 1 claude-code 5` （5s 测试），捕获 prompt 自动退出
- 1 commit

---

## 改动 2：red-lines.md 追加红线 14+15 + 文档化

### 做什么
把 monitor + poll 上升为红线，所有未来 Executor 任务强制启用。

### 怎么做
**references/red-lines.md** 追加：

```
| 14 | Executor 必须配 monitor + 5min 轮询 | 不开监控 = 盲跑，违规 |
| 15 | 轮询进程完成自动终止 | 下次 Executor 必须重启 poll |
```

**docs/engineer-flow.md** 新增（≥40 行）：
- 串行模式：同窗口顺序跑 N 个 phase
- 并行模式：4 窗口各跑独立项目
- 工具链：ccc-exec-launcher.sh 三件套使用说明
- 失败兜底：poll 检测到完成但 Executor 未退出 → kill -9

### 验收
- red-lines.md 含红线 14+15（grep 验证）
- docs/engineer-flow.md 存在且 ≥40 行
- 1 commit

---

## Commit 计划

| Phase | 改动 | Commit message 草稿 |
|-------|------|---------------------|
| 1 | ccc-monitor.sh + ccc-poll.sh + ccc-exec-launcher.sh | `feat(tooling): ccc-monitor + ccc-poll + ccc-exec-launcher (v0.7d-prime)` |
| 2 | red-lines 14+15 + engineer-flow.md | `docs(red-lines): add line 14+15 (monitor + poll mandatory) (v0.7d-prime)` |

---

## 全局验收清单

- [ ] 3 脚本存在 + 语法 OK
- [ ] 手工跑 monitor 不报错
- [ ] 手工跑 poll 5s 测试完成检测 OK
- [ ] red-lines.md 含 14+15
- [ ] engineer-flow.md ≥ 40 行
- [ ] 2 commits