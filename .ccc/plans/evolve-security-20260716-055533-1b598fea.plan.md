# Plan: evolve-security-20260716-055533-1b598fea — B324 MD5 usedforsecurity 修复

> 撰写：ccc-product | 执行：ccc-dev（manual）

---

## 当前代码状态

`src/xianyu/video/thumbnail.py:298` — `generate_thumbnail()` 内用 `hashlib.md5()` 生成临时文件名哈希，**未传** `usedforsecurity=False`。

- **入口/核心文件：**
  - `src/xianyu/video/thumbnail.py:15` — `import hashlib`
  - `src/xianyu/video/thumbnail.py:298` — `hashlib.md5(f"{video_path}:{time_sec}".encode()).hexdigest()[:8]`（缺少 `usedforsecurity=False`）

- **当前结构要点：**
  - `hashlib.md5()` 调用（line 298）**缺少** `usedforsecurity=False` 参数
  - MD5 哈希仅用于生成临时文件名（`{gettempdir()}/xianyu_thumb_{frame_hash}.png`），**不是**安全用途
  - `hashlib` 已在文件顶部 import（line 15）
  - Python 3.14+ 环境，`usedforsecurity` 参数支持正常（Python 3.9+）

- **待改动点：**
  - `src/xianyu/video/thumbnail.py:298` — 给 `hashlib.md5()` 添加 `usedforsecurity=False`

---

## 范围

- **目标**：修复 Bandit B324 告警——给 MD5 调用添加 `usedforsecurity=False`
- **只改文件：** `["src/xianyu/video/thumbnail.py"]`
- **不改文件：** 所有其他文件（测试、配置、`.ccc/` 等均不改）
- **执行方式：** `manual`
- **Phase 数：** 1

---

## 改动 1：添加 `usedforsecurity=False` 到 MD5 调用

### 做什么
Bandit B324 告警指出 `hashlib.md5()` 用于非安全场景时应显式传入 `usedforsecurity=False`。该处 MD5 仅用于临时文件名生成（防冲突、短路径），不是密码学安全用途。添加参数消除告警。

### 怎么做
`src/xianyu/video/thumbnail.py:298` 做单行改动，在 `hashlib.md5()` 调用中增加 `usedforsecurity=False` 参数。

### 验收清单

- [ ] `usedforsecurity=False` 已添加到 `hashlib.md5()` 调用中
- [ ] bandit 不再报 B324
- [ ] Python 3.14 编译通过
- [ ] 正确性：该处 MD5 不用于安全场景，仅作文件命名

### 验收
- [bandit 不再报 B324]（参考：`bandit -q src/xianyu/video/thumbnail.py 2>&1 | grep -c "B324" || echo "0 B324 findings (expected)"`）
- [编译通过]（参考：`python3 -m compileall -q src/xianyu/video/thumbnail.py`）

---

## Commit 计划

| Phase | 改动 | Commit message 草稿 |
|-------|------|---------------------|
| 1 | line 298 MD5 调用加 `usedforsecurity=False` | `fix(security): add usedforsecurity=False to MD5 hash in thumbnail.py (B324) (phase 1/1)` |

---

## 全局验收清单

- [ ] 编译/类型检查零错误（`python3 -m compileall -q src/xianyu/video/thumbnail.py`）
- [ ] bandit 不再报 B324
- [ ] diff 范围仅限 `src/xianyu/video/thumbnail.py`
- [ ] 1 个 phase 对应 1 个 commit
- [ ] phases.json 与 plan phase 数一致（1）
- [ ] Plan 中所有验收意图全部达成

---

## 后续步骤

无。此修复为单一参数添加，无连锁依赖。
