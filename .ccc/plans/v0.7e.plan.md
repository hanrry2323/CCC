# Plan: [V0.7e — Verifier 独立验收 v0.7-slim]

> 撰写：ccc-planner | 执行：Verifier（独立 session）

---

## 范围

- **目标**：独立 Verifier session 验收 V0.7-slim 精简任务，写 verdict.md，≥3 adversarial probes
- **只改文件**：
  - `.ccc/verdicts/v0.7-slim.verdict.md`（新建/覆盖）
- **不改文件**：任何源码/文档/脚本
- **执行方式**：`manual`
- **Phase 数**：1

---

## Verifier 接受

### 验什么
V0.7-slim 主任务（4 phase 删文件精简）的完整性：
1. cluster-bus 整套是否彻底删除（scripts + tests + docs）
2. 多 IDE adapter 是否只留 opencode
3. dispatch/flywheel/cost/precommit 是否清干净
4. worktree 是否删除
5. 4 file contract 是否完整（plan/report/phases/verdict 都写了吗）

### 怎么验
至少 3 个 adversarial probes（从"攻击者"视角找漏删、残留引用、文档不一致）。

---

## Commit 计划

| Phase | 改动 | Commit message |
|-------|------|---------------|
| 1 | verdict.md | 无 commit（Verifier 不写代码） |

---

## 验收
- verdict.md 存在且 ≥3 probes
- VERDICT: PASS / CONDITIONAL_PASS / FAIL 三选一