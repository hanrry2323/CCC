# Plan: evolve-security-20260716-055533-68c6ef4e — 验收 weak SHA1 修复 (B324)

> 撰写：ccc-product | 执行：ccc-dev（manual）

---

## 当前代码状态

`scripts/_evolve.py:199-207` — `_post_finding()` 中 `hashlib.sha1()` 用于生成 task ID 短指纹，已传 `usedforsecurity=False`。

- **入口/核心文件：**
  - `scripts/_evolve.py:13` — `import hashlib`
  - `scripts/_evolve.py:205` — `hashlib.sha1(fp.encode("utf-8"), usedforsecurity=False).hexdigest()[:8]`

- **当前结构要点：**
  - `hashlib.sha1()` 调用（line 205）已带 `usedforsecurity=False` 参数
  - 该 SHA1 仅生成 task ID 的 8 字符短指纹，非安全用途
  - Python 3.14 环境，`usedforsecurity` 参数支持正常（Python 3.9+）
  - 此前 2 次修复 commit（`1d2bbc5` / `1621e5b`）+ 1 次验证 commit（`17f8e34`）已完成

- **待改动点：** 无。本次 plan 仅做最终验收确认。

---

## 范围

- **目标**：确认 Bandit B324 告警已修复——SHA1 仅作 task ID 短指纹，非安全用途
- **只改文件：** 无（仅验证）
- **不改文件：** 所有测试文件、`.ccc/`、`templates/`、其他 `scripts/` 文件
- **执行方式：** `manual`
- **Phase 数：** 1

---

## 改动 1：验证 `usedforsecurity=False` 已在正确位置

### 做什么
Bandit 曾报 B324 告警的 `hashlib.sha1()` 调用（line 205）已传 `usedforsecurity=False`。通过三个维度确认修复完整性：静态扫描不报警、代码编译通过、语义正确。

### 怎么做
不修改任何文件。执行以下验证：
1. 确认 `scripts/_evolve.py:205` 已有 `usedforsecurity=False`
2. 运行 bandit 确认 B324 不再出现
3. `python3 -m compileall -q` 确认语法正确
4. 确认文件中无其他未处理的 `hashlib.sha1` 调用点

### 验收清单

- [ ] `usedforsecurity=False` 已到位，bandit 不再报 B324
- [ ] 语义正确：SHA1 仅用于 task ID 生成，非安全用途
- [ ] Python 3.14 编译通过
- [ ] 无其他 SHA1 调用点受影响（仅此一处 `hashlib.sha1`）

### 验收
- [bandit 不再报 B324]（参考：`bandit -q scripts/_evolve.py 2>&1 | grep -c "B324" || echo "0 B324 findings (expected)"`）
- [编译通过]（参考：`python3 -m compileall -q scripts/_evolve.py`）
- [import 正常]（参考：`python3 -c "import sys; sys.path.insert(0,'scripts'); from _evolve import _post_finding; print('import OK')"`）

---

## Commit 计划

| Phase | 改动 | Commit message 草稿 |
|-------|------|---------------------|
| 1 | 无改动（仅验证确认） | `verify(security): confirm usedforsecurity=False on SHA1 in _post_finding (B324) [phase=1]` |

---

## 全局验收清单

- [ ] 编译零错误（`python3 -m compileall -q scripts/_evolve.py`）
- [ ] bandit 不再报 B324（`bandit -q scripts/_evolve.py`）
- [ ] 无任何文件改动（仅验证）
- [ ] 1 个 phase 对应 1 个 commit（验证 commit）
- [ ] phases.json 与 plan phase 数一致（1）
- [ ] Plan 中所有验收意图全部达成

---

## 后续步骤

**无需任何后续操作。** 修复已于 commit `1d2bbc5` + `1621e5b` 完成，验证于 `17f8e34` 完成。本 plan 作为最终验收兜底。