# Plan: evolve-security-20260716-055533-68c6ef4e — 验收 SHA1 usedforsecurity 修复 (B324)

> 撰写：ccc-product | 执行：ccc-dev（manual）

---

## 当前代码状态

`scripts/_evolve.py:205` — `_post_finding()` 中 `hashlib.sha1()` 已传 `usedforsecurity=False`，该调用仅用于生成 task ID 短指纹，非安全用途。

- **入口/核心文件：**
  - `scripts/_evolve.py:13` — `import hashlib`
  - `scripts/_evolve.py:205` — `hashlib.sha1(fp.encode("utf-8"), usedforsecurity=False).hexdigest()[:8]`

- **当前结构要点：**
  - `usedforsecurity=False` 参数已于 line 205 到位（commit `1621e5b`）
  - SHA1 仅用于 task ID 短指纹生成（8 字符 hex），非密码学安全用途
  - Python 3.14 环境，`usedforsecurity` 参数（Python 3.9+）支持正常
  - 此前已有 verify commit `3e91937` 确认该修复
  - 当前 evolve 扫描的基线文件早于 fix，行号偏移（报告 line 201，实际 line 205）

- **待改动点：** 无。仅做验证确认。

---

## 范围

- **目标**：确认 Bandit B324 告警已修复——`usedforsecurity=False` 已在正确位置，无遗漏
- **只改文件：** 无（仅验证）
- **不改文件：** 所有其他文件
- **执行方式：** `manual`
- **Phase 数：** 1

---

## 改动 1：验证 SHA1 `usedforsecurity=False` 修复完整性

### 做什么
evolve 扫描报出 B324 时，SHA1 调用尚缺 `usedforsecurity=False`。该修复已于 commit `1621e5b` 落地。通过三个维度确认修复完整：静态扫描不报警、编译通过、语义正确。

### 怎么做
不修改任何文件。执行验证：
1. 确认 `scripts/_evolve.py:205` 已有 `usedforsecurity=False`
2. bandit 扫描确认 B324 不再出现
3. `compileall` 确认语法正确
4. 确认文件中无其他未处理的 `hashlib.sha1` 调用

### 验收清单

- [ ] `usedforsecurity=False` 已到位，bandit 不再报 B324
- [ ] 语义正确：SHA1 仅用于 task ID 指纹，非安全用途
- [ ] Python 3.14 编译通过
- [ ] 文件内无其他未处理的 `hashlib.sha1` 调用

### 验收
- [bandit 不再报 B324]（参考：`bandit -q scripts/_evolve.py 2>&1 | grep -c "B324" || echo "0 B324 (expected)"`）
- [编译通过]（参考：`python3 -m compileall -q scripts/_evolve.py`）
- [import 正常]（参考：`python3 -c "import sys; sys.path.insert(0,'scripts'); from _evolve import _post_finding; print('import OK')"`）

---

## Commit 计划

| Phase | 改动 | Commit message 草稿 |
|-------|------|---------------------|
| 1 | 无改动（仅验证确认） | `verify(security): confirm usedforsecurity=False on SHA1 in _post_finding (B324) [phase=1]` |

---

## 全局验收清单

- [ ] 编译零错误（`python3 -m compileall -q scripts/_evolve.py`）
- [ ] bandit 不再报 B324（`bandit -q scripts/_evolve.py`）
- [ ] 无任何文件改动（仅验证）
- [ ] 1 个 phase 对应 1 个 commit
- [ ] phases.json 与 plan phase 数一致（1）
- [ ] Plan 中所有验收意图全部达成

---

## 后续步骤

**无需任何后续操作。** 修复已于 commit `1621e5b` 完成，verification 已于 `3e91937` 完成。本 plan 作为最终验收兜底。