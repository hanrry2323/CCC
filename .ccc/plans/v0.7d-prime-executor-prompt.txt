## Goal

End state:
  CCC 工具链沉淀 3 个脚本（ccc-monitor.sh / ccc-poll.sh / ccc-exec-launcher.sh）+ 2 条新红线（14+15）+ docs/engineer-flow.md 文档。未来所有 Executor 任务通过 ccc-exec-launcher.sh 一键起 monitor + Executor + poll 三件套。

Evidence:
  ```bash
  cd /Users/apple/program/CCC
  
  # 1. 3 脚本存在
  for s in ccc-monitor.sh ccc-poll.sh ccc-exec-launcher.sh; do
    [ -f scripts/$s ] && bash -n scripts/$s && echo "OK $s"
  done
  
  # 2. monitor 脚本手测（已有 monitor 窗口时不应重复开）
  bash scripts/ccc-monitor.sh   # 应输出"已存在"
  
  # 3. poll 脚本手测 5s 间隔（必须能在 prompt 符出现时退出）
  timeout 60 bash scripts/ccc-poll.sh 1 claude-code 5 2>&1 | tail -5
  
  # 4. red-lines 14+15 追加
  grep -c "Executor 必须配 monitor" references/red-lines.md   # 1
  grep -c "轮询进程完成自动终止" references/red-lines.md   # 1
  
  # 5. docs/engineer-flow.md ≥ 40 行
  [ -f docs/engineer-flow.md ] && wc -l docs/engineer-flow.md   # ≥40
  
  # 6. 2 commits
  git log --oneline | grep "v0.7d-prime" | wc -l   # 2
  ```

Constraints:
  - 禁止修改 SKILL.md（不引入新触发词）
  - 禁止动 V0.7-slim / V0.7a 已修文档
  - 单 phase 单 commit
  - scripts 必须 `chmod +x`

Budget:
  最多 3 次重试。

## Phase 1: Plan
读 `/Users/apple/program/CCC/.ccc/plans/v0.7d-prime.plan.md` 全文。

## Phase 2: Implement

**Phase 1**: 创建 3 个工具脚本
- `scripts/ccc-monitor.sh`：开 tmux monitor 窗口（已存在则跳过）
- `scripts/ccc-poll.sh`：5 分钟轮询 + 完成检测 + 自动终止
- `scripts/ccc-exec-launcher.sh`：三件套整合（monitor + Executor + poll）

每个脚本都要：
1. 加 shebang `#!/bin/bash`
2. `chmod +x`
3. 顶部注释说明用法

**Phase 2**: 文档化
- `references/red-lines.md` 追加红线 14+15
- `docs/engineer-flow.md` 新建（≥40 行）覆盖：
  - 串行 vs 并行投递模式
  - ccc-exec-launcher.sh 用法
  - 失败兜底（poll 异常退出）

写 `.ccc/reports/v0.7d-prime.report.md`（含 > VERDICT: 段占位引用）+ 更新 phases.json 2 行 → done。

## Phase 3: Test & Document
- 跑 Evidence 6 条命令
- 关键测试：Phase 1 commit 后手工跑 monitor 脚本（不实际新建窗口，验证"已存在"分支）

## Phase 4: Commit
- Phase 1: `bash scripts/ccc-exec-commit.sh . v0.7d-prime` → `feat(tooling): ccc-monitor + ccc-poll + ccc-exec-launcher (v0.7d-prime)`
- Phase 2: `bash scripts/ccc-exec-commit.sh . v0.7d-prime` → `docs(red-lines): add line 14+15 (monitor + poll mandatory) (v0.7d-prime)`

## Phase 5: Self-Verify
- 跑 Evidence 6 条命令，逐条报告 PASS/FAIL

> **不要做的事**：
> - 不要碰 SKILL.md（不引入新触发词，trigger 不变）
> - 不要改 V0.7-slim / V0.7a 已修文件
> - 不要实际运行 monitor 脚本（避免重复开窗口）