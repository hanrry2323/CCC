## Goal

End state:
  tmux session `claude-code` 有 5 个窗口按顺序（ccc / qb / hp / xianyu / monitor），每个 cwd 正确。

Evidence:
  ```bash
  tmux list-windows -t claude-code -F "#I: #W @ #{pane_current_path}" 2>/dev/null
  
  for i in 0 1 2 3; do
    p=$(tmux send-keys -t claude-code:$i 'pwd' Enter \; capture-pane -t claude-code:$i -p 2>/dev/null | tail -1)
    echo "window $i: $p"
  done
  ```

Constraints:
  - 禁止改任何源码/文档
  - 禁止残留旧窗口（关前确认无 Claude 在跑）
  - kill-session 前先确认（`tmux list-windows` 快照备份）

Budget:
  2 次重试。

## Phase 1: Plan
读 `/Users/apple/program/CCC/.ccc/plans/v0.7d.plan.md` 全文。

## Phase 2: Implement

1. 快照当前状态：`tmux list-windows -t claude-code > /tmp/pre-kill-snapshot.txt`
2. 检查所有窗口是否回到 prompt（没有 "esc to interrupt"）
3. `tmux kill-session -t claude-code`
4. 按 plan 重建 5 窗口
5. 验证顺序 + cwd

## Phase 3: Test & Document
- 跑 Evidence 验证
- 写 `.ccc/reports/v0.7d.report.md`（含 > VERDICT: 段占位）
- 更新 phases.json → done

## Phase 4: Commit
- 无 commit（纯 tmux 操作，不改文件）

## Phase 5: Self-Verify
- 跑 Evidence 命令，报告 PASS/FAIL