# v0.18-detail

> 标题: 添加 /api/tasks/{id} 详情端点
> 创建: 2026-07-07T11:10:55Z

## 目标

实现 GET /api/tasks/{id} 返回单个任务的完整信息：
- 所有字段
- 移动历史（from→to + 时间戳）
- 关联的角色执行日志
需在 task JSONL 里维护 moves[] 数组

## Phase

(由 dev 拆)

## Commit 计划

- dev 完成后自动 commit + push
