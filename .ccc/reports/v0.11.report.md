# v0.11 报告：开箱即用调度 + 队列真测试

## 目标
v0.11 = v0.10 评估的 2 个缺口：
- a: 缺 install-ccc-scheduler + 默认钩子模板
- b: 缺 ccc-queue 多 phase 真测

## Phase 执行

### Phase a1: 默认钩子模板（opencode 44.66s 写）
- templates/hooks/post-exec.sh — 自动 git add+commit
- templates/hooks/on-error.sh — L2 通知 + 落 abnormal report
- templates/hooks/pre-commit.sh — soft lint (TODO/print/debugger)
- 修 1 处：on-error.sh 调 ccc-notify 用 positional（不是 --level flag）

### Phase a2: install-ccc-scheduler（opencode 84.99s 写）
- scripts/install-ccc-scheduler.sh — install/uninstall/status 3 模式
- 修 2 处：默认 target launcher（不是 flywheel）、Label com.ccc.*（不是 localhost）
- 加 --dry-run 选项

### Phase b-fix: 修红线 X2 失守
- v0.11b1 实测：launcher kill opencode 不级联到孙子 node 进程
- 修 1：opencode-exec.py 用 start_new_session + os.killpg
- 修 2：watchdog 加 pgrep "opencode (run|exec)" + pkill -f 兜底（macOS killpg 不可靠）

### Phase b1: 队列 3 phase 真测试
- test_queue_e2e_3phase_pass.py: 3 全成功 → exit 0
- test_queue_e2e_mid_fail.py: 中间失败 → exit 5
- test_queue_e2e_resume.py: pause 后续跑 → exit 0
- 改 ccc-queue.sh：加 CCC_LAUNCHER_OVERRIDE（mock launcher 用）

## 红线验证

| 红线 | 验证 |
|------|------|
| 红线 4（单 phase 单 commit）| 钩子模板自动 commit |
| 红线 5（phases.json 必写全）| 队列状态回写 |
| 红线 X2（每 phase 必杀）| killpg + pkill 兜底 + watchdog 扫 opencode run |
| 红线 X3（启动前 watchdog）| launcher Step 1 + precheck Gate 5 |

## pytest
66 passed in 16.81s (63 + 3 新增)

## Commit
- 21d8723 feat(v0.11-a2): install-ccc-scheduler
- d7c8425 fix(v0.11): opencode-exec 长 prompt --file
- 99475fe fix(v0.11-bfix): 红线 X2 killpg
- (待定) test(v0.11-b1): 队列 3 phase 真测
- (待定) chore(v0.11): 收尾

## Lesson 新增
- Lesson 33: opencode run positionals 截断 200 字符（已修：长 prompt 走 --file）

> VERDICT: .ccc/verdicts/v0.11-verdict.md
