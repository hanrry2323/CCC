# 任务书 A2 · 前端加固与回归固化（窗口 A 第二波）

> 本文件是给 Claude Code 的整段指令，复制全部内容到窗口 A 即可。  
> 前提：第一波（`task-A-web-frontend.md`）已实现并通过 Codex 审查后再接本任务书。

## 0. 先读

1. `CLAUDE.md`
2. 第一波完成报告（老板会贴给你）
3. `docs/dispatch/2026-08-01-squad-dispatch-plan.md`（硬规则必须遵守）

## 1. 任务目标

1. **回归固化**：把第一波修复的行为全部固化成自动化前端测试，以后一键可回归
2. **看板体验**：筛选/排序/拖拽稳定性、epic 聚合视图可用性
3. **聊天 UI 完善**：错误态、重试、流式状态展示
4. **前后端契约一致性**：重构后字段（含 schema_version 兼容）与前端消费对齐，输出差异清单

## 2. 允许范围

- 网页前端源码：看板、运维页、聊天、样式、公共组件
- 前端测试与测试基建

## 3. 红线（禁止）

- 后端业务逻辑、`scripts/`、`desktop/`、`src-tauri/`
- 4000/4100 与 relay 相关
- **网页鉴权改造不在本波**（真风险，另行安排，避免与窗口 B 范围打架）
- 密钥、真实凭据、生产配置；删除文件（除非计划说明理由）
- 提交 main

## 4. 流程（spec-first 门）

第一轮：`/plan` 输出「测试固化方案 + 体验增强清单 + 契约核对方法」，**不写代码**。  
确认后实现，测试全绿再提交。

## 5. 验收标准

- 第一波修复均有自动化测试覆盖且全绿
- 看板筛选/排序/拖拽无回归，有测试
- 聊天错误态/重试可用
- 契约差异清单每条有结论：已对齐 / 需后端改（后者记录移交窗口 B，不自己动后端）
- 提交在 `codex/ws-1-web` 分支

## 6. 完成报告格式

发现 → 动作 → 证据 → 移交项（需后端配合的单独列）
