# 任务书 A · Web 前端修复（窗口 A）

> 本文件是给 Claude Code 的整段指令，复制全部内容到窗口 A 即可。

## 0. 先读

1. `CLAUDE.md`
2. docs 中与 web 看板 / 运维页 / 聊天相关的文档
3. `docs/dispatch/2026-08-01-squad-dispatch-plan.md`（硬规则必须遵守）

## 1. 任务目标

CCC 刚完成 stage5-8 重构，网页看板与运维页已恢复但存在重构遗留问题：

1. 全量前端自检：列出断链、失效交互、样式回归的清单（能证明的才写）
2. 修复看板 / 运维页 / 聊天相关明确 bug
3. 补前端行为测试（现有测试基建上补，不新建一套）

## 2. 允许范围（只准动这些）

- 网页前端源码：看板、运维页、聊天、样式、公共组件
- 前端测试与测试基建
- 为联调所必需的最小后端适配（必须在报告中说明动了哪一处、为什么）

## 3. 红线（禁止）

- `scripts/`、`desktop/`、`src-tauri/`、引擎与后端业务逻辑
- 4000/4100 端口与 relay 相关改动（窗口 D 的活）
- 密钥、真实凭据、生产配置
- 删除文件（除非在计划里说明理由）
- 提交到 main

## 4. 流程（spec-first 门）

第一轮：只做 `/plan`，输出「自检清单 + 修复计划 + 验收方法」，**不写代码**。  
等老板把确认回传后再实现。

## 5. 验收标准

- 自检清单每条有结论（修了 / 不是问题 / 暂缓）
- `#/board` 看板渲染、拖拽、详情、汇总可用（参考 29 卡基准）
- `#/ops` 运维页各域渲染正常，relay 域优雅降级不白屏
- 修复项有对应测试，前端测试全绿
- 提交在 `codex/ws-1-web` 分支

## 6. 完成报告格式

- 自检发现（行为层面，不贴代码）
- 修了什么、怎么验证（附命令输出）
- 遗留风险
