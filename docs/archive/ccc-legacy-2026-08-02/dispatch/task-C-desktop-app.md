# 任务书 C · 桌面端 App 修复（窗口 C）

> 本文件是给 Claude Code 的整段指令，复制全部内容到窗口 C 即可。

## 0. 先读

1. `CLAUDE.md`
2. `desktop/README.md`、docs 中桌面端相关文档
3. `docs/dispatch/2026-08-01-squad-dispatch-plan.md`（硬规则必须遵守）

## 1. 任务目标

1. 桌面端自检：构建/启动、与后端 API 对接、窗口与状态展示的重构遗留问题
2. 修复明确 bug（连接、状态展示、流程按钮、打包相关）
3. 补类型检查/测试（桌面端现有的基建）

## 2. 允许范围

- `desktop/`（Swift）与 `src-tauri/`（Tauri/Rust）全部相关文件
- 桌面端测试与构建配置

## 3. 红线（禁止）

- `app/`、`scripts/`、web 前端、docs 主体（只允许桌面端相关说明）
- 4000/4100 与 relay 相关
- 密钥/凭据/签名证书类文件
- 提交 main

## 4. 流程（spec-first 门）

第一轮：`/plan` 输出「构建基线 + 问题清单 + 修复计划」，**不写代码**。  
确认后实现，构建与测试全过再提交。

## 5. 验收标准

- 本地可构建可启动（贴构建命令与结果）
- 核心流程与后端联通（连接状态、任务展示、关键按钮）
- 类型检查/测试全绿（现有基建）
- 报告列出遗留问题（无证据不写）
