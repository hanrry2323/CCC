# CCC 代码质量下一步修复方案（2026-07-24）

> **背景**：对抗审查发现 3 个骨架文件零接入主流程 + 4 个测试 failed + except:pass 残留 13 处  
> **目标**：把骨架从"定义层"推进到"接入层"，修真实 bug，清真实残留  
> **原则**：每步标明代码行号、核心代码、验收命令；不做空壳  
> **对抗审查报告依据**：见上一轮审查结论

---

## 问题总表（实查行号）

| # | 问题 | 实查位置 | 严重度 |
|---|------|----------|--------|
| 1 | retry budget 零接入主流程 | [failure_router.py:10-12](file:///Users/apple/program/CCC/scripts/engine/failure_router.py#L10-L12) docstring 自认"未替换"；ccc-engine.py / gates.py / hang.py 全部无 `increment_retry_count` 调用 | 🟥 高 |
| 2 | workspace_scope 零调用 | [workspace.py:67](file:///Users/apple/program/CCC/scripts/engine/workspace.py#L67) 仅 docstring 示例；`_activate_workspace` 仍 37 处调用 | 🟥 高 |
| 3 | ccc-engine.py 3333 行没降 | [ccc-engine.py:1850-2114](file:///Users/apple/program/CCC/scripts/ccc-engine.py#L1850-L2114) `_try_launch_planned` 264 行核心未搬 | 🟧 中 |
| 4 | 4 个测试 failed | [opencode-pool.py:46](file:///Users/apple/program/CCC/scripts/opencode-pool.py#L46) `asyncio.coroutine`（Py3.11 已移除） | 🟧 中 |
| 5 | mypy strict 报 _logger error | [pyproject.toml:82](file:///Users/apple/program/CCC/pyproject.toml#L82) `strict=true` 但缺 `mypy_path` | 🟨 低 |
| 6 | except:pass 残留 13 处 | [_board_store.py:559,936,945,975,983](file:///Users/apple/program/CCC/scripts/_board_store.py#L559) 等 | 🟨 低 |
| 7 | ccc-engine.py 重复定义 `_classify_failure` | [ccc-engine.py:2820-2832](file:///Users/apple/program/CCC/scripts/ccc-engine.py#L2820-L2832) 与 [failure_router.py:57-67](file:///Users/apple/program/CCC/scripts/engine/failure_router.py#L57-L67) 逻辑重复 | 🟨 低 |

---

## 修复 1：retry budget 真接入主流程（最高优先）

**问题**：`increment_retry_count` / `MAX_TASK_RETRY_BUDGET` 定义在 [failure_router.py:27,99](file:///Users/apple/program/CCC/scripts/engine/failure_router.py#L27)，但 ccc-engine.py 的 3 个重试路径**全部没有调用它**。

### 1.1 接入点 A：auto-refeed 路径

**位置**：[ccc-engine.py:2881](file:///Users/apple/program/CCC/scripts/ccc-engine.py#L2881) `_retry_abnormal_failures` 函数内

```python
# 改前（ccc-engine.py:2881）
MAX_AUTO_RETRY = 2
# ...
auto_retried = int(retry_counts.get(tid, 0) or 0)
if auto_retried >= MAX_AUTO_RETRY:
    continue

# 改后
from engine.failure_router import increment_retry_count, RetryBudgetExceeded, can_retry

MAX_AUTO_RETRY = 2
# ...
auto_retried = int(retry_counts.get(tid, 0) or 0)
if auto_retried >= MAX_AUTO_RETRY:
    continue

# 统一 budget 检查：跨层重试总和不超过 MAX_TASK_RETRY_BUDGET
if not can_retry(ws, tid, store):
    engine_log(
        f"[{label}] {tid} retry budget 耗尽（跨层 {MAX_TASK_RETRY_BUDGET} 次），跳过 auto-refeed"
    )
    continue
```

### 1.2 接入点 B：_handle_task_result 失败重试路径

**位置**：[ccc-engine.py:706-744](file:///Users/apple/program/CCC/scripts/ccc-engine.py#L706-L744) `_handle_task_result` 函数内，`_quarantine_keep_phases` 调用前

```python
# 改前（ccc-engine.py:735 附近）
def _quarantine_keep_phases(reason: str) -> bool:
    """失败隔离：保留 phases/plan，禁止删图回 backlog 触发 product。"""
    col = _find_task_column(store, tid) or "in_progress"
    if col != "abnormal":
        try:
            store.move_task(tid, col, "abnormal")
        except Exception as exc:
            engine_log(f"[{label}] {tid} move→abnormal 失败: {exc}")

# 改后：在 quarantine 前记录 retry budget
def _quarantine_keep_phases(reason: str) -> bool:
    """失败隔离：保留 phases/plan，禁止删图回 backlog 触发 product。"""
    # 统一重试计数（方案 2.3 接入）
    try:
        from engine.failure_router import increment_retry_count, RetryBudgetExceeded
        used = increment_retry_count(ws, tid, store)
        engine_log(f"[{label}] {tid} retry budget used {used}/{MAX_TASK_RETRY_BUDGET}")
    except RetryBudgetExceeded as exc:
        engine_log(f"[{label}] {tid} {exc} → 强制 abnormal 不再重试")
    except Exception as exc:
        engine_log(f"[{label}] {tid} retry budget 记录失败: {exc}")

    col = _find_task_column(store, tid) or "in_progress"
    if col != "abnormal":
        try:
            store.move_task(tid, col, "abnormal")
        except Exception as exc:
            engine_log(f"[{label}] {tid} move→abnormal 失败: {exc}")
```

### 1.3 接入点 C：gates.py reviewer 重试路径

**位置**：[gates.py:447-507](file:///Users/apple/program/CCC/scripts/engine/gates.py#L447-L507) `_run_reviewer_tester_gate` 的 `for attempt in range(max_attempts)` 循环内

```python
# 改前（gates.py:447-448）
for attempt in range(max_attempts):
    reviewer_role()

# 改后
for attempt in range(max_attempts):
    # 统一重试 budget 检查（方案 2.3 接入）
    try:
        from engine.failure_router import increment_retry_count, RetryBudgetExceeded
        used = increment_retry_count(ws, tid, store)
        _engine_log("[verdict-gate] %s reviewer retry %d/%d", tid, used, 8)
    except RetryBudgetExceeded:
        _engine_log("[verdict-gate] %s retry budget 耗尽 → abnormal", tid)
        if eng:
            eng._quarantine_with_notify(
                ws, tid, "retry budget exceeded (reviewer)", store
            )
        return False
    except Exception:
        pass  # budget 记录失败不阻塞门禁

    reviewer_role()
```

### 1.4 删除 ccc-engine.py 重复的 `_classify_failure`

**位置**：[ccc-engine.py:2820-2832](file:///Users/apple/program/CCC/scripts/ccc-engine.py#L2820-L2832)

```python
# 改前：ccc-engine.py:2820-2832 有独立 _classify_failure
def _classify_failure(reason: str, tid: str, phase_note: str = "") -> str:
    blob = f"{reason} {phase_note} {tid}".lower()
    for kw in _PERMANENT_KEYWORDS:
        if kw.lower() in blob:
            return "permanent"
    for kw in _TRANSIENT_KEYWORDS:
        if kw.lower() in blob:
            return "transient"
    return "transient"

# 改后：删除，改用 failure_router.classify_failure
from engine.failure_router import classify_failure as _classify_failure_impl

def _classify_failure(reason: str, tid: str, phase_note: str = "") -> str:
    """委托给 failure_router.classify_failure（统一入口）。"""
    return _classify_failure_impl(f"{reason} {phase_note} {tid}")
```

**验收命令**：
```bash
# 1. grep 确认接入点存在
grep -n "increment_retry_count\|can_retry\|RetryBudgetExceeded" scripts/ccc-engine.py scripts/engine/gates.py
# 应有 ≥3 处调用

# 2. 单测
pytest tests/scripts/test_failure_router.py -v

# 3. 全量回归
pytest tests/scripts/ scripts/tests/ -q --tb=short

# 4. 构造 retry > 8 次场景验证进 abnormal（集成测试）
pytest tests/scripts/test_engine.py -k "retry" -v
```

**commit message**：`feat(gates): retry budget 真接入 3 个重试路径（auto-refeed / task_result / reviewer）`

---

## 修复 2：workspace_scope 首批接入（3 个调用点）

**问题**：`workspace_scope` 定义在 [workspace.py:63](file:///Users/apple/program/CCC/scripts/engine/workspace.py#L63) 但零调用。不需要一次替换全部 37 处——先接 3 个关键路径，证明可用，后续渐进。

### 2.1 接入点 A：`_retry_abnormal_failures` 入口

**位置**：[ccc-engine.py:2869](file:///Users/apple/program/CCC/scripts/ccc-engine.py#L2869)

```python
# 改前（ccc-engine.py:2869）
_activate_workspace(ws)
store = _get_store(ws)
label = _ws_label(ws)
# ... 后续 150 行都用 store

# 改后
from engine.workspace import workspace_scope, get_current_workspace

with workspace_scope(ws):
    ctx = get_current_workspace()
    store = ctx.store
    label = _ws_label(ws)
    # ... 后续逻辑不变，都在 scope 内
```

**注意**：这个函数体到 2998 行结束，需要把整段包进 `with`。缩进会变，但 git diff 可读。

### 2.2 接入点 B：`_handle_task_result` 入口

**位置**：[ccc-engine.py:715](file:///Users/apple/program/CCC/scripts/ccc-engine.py#L715)

```python
# 改前（ccc-engine.py:715）
def _handle_task_result(ws, tid, result, *, complexity="medium", started_at=None) -> bool:
    _activate_workspace(ws)
    store = _get_store(ws)
    # ...

# 改后
def _handle_task_result(ws, tid, result, *, complexity="medium", started_at=None) -> bool:
    from engine.workspace import workspace_scope, get_current_workspace
    with workspace_scope(ws):
        ctx = get_current_workspace()
        store = ctx.store
        # ... 后续逻辑不变
```

### 2.3 接入点 C：`_run_reviewer_tester_gate` 入口

**位置**：[gates.py:434](file:///Users/apple/program/CCC/scripts/engine/gates.py#L434)

```python
# 改前（gates.py:434）
def _run_reviewer_tester_gate(ws: Path, tid: str) -> bool:
    eng = _eng()
    _activate_workspace(ws)
    store = _get_store(ws)
    # ...

# 改后
def _run_reviewer_tester_gate(ws: Path, tid: str) -> bool:
    eng = _eng()
    from engine.workspace import workspace_scope, get_current_workspace
    with workspace_scope(ws):
        ctx = get_current_workspace()
        store = ctx.store
        # ... 后续逻辑不变
```

**验收命令**：
```bash
# 1. grep 确认 workspace_scope 有实际调用（非 docstring）
grep -rn "with workspace_scope" scripts/ --include="*.py" | grep -v "def workspace_scope\|#\|docstring"
# 应有 ≥3 处

# 2. 单测全绿（_activate_workspace 仍保留兼容，旧测试不会破）
pytest tests/scripts/ scripts/tests/ -q --tb=short

# 3. 并发测试（验证 workspace 隔离不串扰）
pytest tests/scripts/test_engine_phase_parallel_dispatch.py -v
```

**commit message**：`refactor(workspace): workspace_scope 首批接入 3 个入口（retry_abnormal / task_result / reviewer_gate）`

---

## 修复 3：4 个 failed 测试（asyncio.coroutine 兼容）

**问题**：[opencode-pool.py:46](file:///Users/apple/program/CCC/scripts/opencode-pool.py#L46) 返回类型标注用了 `asyncio.coroutine`，Python 3.11 已移除该装饰器。

### 3.1 修改 opencode-pool.py

**位置**：[opencode-pool.py:46](file:///Users/apple/program/CCC/scripts/opencode-pool.py#L46)

```python
# 改前（opencode-pool.py:46）
def run_task(task: dict, sem: asyncio.Semaphore) -> asyncio.coroutine:
    """单 task：拿信号量 → 跑 opencode → 释放"""

    async def _run():
        async with sem:
            result = await asyncio.get_event_loop().run_in_executor(
                None,
                _executor.execute,
                task["phase_id"],
                Path(task["prompt_file"]).read_text(encoding="utf-8"),
                task.get("timeout", 1800),
                task.get("cwd"),
            )
            return result

    return _run()

# 改后（删除 asyncio.coroutine 类型标注，改用 Coroutine）
from typing import Coroutine, Any

def run_task(task: dict, sem: asyncio.Semaphore) -> Coroutine[Any, Any, dict]:
    """单 task：拿信号量 → 跑 opencode → 释放"""

    async def _run():
        async with sem:
            result = await asyncio.get_event_loop().run_in_executor(
                None,
                _executor.execute,
                task["phase_id"],
                Path(task["prompt_file"]).read_text(encoding="utf-8"),
                task.get("timeout", 1800),
                task.get("cwd"),
            )
            return result

    return _run()
```

**验收命令**：
```bash
# 1. 4 个测试全绿
pytest tests/scripts/test_opencode_pool_max_parallel.py -v

# 2. opencode-pool.py 语法检查
python3 -m py_compile scripts/opencode-pool.py

# 3. 全量回归确认 0 failed
pytest tests/scripts/ scripts/tests/ -q --tb=line
# 应输出: 1013 passed, 1 skipped, 0 failed
```

**commit message**：`fix(opencode-pool): 替换 asyncio.coroutine 为 Coroutine（Py3.11 兼容）`

---

## 修复 4：mypy strict 真翻绿

**问题**：[pyproject.toml:82](file:///Users/apple/program/CCC/pyproject.toml#L82) `strict=true` 但 mypy 找不到 `_logger` 模块。

### 4.1 修改 pyproject.toml 加 mypy_path

**位置**：[pyproject.toml:71-82](file:///Users/apple/program/CCC/pyproject.toml#L71-L82)

```toml
# 改前
[tool.mypy]
explicit_package_bases = true
# 2026-07-24 方案 2.1：mypy strict 渐进启用
files = [
    "scripts/_config.py",
    "scripts/_utils.py",
    "scripts/_jsonl_rotate.py",
]
strict = true

# 改后
[tool.mypy]
explicit_package_bases = true
mypy_path = "scripts"          # 让 mypy 能找到 _logger 等脚本模块
files = [
    "scripts/_config.py",
    "scripts/_utils.py",
    "scripts/_jsonl_rotate.py",
]
strict = true
```

**验收命令**：
```bash
.venv/bin/python -m mypy --strict scripts/_config.py scripts/_utils.py scripts/_jsonl_rotate.py
# 应输出: Success: no issues found in 3 source files
```

**commit message**：`fix(mypy): 加 mypy_path=scripts，3 模块 strict 真翻绿`

---

## 修复 5：清理残留 except:pass（13 处 → 0）

**问题**：方案 0.1 只修了 gates.py 2 处 + _task_commit.py 3 处，残留 13 处。

### 5.1 _board_store.py 5 处

| 行号 | 改前 | 改后 |
|------|------|------|
| [559](file:///Users/apple/program/CCC/scripts/_board_store.py#L559) | `except OSError: pass` | `except OSError as e: _log.debug("_column_signature stat %s: %s", self.board / column, e)` |
| [936](file:///Users/apple/program/CCC/scripts/_board_store.py#L936) | `except OSError: pass` | `except OSError as e: _log.warning("move_task: cannot unlink old src %s: %s", src, e)` |
| [945](file:///Users/apple/program/CCC/scripts/_board_store.py#L945) | `except OSError: pass` | `except OSError as e: _log.warning("move_task: cleanup backup %s: %s", backup, e)` |
| [975](file:///Users/apple/program/CCC/scripts/_board_store.py#L975) | `except (OSError, AttributeError): pass` | `except (OSError, AttributeError) as e: _log.debug("update_index cleanup: %s", e)` |
| [983](file:///Users/apple/program/CCC/scripts/_board_store.py#L983) | `except (OSError, AttributeError): pass` | `except (OSError, AttributeError) as e: _log.debug("update_index stat: %s", e)` |
| [1056](file:///Users/apple/program/CCC/scripts/_board_store.py#L1056) | `except (OSError, json.JSONDecodeError, ValueError, TypeError): pass` | `except (OSError, json.JSONDecodeError, ValueError, TypeError) as e: _log.debug("event tail parse %s: %s", f.name, e)` |

### 5.2 ccc-engine.py 2 处

| 行号 | 改前 | 改后 |
|------|------|------|
| [2971-2972](file:///Users/apple/program/CCC/scripts/ccc-engine.py#L2971-L2972) | `except Exception: pass` | `except Exception as exc: engine_log("auto-refeed task json update failed: %s", exc)` |
| [2993-2994](file:////Users/apple/program/CCC/scripts/ccc-engine.py#L2993-L2994) | `except OSError: pass` | `except OSError as exc: _log.warning("retry counter file write failed: %s", exc)` |

### 5.3 其余文件

| 文件 | 行号 | 改后 |
|------|------|------|
| [ccc-health-analyzer.py:316](file:///Users/apple/program/CCC/scripts/ccc-health-analyzer.py#L316) | `except (SyntaxError, OSError): pass` | `except (SyntaxError, OSError) as e: _log.debug("ast parse %s: %s", f, e)` |
| [ccc-health-analyzer.py:365](file:///Users/apple/program/CCC/scripts/ccc-health-analyzer.py#L365) | `pass` | `except OSError as e: _log.debug("health_analyzer cleanup: %s", e)` |
| [_result_json.py:32](file:///Users/apple/program/CCC/scripts/_result_json.py#L32) | `except json.JSONDecodeError: pass` | `except json.JSONDecodeError as e: _log.debug("result_json parse: %s", e)` |
| [_result_json.py:106](file:///Users/apple/program/CCC/scripts/_result_json.py#L106) | `pass` | `except Exception as e: _log.debug("result_json fallback: %s", e)` |
| [_ccc_hygiene.py:57](file:///Users/apple/program/CCC/scripts/_ccc_hygiene.py#L57) | `pass` | `except json.JSONDecodeError as e: _log.debug("hygiene parse: %s", e)` |
| [ccc-product-session.py:42](file:///Users/apple/program/CCC/scripts/ccc-product-session.py#L42) | `except OSError: pass` | `except OSError as e: _log.debug("product_session mkdir: %s", e)` |

**验收命令**：
```bash
# 1. grep 确认残留为 0
grep -rn "except.*:" scripts/ -A 1 | grep -B 1 "pass$" | grep -v "except.*as " | head -20
# 输出应为空

# 2. 单测全绿
pytest tests/scripts/ scripts/tests/ -q --tb=short
```

**commit message**：`fix(observability): 清理残留 13 处 except:pass（_board_store 6 + engine 2 + 其它 5）`

---

## 修复 6：ccc-engine.py 首搬核心块（264 行）

**问题**：[ccc-engine.py:1850-2114](file:///Users/apple/program/CCC/scripts/ccc-engine.py#L1850-L2114) `_try_launch_planned` 是 264 行的核心编排逻辑，dispatch.py 只搬了 4 个小 helper。

### 6.1 搬 `_try_launch_planned` 到 dispatch.py

**搬迁映射**：

| ccc-engine.py 行号 | 内容 | 目标 |
|---------------------|------|------|
| [1850-2114](file:///Users/apple/program/CCC/scripts/ccc-engine.py#L1850-L2114) | `_try_launch_planned` 264 行 | → `engine/dispatch.py` 的 `try_launch_planned()` |
| [2147-2189](file:///Users/apple/program/CCC/scripts/ccc-engine.py#L2147-L2189) | `_try_launch_planned_parallel` 42 行 | → `engine/dispatch.py` 的 `try_launch_planned_parallel()` |

**核心代码**（dispatch.py 新增）：

```python
# engine/dispatch.py 新增

def try_launch_planned(
    ws: Path,
    active_tasks: dict[str, dict],
    *,
    store: FileBoardStore,
    eng,  # Engine 实例
) -> bool:
    """从 planned 列拉卡启动 dev_role。

    从 ccc-engine.py:1850-2114 搬迁。保留原有逻辑，只改 _activate_workspace
    → workspace_scope。
    """
    from engine.workspace import workspace_scope, get_current_workspace

    with workspace_scope(ws):
        ctx = get_current_workspace()
        store = ctx.store
        label = _ws_label(ws)
        # ... 原 1850-2114 逻辑完整搬入
        # 所有 _activate_workspace(ws) 调用删除（已在 scope 内）
        # 所有 _get_store(ws) 改为 ctx.store
        ...
```

**ccc-engine.py 改为薄委托**：

```python
# ccc-engine.py:1850 改为
def _try_launch_planned(ws: Path, active_tasks: dict[str, dict]) -> bool:
    """委托给 engine.dispatch.try_launch_planned。"""
    from engine.dispatch import try_launch_planned
    return try_launch_planned(ws, active_tasks, eng=_eng())
```

**验收命令**：
```bash
# 1. 行数检查
wc -l scripts/ccc-engine.py  # 应从 3333 降到 ~3050（降 ~280 行）
wc -l scripts/engine/dispatch.py  # 应从 68 涨到 ~350

# 2. 语法检查
python3 -m py_compile scripts/ccc-engine.py scripts/engine/dispatch.py

# 3. 单测全绿
pytest tests/scripts/test_engine.py tests/scripts/test_engine_phase_parallel_dispatch.py -v

# 4. 自检
bash scripts/ccc-self-check.sh
```

**commit message**：`refactor(engine): 搬迁 _try_launch_planned 264 行到 dispatch.py，主文件降至 ~3050 行`

---

## 执行顺序与优先级

| 顺序 | 修复 | 预估 | 风险 | 优先级 |
|------|------|------|------|--------|
| 1 | **修复 3** asyncio.coroutine | 0.5h | 极低 | P0 — 先让测试 0 failed |
| 2 | **修复 4** mypy_path | 0.5h | 极低 | P0 — 让 strict 真翻绿 |
| 3 | **修复 5** except:pass 残留 | 2h | 低 | P0 — 彻底清零 |
| 4 | **修复 1** retry budget 接入 | 1 天 | 中 | P1 — 最高价值 |
| 5 | **修复 2** workspace_scope 接入 | 1 天 | 中 | P1 — 第二价值 |
| 6 | **修复 6** 搬 _try_launch_planned | 2 天 | 中高 | P2 — God Module 首刀 |

---

## 验收总标准（全部完成后）

```bash
# 1. 测试 0 failed
pytest tests/scripts/ scripts/tests/ -q --tb=no
# 应输出: 1013 passed, 1 skipped, 0 failed

# 2. mypy strict 真翻绿
.venv/bin/python -m mypy --strict scripts/_config.py scripts/_utils.py scripts/_jsonl_rotate.py
# 应输出: Success: no issues found

# 3. retry budget 有实际接入
grep -rn "increment_retry_count\|can_retry\|RetryBudgetExceeded" scripts/ccc-engine.py scripts/engine/gates.py
# 应有 ≥3 处调用

# 4. workspace_scope 有实际调用
grep -rn "with workspace_scope" scripts/ --include="*.py" | grep -v "def workspace_scope\|#"
# 应有 ≥3 处

# 5. except:pass 残留为 0
grep -rn "except.*:" scripts/ -A 1 | grep -B 1 "pass$" | grep -v "except.*as "
# 输出应为空

# 6. ccc-engine.py 行数下降
wc -l scripts/ccc-engine.py
# 应 < 3100（从 3333 降）

# 7. pre-commit 6 hooks 全过
pre-commit run --all-files
```

---

*文档路径：[docs/CCC-代码质量下一步修复方案-2026-07-24.md](file:///Users/apple/program/CCC/docs/CCC-代码质量下一步修复方案-2026-07-24.md)*
