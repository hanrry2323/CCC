# KPI R2 修复：duration_s fill

| 字段 | 值 |
|------|-----|
| brief_id | `2026-07-23-kpi-r2-fix` |
| 对照 | R1 FAIL 主因 queue；次因 duration fill 0.286 |
| 本轮主攻 | `duration_s_fill`（allowlist）；queue 观察不靠加并发 |

## 改动

1. `_log_opencode_done`：`result.json` 缺 `duration_s` 时用 `wall_s` 回填  
2. `board_ops` / `script_seed` result.json 写入 `duration_s`

queue_wait≤300 仍为 stretch；同仓 1 OpenCode 下 6 卡串行有地板，R2 先收观测门再看排队 Δ。
