# KPI R5 修复：queue 独立卡主门 + 卫生 dirty 豁免

| 字段 | 值 |
|------|-----|
| brief_id | `2026-07-23-kpi-r5-fix` |
| 对照 | R4 验收 [`2026-07-23-kpi-r4-eval.md`](./2026-07-23-kpi-r4-eval.md) · 方案 [`2026-07-23-kpi-r5-opt-plan.md`](./2026-07-23-kpi-r5-opt-plan.md) |
| 科学校验 | R4 队列回放 → 新 scorecard **PASS**（`test_r4_replay_indep_gate_would_pass`） |

## 改动

1. **scorecard**：主门 `queue_wait_p95_s` → `time_agg.queue_wait_indep_s.p95`≤300；全量 `queue_wait_p95_all_s`≤900 观测  
2. **efficiency-report**：标记 `serial_successor`（`-w2+`），产出 indep/successor 分位数  
3. **`_task_commit`**：卫生判定优先于 product 非空；`.ccc-only`/ops 只 stage `.ccc/**`；`docs/reports/**` 为 harness 噪声不 dirty_block  
4. **权威 / consensus / stress-kpi-loop**：写入 queue 双轨口径  

禁止：加 `MAX_CONCURRENT`。
