# 计划页面（#/plans）实施方案 · Codex 审查意见

> 审查人：Codex（W3，方案外脑/审核执行体）· 日期：2026-08-09
> 审查对象：`docs/notes/2026-08-09-plans-page-implementation.md`（v1.0，待审批）
> 结论：**方向正确、结构完整、可执行。附 2 个必须先改的点 + 3 个建议，改后定稿。**

---

## 〇、执行前提（老板硬性，方案须补充）

**本次实施不走 CCC 自动流程。** 由 Claude Code 亲自开发执行；不建卡文件、不进看板、不走 Engine 派发、不机审合入。

因此 §8「拆卡 8 张（ccc021~ccc028）」**改为 8 个实施阶段/检查点**，编号仅用于排序，不产生任务卡：

| 阶段 | 内容 | 审核人 |
|------|------|--------|
| S1 | 方案模板 + 路径规范落地 | Codex |
| S2 | 存量文档处置 | Codex |
| S3 | 入口文档更新 | Codex |
| S4 | INT 体系退役 | Codex |
| S5 | 后端 API | Codex |
| S6 | 前端页面 | Codex |
| S7 | 导航 + 路由 | Codex |
| S8 | 端到端验收 | Codex（总验收） |

每个阶段完成即由 Codex 审核，通过后再进下一阶段；最终 S8 全链路验收。Claude Code 全程亲自写码，不走任何自动化派发。

---

## 一、必须修改（2 项，改后方可定稿）

### M1 · 补「qx-map 方案主档红线修订」，否则双主档冲突

方案 §6.2 只写了移除 qx-map `AGENTS.md` 的 INT 分派章节，**漏了一条关键红线**：

qx-map `AGENTS.md` 现有硬规则：「所有方案/决策全文唯一主档 = `__archive__/decisions/`，hp-kb 仅索引」。

本次方案把活跃方案主档迁到 CCC `docs/projects/<prefix>/plans/`，与这条红线直接冲突。必须同步修订：

- qx-map `AGENTS.md`「方案/决策单一主档」段改为：**决策定稿归档 = qx-map `__archive__/decisions/`；活跃方案主档 = CCC `docs/projects/<prefix>/plans/`**
- hp-kb 索引规则同步：索引指针指向 CCC 权威源路径，不再指向 qx-map
- 修订动作并入 S3（入口文档更新）或 S4，验收标准里显式列出

不修的话，三个 CLI 会读到两处"唯一主档"，口径重新分裂——正是本次要消灭的问题。

### M2 · 存量迁移清单须老板过目一次（涉及 qx-map 外脑仓）

§4.2 位置 D 有 4 个 qx-map 决策档要迁入 CCC plans/。方向与老板"qx-map 退外脑"的定位一致，但 qx-map 有「大迁移须人确认」的红线：

- 迁移前把「位置 A–G 全量处置清单」给老板过目确认（尤其 qx-map 4 个迁出文件）
- 确认后执行 S2；每个迁移文件旧位留 stub 跳转
- `ccc-开发与业务分离-架构基线-2026-08-09.md` 昨天刚落、尚未定稿，迁入 `plans/006` 后保留原文、继续演进，不截断

### M2 核验结果（2026-08-09 · Codex 逐项对盘）

**已核验通过**：
- 清单 10 个源文件全部真实存在（CCC notes 3 个 + qx-map decisions 7 个）✓
- 目标 `docs/projects/ccc|xy|qb/plans/` 为全新目录，编号无冲突 ✓
- CCC `docs/notes/` 13 个文件全部有处置（迁 3 / 标史 4 / 迁 archive 2 / 方案自身 2 / review 1 / 本方案 1）✓

**必须补 2 项（清单遗漏的活跃方案）：**

| # | 来源 | 文件 | 处置 |
|---|------|------|------|
| 11 | qx-map decisions/ | `ccc-loop-engineering-评估与100卡基线计划-2026-08-08.md`（08-08 定稿 v2，活跃计划，无废弃标记） | 迁入 `docs/projects/ccc/plans/007-loop-engineering-100-cards.md` |
| 12 | qx-map decisions/ | `claude-code-心智分层-方案-2026-08-03.md`（方案定稿、待老板确认文案后执行） | 迁入 `docs/projects/ccc/plans/008-claude-mind-tiering.md`；备注「待核：是否已被 2026-08-06 双脑架构替代」，状态按原文保留 |

**必须显式归类 4 项（不迁，但清单须逐行列出，不能吞进「其余 ~50 个不动」）：**

| 文件 | 归类 | 动作 |
|------|------|------|
| `hp-qxmap-整合-执行方案-2026-08-03.md` | 🔴 作废 | 文件内补废弃标记（与 INT-040「Trae 停用，定位收敛暂停」口径一致） |
| `quanthive-退出链路加固-方案-2026-08-03.md` | 🟡 历史 | 标史（INT-041 已关闭，QuantHive 独立轨道不迁） |
| `ccc-refactor-方案-定稿-2026-08-02.md` | 🟡 永久基线 | 留 qx-map（"永久基线"例外，不衰减不迁移） |
| `中转站单点化评估与2017优化-方案` / `loop-router-dashboard-卡片化双机改造-方案` / `ccc-execution-roadmap-2026-08-03.md` | 🟡 历史 | 标史（均已落地/被替代） |

**建议**：本方案（`2026-08-09-plans-page-implementation.md`）定稿后自身迁入 `docs/projects/ccc/plans/`，不留 notes（自举收口，编号在 S1 定模板后分配）。

以上补完后清单即闭环，可开始 S1。

---

## 二、建议（3 项，不阻塞，实现时并入）

1. **转卡后方案状态自动推进**：`/plans/convert` 成功调 `new-card.sh` 后，方案状态自动从「已确认/草案」推进为「部分执行」，减少人工改状态；关联卡字段由脚本写入。
2. **前端补单测**：§8.1 只写了后端 API 测试和端到端。CCC 前端已有 vitest 基建，S6 验收标准补：列表渲染/筛选/转卡按钮触发均有单测。
3. **合并类迁移保留原文**：位置 D「线路图升级」与 `ccc/plans/002` 合并时，先保留原文件全文再合并，禁止丢内容；原文件标「已迁移至 CCC」。

---

## 三、确认采纳（无需再议）

- 路径规范（`docs/projects/<prefix>/plans/<NNN>-<slug>.md`）与卡编号分区独立 ✓
- 模板六段 + 五态状态机 ✓
- 前端 `#/plans` 路由、看板与线路图之间导航、「转为任务卡」人触发 ✓
- API 五端点（list/detail/create/update/convert）✓
- 存量处置四分类（迁/标史/作废/不动）+ 旧位 stub ✓
- INT 体系完整退役（标记 + 守卫降级 + SOP 作废 + 只读历史保留）✓
- HP 只读向量索引、不双写、第二阶段实施 ✓
- 红线（不双写/不自动转卡/不破坏现有功能/notes 不新建方案）✓

---

## 四、定稿后动作

1. Claude Code 按本意见修订方案（M1/M2 + 建议并入）→ 方案状态置「已确认」
2. 老板确认存量迁移清单（M2）
3. 按 8 阶段推进，每阶段完成报 Codex 审核
4. S8 总验收通过后，方案置「已完成」，页面上线

---

## 五、S5 打回指令（2026-08-09 · Codex 审核结论）

**结论：S5 代码本身合格，但测试未入库，打回补件。**

验收现状：`server/board/plans.py`（468 行）与 `server/web/server.py` 5 个端点已实现且实测可用（列表/详情/路径白名单防穿越/自动编号/写盘后校验自删/状态五态/转卡推进）。但提交 `b3937c6c` 无测试文件，`server/tests/` 无 plans 测试，CI pytest 门禁不覆盖该模块。

**补件要求（唯一验收路径）：**

新增 `server/tests/test_plans.py`，覆盖：

1. `list_plans`：按项目/状态/关键词筛选 + 排序
2. `get_plan`：正常读取；**非法路径拒绝**（含 `../` 目录穿越用例）
3. `create_plan`：自动编号递增；非法前缀拒绝；写盘后校验失败自删（用临时目录做隔离）
4. `update_plan`：非法状态拒绝（五态校验）；内容替换保留头部
5. `convert_plan`：缺「转卡计划」段报错；出卡调用 mock（不真建卡）

**复验标准**：`pytest server/tests/ -v --tb=short` 全绿（含既有 43 个测试不回归）；提交后报 Codex，Codex 独立复跑通过即放行 S6。

**收口注意**：工作树未跟踪的 `uv.lock` 若非本任务产物，确认后清理或加入忽略，不得混入提交。

**打回记录（防重放）**：

| 轮次 | 时间 | Claude Code 提交 | Codex 动作 |
|------|------|------------------|------------|
| 1 | 15:09 | `b3937c6c`（无测试） | 打回：要求补 `server/tests/test_plans.py` |
| 2 | 15:2x | `b3937c6c`（重复） | 打回：指令落盘本文件第五节 |
| 3 | 15:4x | `b3937c6c`（重复） | 打回：本记录；阻塞状态 |

**当前状态：S5 阻塞，等待补件**。`git log` 无新提交、`server/tests/` 无 plans 测试、工作树无未提交补件。补件完成后按上方复验标准走；未补件前任何重复提交一律视为无效。
