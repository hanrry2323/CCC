# 2026-08-06 E2E Smoke（ccc001）

## 目的

最小可验收的端到端烟雾：验证 Engine 从任务卡派发 Claude Code → 建 worktree → 新增本文件 → commit+push 到独立分支 → 回写卡头为「已回写」的整条流水线贯通。

## 验证链路

1. 卡头「派发：engine」→ Engine 识别。
2. Engine 建 worktree `ccc-dev-ws-ccc001`（10 位分支）。
3. 本文件为新栈唯一改动（≤20 行）。
4. commit+push 到 `codex/ccc001-e2e-smoke-engine-dirty`。
5. 卡头「状态」由 Engine/执行体回写为「已回写」。

## 结果

本文件由 Claude Code 执行体在独立 worktree 内新增，证明 E2E 单端流水线可用。
