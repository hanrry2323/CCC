# M6 方案（待审批后执行）

> 北星不变。冻结心智补丁。

## 目标

消化执行中积压（xy002），并把 ccc007 经「合入批准」合入；巩固真机审为默认路径（少手动 `--audit`）。

## 范围

1. **ccc007 合入**：老板人审后 `approve-merge.sh ccc007`（ff；验证 rebase 提醒生效）。
2. **xy002**：取证推进——已回写则走机审/合入；超时/跑偏则打回挂账（卡红线：只动 xianyu）。
3. **自动机审默认路径**：确认「已回写收单 → Engine 自动拉机审」在现网至少再狗粮 1 次（不靠手工 `--audit`）；缺口只修脚本/Engine，不写 SOP。
4. **回归**：既有 pytest + ready_for_merge 空/非空断言抽查。

## 不做

新席位/同义句/Hub/Desktop/Agent SOP。

## 验收

| 指标 | 门槛 |
|------|------|
| 老板调度 | ≤2 |
| xy002 离「执行中」或明确打回挂账 | 是 |
| ccc007 已关闭（合入批准） | 是（听老板口令） |
| 新 SOP | 0 |
