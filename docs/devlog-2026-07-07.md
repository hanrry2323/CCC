# 开发日志 — 2026-07-07

## 阶段：v0.18 — 自动化开发管线通线

> 1 天内完成 6→7 角色升级、看板 UI、opencode 集成、回测角色。
> 核心目标：从"手动点按钮"到"任务 backlog → 自动开发 → released → 回测"闭环。

---

## 一、完成事项

### 1. 架构清理

| 操作 | 文件 | 说明 |
|------|------|------|
| 删除 | `skills/ccc-planner/`、`skills/ccc-verifier/` | 旧 3 角色遗留 |
| 重写 | `SKILL.md`、`CLAUDE.md` | 从 3 角色 pipeline 改为纯 6 角色 |
| 更新 | `references/red-lines.md` | 红线 6 名更新 |
| 清理 | 5 个死脚本（`ccc-precheck/finish/queue/auto-dev/poll`） | 标记废弃 |

### 2. 6+1 角色系统

| 角色 | 频率 | 技能 | 状态 |
|------|------|------|------|
| product | 4h | `ccc-product` | 待办收件箱（不改自动动弹） |
| dev | 10min | `ccc-dev` | 每次 1 个 planned → opencode → testing |
| reviewer | 2h | `ccc-reviewer` | py_compile 通过 → verified |
| tester | 4h | `ccc-tester` | pytest 通过 → verified |
| ops | 30min | `ccc-ops` | 健康检查 |
| kb | 23:00 | `ccc-kb` | git tag + push → released |
| **regress** | **23:30** | `ccc-regress` | 每日回测 released，回归→建 bug |

### 3. 看板系统

- HTTP 服务（`ccc-board-server.py`）绑定 **:7777**
- 看板 UI（`index.html`）6 列中英对照
- REST API: `/api/board`、`/api/tasks`、`/api/tasks/move`、`/api/config`、`/api/roles`、`/api/timeline`、`/api/logs`
- launchd plist `com.ccc.board` KeepAlive 自启

### 4. 自动化开发管线（核心）

```
backlog(收件箱) → planned → in_progress → testing → verified → released
                      ↓           ↓            ↓        ↓         ↓
                   product     dev(opencode)  reviewer  tester    kb
                   (我/Claude)                  +tester           +regress(23:30)
```

**关键修复**：
- dev 改"每次 1 个"（原来扫全部 planned = 超时）
- timeout 从 phases.json 读（可配），删除硬编码 90s
- `opencode-exec.py` PATH 回退 `~/.npm-global/bin/opencode`（launchd 没有这个 PATH）

### 5. 回测角色（第 7 角色）

- `com.ccc.regress` 每天 23:30
- 扫 released → py_compile + git diff → 发现回归→建 bug
- `regression-<原task>` 到 backlog

### 6. 待办 vs 计划 正确定义

| 列 | 用途 | 自动进 dev？ |
|----|------|:----------:|
| backlog | 临时想法/商讨中/未来功能 |  |
| planned | 已确定、ready-to-run | 进入自动化 |

---

## 二、当前看板状态

```
backlog：0       — 待办收件箱空
planned：12     — 等 dev 逐个处理
in_progress：0  — 清理后的空状态
testing：0      
verified：0     
released：5     — 已完成
```

12 个 planned 任务，dev 每 10min 跑 1 个 → 约 2h 全部处理完。

---

## 三、已知问题

1. **opencode 执行慢**：每个任务 ~15-30s 启动 + 执行时间。小颗粒任务可接受。
2. **回测会假阳性**：文档/配置类任务也会被 py_compile 检查，后面按 task 类型过滤。
3. **product 仍用模板**：当前 product_role() 只写占位模板，改为调 Claude API 拆任务在 backlog 计划中。
4. **看板 UI 暂缺角色状态**：`/api/roles` 和 `/api/timeline` 后端已实现但前端未渲染。

---

## 四、技术指标

- 仓库线数: `+7102 / -2204`
- 角色 plist: CCC 7 + qxo 7 = 14
- 看板 API 端点: 7个
- 测试通过: 全绿
- opencode 版本: 1.17.13
