# Release v0.63.0 — bg nudge 真注入 + KPI 缩小复跑

**日期**：2026-07-28  
**一句话**：`nudge_bg_session` 真 `--resume` 注入；prepare 放行新建文件 scope；ccc-demo KPI 缩小 r1 PASS。

## 范围

| 面 | 内容 |
|----|------|
| Engine | `nudge_bg_session` dry_run / spawn；`_role_tool` create-new-file scope |
| Stress | matrix `--apps`；`stress-mx-20260728-kpi-r1` PASS |
| Docs | Layer1 出门收口；程 B；周刊金路径 checklist |

## 验收

```bash
pytest tests/scripts/test_long_lived_session.py tests/scripts/test_role_tool.py -q
bash tests/scripts/test_nudge_bg_session.sh
bash scripts/ccc-dual-host-check.sh
```

## 对齐

2017：`git pull --ff-only` + kickstart Engine/Hub。  
禁止 M1 起 Engine/--bg。

详见 [`CHANGELOG.md`](../../CHANGELOG.md) · [`../product/loop-engineer-authority.md`](../product/loop-engineer-authority.md)。
