# Release v0.61.0 — 三档契约 + fleet 状态机 + 双机同步门禁

**日期**：2026-07-25  
**一句话**：下游只认 `flash`/`Pro`/`code`；`ccc-fleet.sh` 统一启停；配置 SSOT 分层；Engine 启动前双机对齐。

## 范围

| 面 | 内容 |
|----|------|
| 共识 | 三档契约 + 上游解耦；M1 Desktop 双身份隔离；`--bg` 占位（后由 v0.62 交付） |
| Fleet | `ccc-fleet.sh` start/stop/status/health；M1=对话栈 / 2017=编排栈 |
| Self-check | 11 项 + `--preflight` 强门禁 |
| Config | `RelayConfig` / `EngineConfig` / `BoardConfig` dataclass |
| Sync | `ccc-sync-after-push` + dual-host 门禁 |

## 拓扑（硬）

- **M1**：sidecar `:7788` + `relay.m1` + `hub-tunnel` `:17777`
- **Mac2017**：Hub + Board + Engine + `relay.2017`
- 禁止 M1 跑 Hub/Board/Engine

详见 [`CHANGELOG.md`](../../CHANGELOG.md) · [`../deploy/topology.md`](../deploy/topology.md)。
