# Release v0.64.0 — 意图卡供给闭环

**日期**：2026-07-29  
**一句话**：人点「转意图卡」→ Agent 写 L1 → `transfer_gate` 绿才自动进代办；错卡零 OpenCode。

## 版本切片说明

原计划分 **v0.64 / v0.65 / v0.66** 三 bump；落地时**合成一版 `VERSION=v0.64.0`**（能力齐，不另发 0.65/0.66 空号）：

| 计划切片 | 能力 | 状态 |
|----------|------|------|
| 0.64 | 三角色 + SOP + L1→gate→代办 + 右栏收口 | ✅ 本版 |
| 0.65 | 多卡链；逐卡 gate；一卡红不堵链 | ✅ 本版 |
| 0.66 | 耗尽回流新意图卡；须人再点转 | ✅ 本版 |

## 范围

| 面 | 内容 |
|----|------|
| 产品 | 转意图卡 ≠ 定代办；战略讨论优先；禁 invent / 禁自转入队 |
| Hub | `POST …/intent-cards`；`POST …/transfer/validate`；`seed_planned_from_exhaust` |
| Desktop | 快捷「转意图卡」；自动 promotion；右栏=看板计数+意图卡链 |
| SOP | [`references/intent-card-sop.md`](../../references/intent-card-sop.md)（旧 finalize 重定向） |
| 运维 | patrol GREEN 自动归档过期 `authority-patrol` 告警（防假红） |

## 验收

```bash
pytest scripts/tests/test_intent_card_flow.py tests/scripts/test_authority_patrol.py -q
python3 scripts/ccc-authority-patrol.py --dry-run
# Desktop 已装：CFBundleShortVersionString == 0.64.0
```

## 人机主路径（现行）

```text
战略讨论 → 人点「转意图卡」→ L1 planned → gate 绿 → 自动进代办 → Engine
         → gate 红 → 卡留意图层 + fix_hint（零 OpenCode）
```

对齐：2017 `git pull --ff-only` + kickstart Hub；M1 重装 Desktop baseline。

详见 [`CHANGELOG.md`](../../CHANGELOG.md) · authority「意图卡供给闭环」· [`desktop-flow-rail-ux.md`](../product/desktop-flow-rail-ux.md)。
