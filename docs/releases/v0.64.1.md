# Release v0.64.1 — 意图卡右栏 UX + 入口统一

**日期**：2026-07-29  
**一句话**：右栏去掉「讨论方案」；转意图卡统一过门；僵尸 planned 可清；validate 失败不入队。

## 范围

| 面 | 内容 |
|----|------|
| 右栏 | 意图卡 `1/N` · 待转/未过门 · 无讨论按钮 · 已进代办闪标 |
| Desktop | 入口 funnel；文案转意图卡；删 FlowCanvas/taskStack |
| Hub | `abandon-orphans` API |
| Docs | flow-rail / authority / consensus |

## 验收

```bash
pytest scripts/tests/test_intent_card_flow.py \
  scripts/tests/test_desktop_api.py \
  scripts/tests/test_post_exhaust_optimize.py -q --tb=short
```

手测 H1–H8：见计划「意图卡右栏 UX + 缺口补充」§3.2。

## 收尾

push → 2017 pull + kickstart Hub → `package-baseline.sh` → 装 App `0.64.1`。
