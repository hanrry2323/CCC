# Release v0.60.1 — Desktop 发版 + 运维后勤对齐 + 产线闸门

**日期**：2026-07-23  
**一句话**：把已合 main 的 **后勤 Ops UI** 与 **产线提效 P0–P5** 装进同版：Desktop 重打包、双机同 commit、Hub 资产缓存作废。

## 范围

| 面 | 内容 |
|----|------|
| Desktop | `package-baseline.sh` → `/Applications/CCCDesktop.app` **0.60.1**（含 OpsView 后勤供弹） |
| Hub Ops | logistics-first 页面；`HUB_ASSET_VERSION=20260723eff1` |
| Engine | 槽生命周期 / result 契约 / 安全 revert / testing 墙钟超时杀进程 / 短路径硬门 |

## 对齐清单

```bash
# 双机同 HEAD
bash scripts/ccc-dual-host-check.sh

# M1 Desktop
bash desktop/scripts/package-baseline.sh
rm -rf /Applications/CCCDesktop.app && cp -R desktop/.build/CCCDesktop.app /Applications/

# 服务
launchctl kickstart -k "gui/$(id -u)/com.ccc.agent-sidecar"   # M1
# 2017: chat-server + engine + board
```

详见 [`../runbooks/pre-test-dual-host-sync.md`](../runbooks/pre-test-dual-host-sync.md)。

## 刻意不做

- 取消同仓 1 路 OpenCode  
- 仅靠加大 `MAX_CONCURRENT`  
- Desktop App Store / notarize
