# Release v0.62.0 — claude --bg 长任务 + 双机拓扑收口

**日期**：2026-07-26  
**一句话**：Mac2017 reviewer 长 session（`--bg`）+ Hub/Desktop 透出；双机拓扑与 fail-open 真直连收口。

## 范围

| 面 | 内容 |
|----|------|
| Engine | `ccc-reviewer-bg.sh`、`LongLivedSession`、fleet stop 清真进程 |
| Hub | `/api/ops/bg-sessions`、envelope `domains.bg_sessions` |
| Desktop | OpsView 长任务卡（只读展示 2017 状态） |
| Relay | fail-open `CCC_RELAY_DIRECT_URL` 禁止指回 `:4100` |
| 拓扑 | M1 禁 Hub/Board/Engine；隧道默认；权威 `--bg` 口径改为已交付 |

## 未交付（v0.63）

- `nudge_bg_session` 真注入（当前写文件占位）
- 全链路 E2E smoke

## 对齐清单

```bash
# M1
bash scripts/ccc-dual-host-check.sh          # 默认隧道 :17777
bash scripts/ccc-fleet.sh start ui-tier

# 2017（同 VERSION/commit）
git fetch && git merge --ff-only origin/main
bash scripts/ccc-fleet.sh stop all
bash scripts/ccc-fleet.sh start all
curl -u ccc:ccc http://127.0.0.1:7777/api/desktop/version
```

详见 [`CHANGELOG.md`](../../CHANGELOG.md) · [`../product/loop-engineer-authority.md`](../product/loop-engineer-authority.md)。
