# Release v0.60.0 — LPSN 意图飞轮

**日期**：2026-07-22  
**一句话**：**上线 ≠ 完成**；平台把门禁与飞轮做成 L→P→S→N，使 `released` 不再被叙述成意图完成。

## 范围

| 阶段 | 落地 |
|------|------|
| L | Engine → verdict → `released`（既有） |
| P | `_intent_probe` + transfer/acceptance/tester/regress 共用；regress 重放失败建回归 epic |
| S | L1 goals `{exit_condition,status}` + `intent_stable` API / `ccc-mind-update --stable` |
| N | baseline `next_product_goal`；未 S 新开无关产品 epic 须 `supersede_goals` |

出门清单：[`../product/lpsn-ship-gate.md`](../product/lpsn-ship-gate.md)。

## 验证

```bash
pytest tests/scripts/test_intent_probe_lpsn.py -q
bash tests/e2e/test_lpsn_flywheel.sh
python3 scripts/ccc-authority-patrol.py
```

## 刻意不做

P3 多端薄客户、Temporal、Hub 聊天回潮、多仓 VIP→P1 全自动排序。
