# KPI R3 修复：queue 短路径绕互斥 + commit scope + 确定性 FAIL verdict

| 字段 | 值 |
|------|-----|
| brief_id | `2026-07-23-kpi-r3-fix` |
| 对照 | R2 FAIL [`2026-07-23-kpi-r2-eval.md`](./2026-07-23-kpi-r2-eval.md) |
| 主攻 | `queue_wait_p95_s`（短路径绕同仓 OpenCode 互斥）；`work_abnormal_n`（确定性 FAIL 必写 verdict + `commit_gate_hygiene_vs_business_dirty`） |
| 顺带 | `duration_s` 双空回填 0.0（观测门） |

## 改动

1. **`ccc-engine.py`**：`script_seed` / `board_ops` 探测与执行移到 `_workspace_blocks_new_opencode` **之前**（短路径不占槽、不被活 opencode 延后）
2. **`reviewer.py`**：确定性路径 py_compile/acceptance 失败 → 写 **FAIL + fixable** verdict（禁「未产出 verdict」假 quarantine）
3. **`_task_commit.py`**：DoD auto-commit 优先 plan scope / `result.wrote`；范围外业务脏 **留 unstaged**，不吞进本卡 commit
4. **`_log_opencode_done`**：result dict / wall 皆空时 `duration_s=0.0`，保 fill_rate 可统计

禁止：加 `MAX_CONCURRENT`。
