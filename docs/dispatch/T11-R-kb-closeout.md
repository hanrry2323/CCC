# 任务卡 T11-R · T11 收尾：真实提交 + 状态同步（Trae 执行）

> 关联：INT-120（CCC 重构）· 契约：CCC 重构契约 v1（§3 状态同步）· 管理席：Codex
> 执行体：Trae（手动）· 验收：Codex · 状态：待分派 · 日期：2026-08-02
> 前置：T11 验收打回（代码功能独立核验通过：148 测试全绿 / selftest ALL PASSED / 零外脑依赖；纪律未过，见问题清单）

## 打回问题清单（T11 未通过原因）

1. **工作未提交**：T11 全部产出（`server/kb/` + 3 个测试文件 + 4 处修改）仍是未跟踪 / 未提交状态，验收标准第 5 条「真实提交；工作树仅剩 2 个预存项」不满足。
2. **卡头状态未同步**：T11 卡「状态」仍为待分派，违反契约 §3 与任务书回写要求（接单→执行中、回写→已回写）。
3. **反问「是否需要提交代码」**：任务书第 7 步已明确「提交 `chore(kb):`，回写真实 commit hash」——提交是执行体分内动作，不是选择题；把验收标准的执行甩回管理席，违反「验收标准不可自行解释」。

## 目标

完成 T11 收尾：真实提交 + 卡头状态同步 + 复验，使验收标准全部满足。

## 红线（先看）

1. 只做收尾动作：提交已有产出，不新增功能、不改代码逻辑（复验发现必须小修除外，须在回写说明）。
2. 提交范围只含 T11 相关文件：`server/kb/`、`server/tests/test_kb_*.py`、`.gitignore`、`knowledge/README.md`、`server/README.md`、`server/config/config.example.env`；**不得带入** `scripts/.ccc/agent-mind/decided.json` 与 `_update_handoff.py`。
3. 不碰运行面 / 旧代码 / 外脑；验收标准不可自行解释；不删除任何文件。
4. 工作树最终仅剩 2 个预存无关改动。

## 步骤

1. `git add` 上述 T11 范围文件；`git status` 确认未夹带 2 个预存无关改动。
2. 提交一个 commit：`chore(kb): T11 知识库 MCP+本地语义检索`。
3. 更新 T11 卡头状态：`待分派 → 已回写`（回写区补本收尾说明）。
4. 复验：`pytest server/tests/ -q` 全绿 + `python3 -m server.kb.mcp_server --selftest` ALL PASSED + `git status` 仅剩 2 预存项。
5. 回写真实 commit hash。

## 验收标准（Codex 按此验收）

1. 存在真实 `chore(kb):` commit，含全部 T11 文件，未夹带预存无关改动。
2. T11 卡头状态 = 已回写（验收席通过后改已关闭）。
3. 复验输出完整（测试 / selftest / git status）。
4. 工作树仅剩 2 个预存项；未碰旧代码 / 运行面 / 外脑。

## 回写要求

结果摘要（人话一句）、commit hash、复验输出、验收自检对照表；按契约 §3 同步更新本卡卡头状态（接单→执行中，回写→已回写）。

## 回写区

（Trae 回写）
