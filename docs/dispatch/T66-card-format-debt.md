# 任务卡 T66 · 历史卡格式存量债统一（Claude Code 执行）

> 关联：任务卡体系规则（旧卡 69 处格式偏差规范化）· 执行体：Claude Code · 验收：Codex · 状态：待分派 · 派发：engine · 项目：ccc · 日期：2026-08-05
> 工作目录：请先创建独立 worktree `git -C /Users/fan/program/CCC worktree add /Users/fan/program/ccc-dev-ws-t66 -b codex/t66-card-format origin/main`；分支 `codex/t66-card-format`
> **分步提交纪律（硬）**：按项目/批次分步 commit+push；超时 7200s。

## 目标

把历史卡（T1–T54，根目录平铺）卡头统一为合规格式（`> 关联/执行体/状态/日期` 字段齐全、状态值合法、`项目`/`派发` 字段补齐），validate 全绿；不改卡正文内容（目标/验收/回写区只补格式不动语义）。

## 具体项

1. **盘点**：validate.py 扫出全部不合规卡清单（缺字段/状态非法/缺回写区/缺验收标准），分类列出。
2. **统一重写卡头**：每张卡补齐 `关联/执行体/状态/日期`（状态值=五态之一，按实际流转回写）；补 `项目`（从关联推导或映射表）与 `派发`（历史卡=manual，防 Engine 误派）。
3. **正文零语义改动**：目标/红线/范围/步骤/验收标准/回写内容只补格式（如缺 `## 验收标准` 节则从回写要求推导补最小可执行项并标注），不改变原意。
4. **validate 全绿**：修完 validate 零错误（保留「历史卡」宽容规则按需移除）。
5. 回归：loader 扫描/看板/Engine 派发不受影响（新旧混合测试）。

## 红线

1. 只改 docs/dispatch/*.md（T1–T54 根目录卡）+ validate 相关（如需）；**禁止改 server/ 代码逻辑、前端**。
2. 每张卡的语义不得改变（只规范化格式）；改前 git 记录可追溯（逐卡 commit 或分批）。
3. 回写前 push 成功并附证据。

## 验收标准

1. validate 全绿（0 错误）；旧卡字段齐全、状态合法、项目/派发补齐。
2. 抽样核对（≥5 张）语义未变（正文原意保留）。
3. loader/看板/Engine 新旧混合实测正常（测试任务先行占位卡验证）。
4. pytest 全绿、push 证据。

## 回写要求

卡头状态更新为「已回写」；回写区填：盘点清单、重写批次、validate 结果、抽样核对、混合测试、push 证据。

## 回写区

**执行体**：Claude Code（2017）· 日期：
