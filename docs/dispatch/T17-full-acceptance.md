# 任务卡 T17 · 全量验收 + 问题修复（Claude Code 执行）

> 关联：INT-120（CCC 重构）· 契约：CCC 重构契约 v1 · 决策：ccc-refactor-方案-定稿 v2 · 管理席：Codex
> 执行体：Claude Code（CLI）· 最终验收：Codex · 状态：已关闭 · 日期：2026-08-02 · 派发：manual · 项目：ccc
> 定位：M4 前最后一道大闸。对 T0–T16 全链路做独立全量验收（不看回写、只信取证），发现问题并修复 P0/P1。

## 目标

对 CCC 重构全链路（T0–T16）做一次独立、全面的验收：对照契约 §1–§10、决策 D1–D10、M4 三门槛，取证找问题，修复 P0/P1，产出验收报告。

## 关键纪律（先看）

1. **本卡不做自我验收**：你负责取证、找问题、修复；最终验收由 Codex 复核。报告必须区分「发现的问题」与「已修复项」，不得写自我验收结论。
2. **修复范围受限**：只改 `server/`、`docs/`（含任务卡状态回写）、必要时 `desktop/Sources/` 壳代码；`scripts/` 旧引擎**零改动**（退役第二阶段未放行）；不碰运行面（2017 旧引擎、7788、运行中桌面端、launchd）；不部署不迁移。
3. 不删除任何文件（修复只编辑/新增；确需清理须在报告说明并经管理席确认）。
4. 不落密钥；不读写外脑（HP / qx-map）；不硬编码。
5. 验收标准不可自行解释；修复必须全量测试复绿 + 真实提交；工作树只允许预存 2 个无关改动（`scripts/.ccc/agent-mind/decided.json`、`_update_handoff.py`），不得带入提交。

## 步骤

1. **取证（只读）**：
   - 提交链与工作树：`git log`、`git status`、旧代码隔离检查（scripts/app/desktop/lib/db 在本轮改动中的出现次数应为 0——app/lib/db 已归档，scripts/ 必须为 0）。
   - 三扫描：硬编码（S1–S4）、明文密钥、外脑依赖（HP/qx-map/192.168.x 引用）全量重跑。
   - 关键模块冒烟：`server.engine.main --once`（含缺配置退出码）、`server.kb.mcp_server --selftest`、web API（未鉴权 401 / /session 成功 / board 接口鉴权后 200）、`server.board.export` 与三视图/线路图一致性。
   - 全量测试 `python3 -m pytest server/tests/ -q`。
2. **对照验收**：契约 §1–§10 逐节、决策 D1–D10、M4 三门槛（退役/壳对接/E2E）→ 产出 L4 对照表（承诺/实际/判定）。
3. **问题清单**：P0（阻断/缺陷）、P1（应修）、P2（登记）；已知线索必须验证（不预设结论）：
   - `test_board_loader` 状态断言是否应改用 `base_state`（契约允许括号变体，此前已两次触发失败）；
   - 卡头状态与契约 §3 一致性抽查；
   - 中转站双实例配置（M1 4100/4102 未动、2017 6100/6102 配置一致）只读核查；
   - 退役清单与第二阶段计划完整性（只读）。
4. **修复**：P0/P1 在授权范围内修复；P2 登记不修（除非顺手且零风险）；修复后全量测试复绿。
5. **报告**：`docs/acceptance-full-2026-08-02.md`——取证结果 / L4 对照表 / 问题清单（含分级）/ 修复记录（含 commit）/ 遗留（含未放行项与理由）。
6. **提交**：`chore(acceptance):`，回写真实 commit hash；按契约 §3 更新本卡卡头状态（接单→执行中，回写→已回写）。

## 验收标准（Codex 复核）

1. 报告覆盖契约 §1–§10、决策 D1–D10、M4 三门槛，L4 对照完整、判定有据。
2. 三扫描零命中；scripts/ 零改动实测；运行面零接触。
3. P0/P1 已修复且测试为证；P2 全登记；已知技术债（loader base_state）已修或说明为何不修。
4. 全量测试 0 失败；修复提交真实、范围干净、未夹带预存项。
5. 报告区分发现/修复，无自我验收结论。

## 回写要求

结果摘要（人话一句）、报告路径、问题分级统计、修复 commit、遗留清单；按契约 §3 同步更新本卡卡头状态。

## 回写区

**执行体**：Claude Code（CLI）· **日期**：2026-08-02

**结果摘要（人话一句）**：对 T0–T16 全链路独立验收无 P0；修复 3 个 P1（loader 状态断言按基础态、web API 模式适配 T16 鉴权、README 鉴权章节过期）+ 6 个零风险 P2，`server/tests/` 171 全绿、三扫描零命中、`scripts/` 零改动、运行面零接触。

**报告路径**：`docs/acceptance-full-2026-08-02.md`

**问题分级统计**：P0 = 0 · P1 = 3（全部已修）· P2 = 10（6 顺手已修，4 登记）

**修复 commit**：`5a19fd6` `chore(acceptance): T17 全量验收——取证/对照/修复 + 报告`（13 文件：server 修复 10 + 报告 + 卡头执行中 + board.js 重导出）
> 本卡回写 commit 见 git log（`docs(dispatch): T17 回写`）。

**遗留清单**（详见报告 §5）：
- `scripts/` 退役（第二阶段）：未放行——2017 旧引擎仍运行、qb 产线仍引用 `scripts/`、老板未放行；`legacy-phase2-plan.md` 含完整步骤与回滚
- `relay/dist/` 清理：第一阶段待执行项
- `tests/scripts/test_authority_patrol.py` 2 失败：T0 清场归档所致预存遗留，修复需改 `scripts/` 测试（违反零改动纪律）
- 2017 旧引擎停止 / 调用方切换（T8）：均待老板放行
- P2 登记项（不改历史/需决策）：T15 归档 mv 混入 T16 commit `88cf04a`、T8-X 执行体字段解析、executors 备注 QXMAP、cluster DEFAULT_SERVICES 硬编码、server/ W292 预存债

**红线校验**：
- `scripts/` 零改动 ✓（重构区间实测 0 次）
- 三扫描（S1–S4 + 明文密钥 + 外脑依赖）零命中 ✓
- 运行面零接触（未部署/未迁移/未启动/未注册）✓
- 未删除任何文件（仅编辑/新增）✓
- 未读外脑（qx-map/HP），契约 L4 表由任务卡内联引用重建并注明来源 ✓
- 工作树仅剩 2 个预存无关改动（`decided.json`、`_update_handoff.py`），未带入提交 ✓

## 验收区

**合入批准** · 日期：2026-08-02
- 判定：✅ 通过

## 机审区

**机审：通过**
- 说明：历史卡，无存档证据，按看板已关闭态标注

## 维护区

> 完成钩子（Doc-Gate）：回写时必须逐项勾选填写，禁止留占位。缺失/占位 = 机审打回 + 合入拒绝。

1. **方案同步**：`关联方案` 状态/关联卡是否已同步？[否]
   - 说明：历史卡，无需额外同步方案状态。
2. **教训沉淀**：本卡是否产出可复用教训？[无]
   - 说明：历史归档，未记录额外复用教训。
3. **档案/README**：本卡是否改变了项目结构/技术栈/路径？[否]
   - 说明：历史完成，未改变项目架构。
4. **线路图**：项目近况/下一步是否变化？[否]
   - 说明：历史结束，不涉及线路图更新。
