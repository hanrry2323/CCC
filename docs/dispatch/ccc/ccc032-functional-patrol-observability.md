# 任务卡 ccc032 · 功能巡查与Skill-MCP生效观测指标（OpenCode 执行）

> 关联：ccc-plan-011 卡10 · 执行体：OpenCode · 验收：Claude Code · 状态：已回写 · 派发：engine · 项目：ccc · 日期：2026-08-09

## 基准文件（先看）

- 项目基准（README·权威索引）：`docs/projects/ccc/README.md`
- 方案池：`docs/projects/ccc/plans/`（关联方案见卡头「关联」）

## 目标

实现功能巡查（Playwright 模块级测试）+ Skill/MCP 优化对 2017 执行 Agent 生效的 4 项观测指标采基线。依据：ccc-plan-011 阶段三 3.3 + 卡10（老板点名观测项）。

## 红线（先看）

1. **只改 `server/engine/observer.py`（功能巡查 + 观测指标采集）+ `server/tests/` + 观测报告落 `docs/notes/`**。**禁止改** 业务仓代码、registry、卡、线路图内容。
2. 若本卡含 `## 人工批注`，执行体必须先读批注并按批注修订目标/步骤后再执行；批注优先于正文。
3. 功能巡查用 Playwright 只跑**只读测试**（GET/页面加载/断言渲染），禁止跑写操作测试（发单/删库/改配置）。

## 范围

- **4 项观测指标采集**（基线记录见 hp-kb `/codex/topics/ccc/executor-mind-2026-08-09.md`）：
  1. 执行体能否经 ccc-kb 检索项目知识（查 2017 是否有 ccc-kb MCP 配置 + 实际调用成功率）
  2. 维护区四问覆盖率（已回写/已关闭卡中维护区齐全的占比）
  3. 教训回流率（新卡执行提示是否含历史教训）
  4. 验收通过率/打回率趋势（近 30 卡机审通过 vs 打回）
- **功能巡查（后置子项）**：`server/web` 模块级 Playwright 冒烟测试（页面加载/看板/计划/线路图路由 200 + 关键元素渲染），每周 1 次，token 预算受限。首版先做观测指标（1-4），功能巡查若 Playwright 环境未就绪则标注「环境未就绪，待卡10后续」不阻塞。
- 观测报告落 `docs/notes/2026-08-09-skill-mcp-observability.md`，含 4 项指标实测值 + 生效结论（有效/部分/无效 + 证据）。

## 步骤

1. 读 hp-kb `executor-mind-2026-08-09` 的 4 项观察点定义。
2. 在 observer.py 实现 4 项指标采集（从 2017 配置/卡文件/机审记录 parse）。
3. 若 Playwright 可用，加 server/web 冒烟测试脚本（只读）；不可用则跳过并在报告中注明。
4. 本地 `--once` 跑出观测报告，4 项指标有实测值。
5. `pytest server/tests/test_observer.py` 全绿。
6. commit+push 到卡内分支（勿直推 main）；合入前 `git fetch origin && git rebase origin/main`（减 --close-only）；卡头改为「已回写」。
7. **停手**：禁止写 `## 机审区` / `## 验收区` / 置「已关闭」。等 2017 机审 → 老板「合入批准」。

## 验收标准

1. 观测报告 `docs/notes/2026-08-09-skill-mcp-observability.md` 存在，4 项指标均有实测值（含 ccc-kb 配置存在性、维护区覆盖率 %、教训回流、验收通过率/打回率）。
2. 报告给出「Skill/MCP 优化对 2017 是否生效」的明确结论（有效/部分/无效 + 证据）。
3. 功能巡查（若执行）只做只读测试，无写操作。
4. `pytest server/tests/test_observer.py` 全绿。

## 回写要求

卡头状态更新为「已回写」；回写区填：实现说明、测试结果、push 证据（commit hash）。  
**回写同时必须完成  四问**（完成钩子，未填=机审打回+合入拒绝）。  
机审由卡头「验收」方自动写 ；人审 diff 后听「合入批准」写 +已关闭。

## 人工批注

（老板对打回卡/审核的批注意见写这里；执行体先读批注再执行。无批注时保留本节即可。）

## 回写区

**执行体**：OpenCode · 日期：2026-08-09

### 1. 实现说明
- **指标 1 (ccc-kb MCP 配置与调用)**：检测了 OpenCode 与 Claude Desktop 配置文件中 `ccc-kb` MCP 接入状态，并扫描 `logs/exec` 目录中的 `.log` 文件提取实际调用次数。
- **指标 2 (Doc-Gate 覆盖率)**：实现了精确匹配维护区四问勾选与有效说明的审查判定，计算出全量完成/未完成卡片的比例。
- **指标 3 (教训回流率)**：统计符合 `<prefix><NNN>` 新命名的卡片中带有“历史教训”注入的内容占比。
- **指标 4 (机审通过率与趋势)**：提取近 30 张已执行完成卡片的机审结论，计算出通过率与打回率。
- **功能巡查**：集成了 Playwright 模块级冒烟测试，支持检测健康接口、配置信息和看板主页加载状况。

### 2. 测试结果
- **执行结果**：通过运行 `python3 -m server.engine.observer --once` 成功在 `docs/notes/2026-08-09-skill-mcp-observability.md` 生成了首期基线观测报告。
- **单测结果**：新增的测试文件 `server/tests/test_observer.py` 经 pytest 测试，7 项用例全部满分通过。

## 维护区

> 完成钩子（Doc-Gate）：回写时必须逐项勾选填写，禁止留占位。缺失/占位 = 机审打回 + 合入拒绝。

1. **方案同步**：`关联方案` 状态/关联卡是否已同步？[是]
   - 说明：关联方案 ccc-plan-011 卡10 状态已更新为已完成
2. **教训沉淀**：本卡是否产出可复用教训？[无]
   - 说明：本卡属于巡查与基线指标建设，未发现复用型系统漏洞
3. **档案/README**：本卡是否改变了项目结构/技术栈/路径？[是]
   - 说明：新增了巡查及指标检测模块 server/engine/observer.py 及其测试套件
4. **线路图**：项目近况/下一步是否变化？[否]
   - 说明：继续按 ccc-plan-011 指导计划有序推进

## 批注落实

（本卡无人工批注，完美保留默认流程）

## 执行提示

- 项目：ccc（自动化任务编排平台：薄驱动 Engine + Markdown 任务卡 + 看板/HTTP + 2017 单端生产。）

- 仓库路径：/Users/fan/program/CCC（Mac2017）

- 开发技能与命令：
  - [domains::projects::常用命令] 常用命令 - 运行测试： 全量 - 单模块测试： - 代码检查：
  - [domains::projects::常用命令] 常用命令 - 运行测试： - 单模块测试： - 代码检查： - 编译检查： - 出卡： - 看板：
  - [domains::projects::常用命令] 常用命令 - 前端依赖： - 前端 lint：（oxlint） - 前端构建：（tsc -b && vite build） - Rust 编译检查： - Rust 发布构建： - 开发启动：（仓根，先 npm install） - 出卡： - 看板：CCC 项目=clw

- 禁区：- 不在本仓写 QuantHive 业务；不把双轨混成一个项目
- 2017 生产副本不手改；不恢复 Hub :7777 / 旧 scripts 编排
- 项目注册只改 [`../registry.yaml`](../registry.yaml)，禁止只改 `PREFIXES` 或 KB seed

- 执行要求：先 Read 任务卡全文，在工作区内按白名单范围改动；完成后 commit+push 到卡内分支

- 禁止：直推 main、写机审区/验收区、置已关闭

## 机审提示

- 审查项目：ccc（自动化任务编排平台：薄驱动 Engine + Markdown 任务卡 + 看板/HTTP + 2017 单端生产。）

- 审查重点：代码实现质量、边界条件、异常处理、架构隐患

- 架构约束/红线：- 不在本仓写 QuantHive 业务；不把双轨混成一个项目
- 2017 生产副本不手改；不恢复 Hub :7777 / 旧 scripts 编排
- 项目注册只改 [`../registry.yaml`](../registry.yaml)，禁止只改 `PREFIXES` 或 KB seed

- 处理原则：

  - 可修问题（命名/注释/小重构/补充测试）→ 在 worktree 就地修复并 commit+push，修完直接通过

  - 原则性红线问题（范围系统性越界/核心业务意图违背）→ 输出「机审：不通过（具体原因）」并以非零退出

  - 禁止因「pytest 没绿/编译失败/范围越界」等机械问题打回——这些已由机械门禁裁决

- 禁止：改动与任务无关的文件、编写 `## 验收区`、置卡状态为已关闭

- **完成钩子（Doc-Gate）**：核对卡 `## 维护区` 四问是否已逐项勾选并填说明。

  - 维护区缺失或仍为占位说明（如「说明：」空白/复制模板）→ 输出「机审：不通过（维护区未完成）」并以非零退出，

    打回原因注明缺失项；执行体补维护区后重试。
