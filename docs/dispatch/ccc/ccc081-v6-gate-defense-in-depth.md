# 任务卡 ccc081 · V6 门禁纵深防御——结论行字形/混合提交/注释勘误（DSH 执行）

> 关联：环节②交接指令(S116-01)卡0 · 执行体：DSH · 验收：DSH · 状态：已回写 · 派发：engine · 项目：ccc · 日期：2026-08-24

## 目标

堵 V6 漂移检查两项 fail-open 与一处注释错误：① approve-merge.sh 结论行 grep 只认单字形「机审：通过」，粗体/变体信封会静默跳过漂移检查；② 信封+代码混合提交可把漂移基准推到自身；③ validate-plans.sh L281 注释字节碰撞描述不准确。

## 红线

- 白名单：scripts/approve-merge.sh、scripts/validate-plans.sh。
- 禁改机审真值判定语义（machine_audit_passed_text 的通过/不通过识别不动）；只加严漂移检查触发条件与信封纯度校验。
- 不动 OPENCODE_GO_API_KEY / engine.lock / 生产 ledger。

## 范围

- approve-merge.sh：pinned 非空而 last_env_commit 为空时显式 ERROR 阻断（或回退钉基线并在输出标注回退）。
- 信封提交单文件校验：合入前核对被钉信封 commit 的改动文件集是否仅含卡文件本身，含代码改动即拒绝并提示拆分。
- validate-plans.sh L281 注释勘误：仅「分」字含 0x88 续字节，删除不准确表述。

## 步骤

1. 读 approve-merge.sh 漂移检查段，定位 pinned/last_env_commit 判定与 grep 字形。
2. 实现两处加严 + 一处注释修正。
3. 自测：合成变体信封（粗体「**机审：通过**」）走漂移检查路径断言不再静默跳过；构造混合提交样例断言被拒；bash -n 全过。

## 验收标准

- [ ] 变体字形信封不再静默跳过漂移检查（测试或探针输出取证）
- [ ] 含代码改动的信封提交被显式拒绝
- [ ] scripts/validate-plans.sh 注释与实际字节事实一致
- [ ] bash -n 通过；既有 approve-merge 相关测试不回归

## 回写要求

- 回写区列改动点、自测命令与输出摘要、每处 diff 要旨。
- 维护区四问如实填写。

## 人工批注

（留空）

## 回写区

**实现说明**（2026-08-25 · DSH 执行体 · 重试轮，修复第三轮机审 P1-F1/F2 后重新回写）：V6 漂移检查段保持函数化 `v6_drift_gate`（approve-merge.sh:389-485，调用点 :537）。本轮按机审处置令做两处单点修复；validate-plans.sh 注释勘误已于上轮合入（ca94bae9a），本轮零改动。机审真值判定语义（server/board/models.py `machine_audit_passed_text`）零 diff。

### 改动点与 diff 要旨（本轮 176bfd7e9，仅 approve-merge.sh +6/-2）

1. **F1 修复·信封定位 grep 改字节字面量交替**（approve-merge.sh:411）：`[：:]` 多字节括号在 LC_ALL=C 下永不命中规范字形（BSD grep 字节语义，最小探针 `printf '机审：通过\n' | LC_ALL=C grep -Ec '^(机审|结论)[：:]通过'` → 0，而交替写法 → 1）→ 改为 `(：|:)` 字面量交替，两种 locale 语义一致；未采用脚本级 LC_ALL 钉（ccc068 先例为备选方向），避免改变门禁其余文本处理行为。
2. **F2 修复·归一化后全行锚定**（同 :411）：原仅 `^` 左锚，行首散文伪信封（「机审：通过标准见…」）可充当信封前推漂移基线（旧实现在 UTF-8 下 rc=0 静默放行，A/B 实证）。现全行锚定 `^(机审|结论)(：|:)通过(（被审[0-9a-f]{7,40}）|\(被审[0-9a-f]{7,40}\))?$`：尾注仅放行 SOP §五（docs/product/machine-audit-sop.md:55）契约形态「机审：通过（被审 <sha12>）」全角/半角两种括号——历史信封即此形态（main 实测存在 `+机审：通过（被审 c9ca1c06e115）`）；散文伪信封不再命中，未命中者落入既有 fail-closed 安全网（分支 tip 有通过文本却定位不到信封 → 显式阻断）而非静默跳过。注释段（:394-402）同步改写与实现对齐。
3. **②信封纯度校验 / ③钉完整性 / ④漂移检查**（:427-483）：上轮已合入，本轮零改动；③ 中「pinned 非空而 last_env_commit 为空 → ERROR」防御层保留。
4. **③注释勘误**（validate-plans.sh L281-283）：ca94bae9a 已合入并经两轮机审复核吻合（「（」=EF BC 88 尾字节 0x88 仅与「分」(E5 88 86) 续字节碰撞，派/关不含 0x88），本轮 python3 复测仍一致。

### 自测命令与输出摘要（harness：/tmp/v6gate-test2.sh，从 HEAD 提取 `v6_drift_gate` 于合成 git 仓执行）

- **locale 前提声明**：本摘要所有数字均在明示的 locale 下取得；9 场景分别在 en_US.UTF-8 与 LC_ALL=C 各跑一遍，无仅单 locale 成立的结论。
- 9 场景 × 双 locale = 18 项全过（PASS=20 FAIL=0 含 A/B 两断言；bash 3.2.57 兼容）：S1 粗体信封+其后代码漂移→拒「机审后漂移」；S2 纯净粗体信封→放行；S3 同 commit 结论行+夹带 scripts/bar.sh→拒「信封纯度校验失败」；S4 clw011 式「结果：通过」变体（真值过、字形不可定位）→ fail-closed 阻断「无法定位任何信封提交」；S5 SOP 主字形「机审：通过（被审 sha12）」+其后纯卡回写→放行（双 locale 关键回归面）；S6 引用式信封+其后漂移→拒；S7 引用式纯净→放行；**S8【P1-F2 负例】真信封E→漂移D→纯卡散文伪信封F→拒且报「机审后漂移」（基线未被前推）**；S9 对照组非字形散文→同样拒。
- **C-locale 运行用例【P1-F1 负例】**：上述 9 场景整体在 LC_ALL=C 下重跑全过——上轮该环境 PASS=2 FAIL=5 的阻断不复现。
- A/B 对照（同一 S8 合成仓、均钉 UTF-8 排除 F1 干扰）：旧实现 ca94bae9a rc=0 静默放行（漏洞实锤）vs 本轮实现 rc=1 拒绝。
- 字形探针（双 locale 等价）：8 种合法形态（裸/粗体/半角冒号/引用式/clw011 加粗拆分/列表项/被审尾注全半角括号）全命中；「不通过」「机审：通过标准见…」「本席认为结论为通过」「结果：通过」全不命中。
- 回归：`bash -n scripts/approve-merge.sh && bash -n scripts/validate-plans.sh` 双过；`bash scripts/tests/test-card-resolve.sh` V7 全过 rc=0；`bash scripts/validate-plans.sh` 全量「全部通过」rc=0（xy052-055 幽灵引用 WARN 为存量既有，与本卡无关）；`bash scripts/approve-merge.sh` 无参 usage 退出码 2（端到端解析正常）。

### 验收标准对照

- [x] 变体字形信封不再静默跳过漂移检查 —— S1/S6/S8 拒绝输出取证 + C-locale 全场景重跑
- [x] 含代码改动的信封提交被显式拒绝 —— S3 信封纯度拒绝取证
- [x] validate-plans.sh 注释与实际字节事实一致 —— 上轮合入，本轮 python3 字节复测一致
- [x] bash -n 通过；既有测试不回归 —— 双脚本 bash -n 过、test-card-resolve 过、validate-plans 全量 rc=0

## 维护区

1. **方案同步**：[否]
   - 说明：[否]。本卡为环节②交接指令(S116-01)直派加固卡，卡头无 prefix-plan-NNN 关联方案。
2. **教训沉淀**：[无]
   - 说明：[无]。教训已随本卡回写区与三轮机审记录固化——门禁以文本形态定位证据时，识别口径须同时满足 locale 不变量（字节字面量而非多字节括号）与契约锚定（全行锚定+仅放行 SOP 契约形态），并配 fail-closed 兜底；无需另沉淀 docs/notes。
3. **档案/README**：[否]
   - 说明：[否]。纯 scripts 内部门禁加严（信封定位 grep 单点）与注释勘误，未改对外命令用法、配置项或文档口径。
4. **线路图**：[否]
   - 说明：[否]。既有 V6 门禁的纵深防御收口（机审打回修复轮），不构成新里程。

## 机审区

> 并审合并说明（2026-08-25）：本卡同日由两个独立机审实例先后完成审查——实例A（commit 414b0b91，severity=中·不通过）与本席（实例B，独立从零复现）。两实例对 P1-F1 结论一致；实例B 将 A 列为「理论残余面」的伪信封基线前推做了端到端实证（可复现放行），并据双 P1 并存将 severity 上调为重。以下为合并后的唯一权威机审节：A 的关键证据均经本席亲跑复核属实，补充复现以 B 为准。若后续另有机审轮次，请以本节为基准追加，勿并行双写。

**DSH 机审席 · 2026-08-25 · severity：重**

### 范围核对（通过）

提交 ca94bae9a 仅触 scripts/approve-merge.sh、scripts/validate-plans.sh 与本卡回写，白名单内无越界；server/board/models.py 真值函数零 diff（机审真值语义确未动）；validate-plans.sh 仅注释改动（-2/+3），勘误字节事实经本席 python3 实测吻合：（=EF BC 88 尾字节 0x88，分=E5 88 86 含 0x88 续字节，派/关不含 0x88。

### 认可面（逐项复核成立）

- 归一化管线方向正确：UTF-8 locale 下粗体与 engine 引用式变体信封均被识别（两实例探针各自独立复现一致）；其后代码改动判漂移硬拒（合成仓实测 rc=1 且指向正确信封 sha）。
- 信封纯度校验成立：同 commit 结论行+夹带 scripts/bar.sh → 显式拒绝（T3 复现）；多轮审计语义不变（env_commits[0] 取最新，等价旧 break-first）。
- bash -n 双过、test-card-resolve V7 全过、validate-plans 全量 rc=0（本席亲跑）。
- 维护区四问均单选已填、说明实情且抽查属实（方案同步[否]/教训沉淀[无]/档案[否]/线路图[否]，引用工件存在性已核实）。

### P1-F1（本卡新引入回归 · 两实例结论一致 · 已复现）

信封定位最终 grep 用多字节括号表达式匹配全角冒号：LC_ALL=C 下本机 BSD grep 永不命中，连纯文本规范字形都漏认；而旧模式（ASCII 类+字面量）同环境正常——识别口径在其宣称加严的维度发生环境敏感回归。
证据（全部本席亲跑）：
- `bash /tmp/v6gate-test.sh` 默认 C locale → **PASS=2 FAIL=5**：T1/T2/T3/T5/T6 被「无法定位任何信封提交」误拦，含 T5（规范信封放行的回归场景）；`LC_ALL=en_US.UTF-8` → PASS=7 FAIL=0——回写声明的七场景全过仅在 UTF-8 locale 成立；
- 最小复现：`printf '机审：通过\n' | LC_ALL=C grep -Ec '^(机审|结论)[：:]通过'` → 0；交替写法 `'^(机审|结论)(：|:)通过'` → 1；
- 仓内先例：validate-plans.sh:25-34 因同类字节/locale 事故已硬钉 LC_ALL（ccc068，8.2 漏判复活），本卡未沿用该既定模式且 approve-merge.sh 未钉 locale；
- 自动化路径真实存在（server/config/merge-executor-instruction.md），launchd 环境 LANG 为空——任何未导出 locale 的运行即触发整条合入链路不可用（fail-closed 方向安全，但属门禁整体可用性事故）。
修复方向：括号类改字节字面量交替（两种 locale 语义一致），或按 ccc068 先例在脚本头钉 LC_ALL（en_US.UTF-8 缺失退 C.UTF-8）。

### P1-F2（目标②未闭合 · 实例A 列理论面、实例B 升级为实证绕过）

信封识别仅行首锚定、无行尾锚定：纯卡提交中行首为结论类字样紧贴冒号加通过字样的散文行即可充当伪信封、前推漂移基线。
实证（合成仓三连提交，UTF-8 locale）：E=纯卡规范信封 → D=白名单内代码漂移 → F=纯卡散文伪信封（反例句式置于行首）→ 当前实现返回 0 放行、诊断 baseline=F，D 完全逃过漂移 diff；对照版改行尾锚定后返回 1、正确抓 E 后漂移。危害边界：scope-check 限缩至白名单文件，而门禁加固卡的白名单恰是 gate 脚本自身（最有价值攻击面）；ledger 按 id 记录不钉 commit（A 实例核实 provenance 门禁位于 :641-655），无法覆盖审计后的时序漂移。备注：机审前缀散文的同类弱点在旧实现已存在，但本卡目标②明言堵「基线推到自身」，故按目标完成度缺口计入。
修复方向：归一化后全行锚定（看板契约规定结论行为整行裸字形，与契约一致）；未命中者落入既有 fail-closed 安全网而非静默放行。

### 轻微项 / F3（记录不单独计分）

- rename 边缘：`git show --name-only` 对重命名显示 old-to-new 花括号格式可能误拒（fail-closed 方向，罕见）；merge commit combined-diff 盲区同理一并记录。
- 回写区引用行号 :385-479/:531 与实际 389-481/:533 有 ±2 偏差（函数名唯一，不影响定位复现）。
- 自测摘要缺 locale 前提标注：「PASS=7 FAIL=0」等仅在 UTF-8 成立，证据完整性存瑕疵（非造假，UTF-8 下可复现）。

### severity 判定

影响面 3（F1 使非 UTF-8 运行环境合入链路全量阻断 + F2 为 V6 威胁模型内可复现的静默绕过，二者均作用于每张卡的唯一合入门禁）＋ 改动深度 2（核心门禁段函数化重构）＋ 红线邻近 2（真值语义邻域，零改动有实证）＝ 7 分，且影响面维度高，按规则强制重。

### 维护区四问核对

四问均单选已填、说明非占位且抽查属实；唯自测输出缺 locale 前提标注（见轻微项）。

处置：重级打回引擎——按 F1/F2 各一处单行修复，harness 补两类负例（C-locale 运行用例、伪信封基线前推用例）后重新回写送审。本席不代改门禁语义。

机审：不通过（severity=重：P1-F1 多字节括号在 C locale 漏认规范信封字形致合入链路全量阻断——PASS=2 FAIL=5 实证、ccc068 钉 locale 先例未沿用；P1-F2 无右锚伪信封前推漂移基线实证绕过目标②——RC=0 可复现；修复后重新机审）

---

### 第三轮复核（实例C · 独立从零复现 · 本节为当前有效结论，上文两轮为历史记录）

**DSH 机审席 · 2026-08-25 · severity：重**

前置状态核实：HEAD d160509b0 与 origin/codex/ccc081-v6-gate-defense-in-depth 一致，自上轮「不通过」以来零新提交，代码仍停在 ca94bae9a；本轮全部判定基于该未变状态。

范围与认可面复核（本席亲跑，与前轮一致）：ca94bae9a 仅触卡文件＋approve-merge.sh＋validate-plans.sh（models.py 零 diff）；validate-plans.sh 勘误注释字节事实本席 python3 复测吻合（（=EF BC 88 尾字节 0x88、分=E5 88 86 含 0x88、派/关不含）；bash -n 双过；test-card-resolve V7 全过 rc=0；validate-plans 全量 rc=0 全部通过；合成仓复验信封纯度对混合提交仍正确拒绝、UTF-8 下规范信封+漂移正确抓拒。维护区四问格式与真实性核对维持成立。

**P1-F1 复现成立，且确证为本卡新引入回归**：
- 最小探针：`printf '机审：通过\n' | LC_ALL=C grep -Ec '^(机审|结论)[：:]通过'` → 0 命中；交替写法 `'^(机审|结论)(：|:)通过'` 同环境 → 命中；旧模式 `'^\+[[:space:]]*机审：通过'` 同环境 → 命中——识别口径在其宣称加严的维度发生环境敏感回归实锤；
- 端到端（从真实文件 sed 提取 `v6_drift_gate` 于合成 git 仓）：UTF-8 规范信封E+漂移D → 正确拒（rc=1 漂移报错）；同一仓换 LC_ALL=C → 「无法定位任何信封提交」整段阻断（rc=1，fail-closed 方向、可用性事故）；
- approve-merge.sh 及其 source 的 lib/card-resolve.sh 均无 LC_ALL 钉；姊妹门禁 validate-plans.sh:25-34 对同类字节/locale 事故已硬钉（ccc068 先例），本卡未沿用；
- 引用勘误（不改结论）：上轮「自动化路径真实存在（server/config/merge-executor-instruction.md），launchd 环境 LANG 为空」一句，该文件并不含此表述；locale-less 生产运行的实证依据应为本仓 validate-plans.sh:22-24 记录的 tst005/ccc068 事故原文（「pytest 子进程 UTF-8 绿、launchd 部署 C 红」）。事实成立、引证更正。
- 可移植性备注：本轮实证限于本机 BSD grep（2.6.0-FreeBSD）；Linux GNU grep 在 C locale 下的行为未测，修复方向按两种 locale 语义一致的字面量交替处理即天然免疫该分歧。

**P1-F2 复现成立（静默绕过，目标②未闭合）**：
- 合成仓三连提交（UTF-8）：C0(代码)→E(规范信封，被审钉=C0)→D(白名单内 scripts/bar.sh 漂移)→F(纯卡提交，新增行首散文「机审：通过标准见验收标准一节…」)→ 现实现 **rc=0 放行**，D 完全逃过漂移 diff；
- 对照组：同构提交但散文行不以结论字形开头（「本席认为结论为通过…」）→ rc=1 正确抓 D——证实翻转成因即归一化删行内空白后仅 `^` 左锚定、无整行/右锚定所致的结论 token 前缀误命中；
- 危害边界本席一手复核：ledger provenance 门禁（approve-merge.sh:641-657）按 `has_action('machine_audit_pass', <id>)` 记账、不钉 commit，无法覆盖审计后时序漂移；信封纯度校验拦不住纯卡伪信封；加固卡白名单恰含 gate 脚本自身，为威胁模型内最有价值攻击面。

轻微项（追加记录）：回写区引用行号 :385-479/:531 实为 389-481/:533（±2 偏差，函数名唯一不影响定位）；自测摘要缺 locale 前提标注（PASS=7 FAIL=0 仅 UTF-8 成立）。

severity 判定（第三轮）：影响面 3 ＋ 改动深度 2 ＋ 红线邻近 2 ＝ 7 分，影响面维度高 → 强制重，与前两轮一致。

处置：维持重级打回引擎——按 F1（括号类改字节字面量交替，或依 ccc068 先例脚本头硬钉 LC_ALL 并设 C.UTF-8 回退）与 F2（归一化后全行锚定，未命中落既有 fail-closed 安全网而非静默放行）各一处单行级修复；harness 补两类负例（C-locale 运行用例、伪信封基线前推用例）后重新回写送审。本席不代改门禁语义。

机审：不通过（severity=重维持：P1-F1 `[：:]` 多字节括号 C locale 漏认规范信封——探针 0 命中＋端到端阻断实证、旧模式同环境命中＝本卡新回归、ccc068 钉 locale 先例未沿用；P1-F2 无右锚伪信封前推漂移基线 rc=0 静默放行——三连提交＋对照组实证、目标②未闭合；修复后重新机审）
