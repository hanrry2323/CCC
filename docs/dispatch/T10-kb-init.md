# 任务卡 T10 · CCC 知识库初始化 + 种子入库（依赖 T9 · M4 前哨）

> 关联：INT-120（CCC 重构）· 契约：CCC 重构契约 v1（D3 知识 / P5 知识移植 / M4 移交）
> 管理席：Claude Code（调度）· 执行体：Trae（窗口 C）· 验收：Claude Code
> 状态：已关闭 · 日期：2026-08-02
> **依赖：T9（种子包就绪后本卡才可开工）**

## 目标

初始化 **CCC 自建知识库**（结构 + 检索能力），导入 T9 种子包，验证检索可用——M4 独立移交日的最后前哨。

## 红线（先看）

1. 只建 CCC 仓内知识库（如 `knowledge/` 或复用 `server/` 下方案）；**零依赖外脑**（运行时不再读 qx-map / hp-kb）。
2. 不碰运行面；不读不写外脑（除 T9 种子包）；完成必须提交。
3. 验收标准不可自行解释。

## 范围

- 新建：CCC 知识库目录 + 结构（节点/路径/项目/决策/教训 分域）。
- 导入：T9 种子包。
- 检索：基础检索能力（文件结构检索即可，M4 后可演进向量）。
- 不动：外脑、业务代码、运行面。

## 步骤

1. 建 CCC 知识库目录结构（分域）。
2. 导入 T9 种子包（节点/路径/项目元数据/决策/教训）。
3. 验证检索：按域能定位种子条目（列出 / 关键词）。
4. 写 `README.md`：知识库用法 + 维护规则（以后 CCC 决策/教训只写本库，不写外脑）。
5. 提交 `feat(kb):`，回写真实 commit hash。

## 验收标准

1. 知识库结构清晰、种子全入库（四类条目可检索到）。
2. README 明确「CCC 知识独立维护」规则。
3. 零外脑依赖（代码/文档不含 qx-map 读写）；提交真实。

## 回写要求

结果摘要、知识库结构、检索验证输出、commit hash、验收自检对照表。**状态同步（§3）**。

## 回写区

（Trae-C 回写）

## 验收通过（Claude Code · 2026-08-02）

- 独立复核：`knowledge/` 结构齐全（README + 检索脚本 + 4 域 seed.md + seed/ 快照）；检索 3 例实证通过（4100→nodes-paths / 独立→decisions / --list lessons 13 条）；外脑 qx-map 零改动；commit `4b79f98`
- 纪律更正：Trae 未写回写区 + 未同步卡头状态（§3），验收席代补「已关闭」
- 意义：**P5 知识移植完成，M4 独立移交日前置条件就绪**
