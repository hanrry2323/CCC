# 任务卡 T9 · P5 知识移植种子准备（Trae 窗口 C）

> 关联：INT-120（CCC 重构）· 契约：CCC 重构契约 v1（D3 知识 / P5 知识移植）
> 管理席：Claude Code（调度）· 执行体：Trae（窗口 C）· 验收：Claude Code
> 状态：已关闭 · 日期：2026-08-02
> 前置：P4（T7）已完成；输出供 T10（CCC 知识库初始化）消费

## 目标

打包 CCC 独立运行所需的**知识种子**：节点 / 路径 / 项目元数据 / 决策 / 教训。从外脑权威源（qx-map）提炼为**结构化种子包**，供 T10 一次性移植进 CCC 自建知识库（D3：成熟后一次性移植，之后独立运行零外脑依赖）。

## 红线（先看）

1. **只读外脑**：从 qx-map 权威源（`cluster/path-authority.md`、`cluster/cluster.json`、`command-post/intents.md`、`__archive__/decisions/`、hp-kb `/codex/topics/`）提炼，**不修改任何外脑文件**。
2. 种子包落地到 **CCC 仓** `docs/kb-seed/`（可提交）；不含密钥、不含运行面敏感信息。
3. 完成必须提交；验收标准不可自行解释。

## 范围

- 产出：`docs/kb-seed/` 下结构化种子文件（JSON/Markdown）——节点清单、路径权威、项目元数据、关键决策摘要、教训清单。
- 消费方：T10（CCC 知识库初始化 + 入库）。
- 不动：外脑任何文件、CCC 业务代码、运行面。

## 步骤

1. 提炼**节点/路径**：从 `path-authority.md` + `cluster.json`（机器、IP、SSH、路径、服务）。
2. 提炼**项目元数据**：CCC / qb / QuantHive 等（位置、性质、访问方式）。
3. 提炼**关键决策**：`__archive__/decisions/` + hp-kb（每条：标题 / 日期 / 决策 / 状态）。
4. 提炼**教训**：hp-kb `/codex/topics/` 教训类（踩坑 / 经验 / 红线）。
5. 组织为 `docs/kb-seed/` 种子包（结构清晰、可导入）。
6. 提交 `docs(kb-seed):`，回写真实 commit hash。

## 验收标准

1. 种子包四类齐全（节点/路径 / 项目元数据 / 决策 / 教训）。
2. 不含密钥 / 运行面敏感信息；来源可追溯（每类标注权威源）。
3. 外脑零改动（git status 证明）；提交真实。

## 回写要求

结果摘要、种子包结构、来源清单、commit hash、验收自检对照表。**状态同步（§3）**。

## 回写区

（Trae-C 回写）

## 验收通过（Claude Code · 2026-08-02）

- 独立复核：种子包四类齐（节点/项目元数据/决策 6/教训 3 类）；外脑 qx-map 零改动；commit `b423be5`
- 纪律更正：Trae 未写回写区 + 未同步卡头状态（§3），验收席代补「已关闭」
