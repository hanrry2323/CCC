# 任务卡 T99-flow-test · 自动化流程跑通验证（占位任务）

> 关联：阶段 3 流程验证 · 执行体：Claude Code · 验收：Codex · 状态：待分派 · 日期：2026-08-04

## 目标

验证「Codex 出卡 → 2017 pull → Engine 自动派发 → 2017 执行体执行 → 收单回写 → 看板可见」全链路。本卡为占位任务，不写业务代码。

## 红线

1. 只做占位动作：在 `/tmp/ccc-flow-test.txt` 写入一行 `flow-ok`，不回写其他文件、不修改仓库任何代码。
2. 完成后输出确认文本并正常退出（exit 0）。
3. 本卡为临时测试卡，验收后删除，看板无残留。

## 验收标准

1. 卡头状态：待分派 → 执行中 → 已回写（Engine 自动派发，非人工改卡）。
2. 执行日志存在：`/tmp/ccc-flow-test.txt` 内容为 `flow-ok`。
3. 看板可见：/board/states 出现本卡状态流转；验收后删除，/board 无残留。

## 回写区

**执行体**：Claude Code（2017）· 日期：
