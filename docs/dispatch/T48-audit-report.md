# T48 问题审计报告（Codex 补做 · 2026-08-05）

> 方式：生产端（2017 :7788）API 级复验 + 无头 Chrome 逐视图走查 + 并发连接实测。
> 结论先行：**当前最大问题是「页面刷新即可能白屏」——静态资源并发加载被 ERR_CONNECTION_RESET 打断，SPA 初始化中断。** 其余为文案/口径/降级类问题。

## 走查记录

- API 级：`verify-shell.sh` 六场景全 PASS（免登录/项目/零 5xx/流式/思考折叠/断流）——API 层健康。
- 浏览器级（无头 Chrome 1440×900）：多次加载中 2/3 出现 `js/state.js`、`css/variables.css`、`js/components/runtimeStatus.js` 等 ERR_CONNECTION_RESET；失败时 `#hub-views` 无内容、composer 不可见（rect 0,0）、视图只剩导航骨架。
- 并发实测：M1→2017 :7788 并发 100 请求，**41 次 ConnectionReset（41%）**；2017 本机 127.0.0.1 并发 100 → 98 OK；2017 本机走局域网 IP 并发 100 → 93 OK / 7 失败；M1→2017 :22（TCP 层）并发 100 → 100 OK。

## 问题清单（按证据分级）

| # | 级别 | 现象（用户视角） | 复现/证据 | 修复方向 |
|---|------|------------------|-----------|----------|
| 1 | **P0** | 刷新页面经常只剩导航栏，对话/看板/控制台全是空的 | 无头走查多次加载中 state.js 等资源 ERR_CONNECTION_RESET；M1→2017 并发 41% reset | 服务端：诊断 macOS listen backlog/somaxconn、ThreadingHTTPServer 线程与连接管理；前端：关键 JS 内联/合并 + 资源加载失败自动重试；M1 侧：验证关闭防火墙是否消除（ALF 开启中） |
| 2 | **P1** | SPA 初始化失败时无任何提示/重试入口，静默白屏 | state.js 失败后页面仅导航骨架，无错误横幅 | 前端加「资源加载失败，点击重试」降级层；关键脚本失败自动 reload 一次 |
| 3 | **P1** | 免登录模式下标题栏仍显示「未登录」+「退出」按钮 | 正常加载后标题栏文案恒为「未登录」 | auth.js:156 按 auth_required=false 显示「免登录模式」，隐藏「退出」 |
| 4 | **P2** | 对话大脑回答「77 张任务卡」，validate/看板为 76 张 | 实测对话返回 77；validate 输出 76 | 统一统计口径（大脑数法含历史 R 变体或 T-mapping，需对齐） |
| 5 | **P2** | 看板/控制台/右栏卡流全部依赖单一 app 初始化链，资源一断全空 | 与 #1 同源的观察项 | 与 #1/#2 一并修复（前端韧性） |
| 6 | **P2** | 无头 375px 视口无横向溢出（通过项，记录为对照基线） | 走查 `mobile_375_overflow=false` | 无需修 |
| 7 | **P3** | 桌面端与 HTTP 壳共享 #1 的并发拉取风险（桌面拉 /projects /board 等也会偶发失败） | 同源网络层问题 | 桌面端 API 客户端加重试/退避（现有部分场景已有） |
| 8 | **P3** | Nginx 统一入口模板/脚本已就绪但 2017 实际未安装（Homebrew formula DSL 报错） | T63 记录 | 后续修 brew 环境或换安装法 |

## 说明

- 本轮为高置信度补审（核心 P0 已抓到）；完整 7 维度 ≥15 条走查（文案/交互/视觉/性能逐项）随 T50 整体联调执行。
- 本卡不改代码；清单转入整体联调卡修复。
