# 交接给 M1 · qx-map 权威文档两环节模型对齐（2026-08-23）

> 提交方：S116-01 · 2017 Claude Code（CCC 管理席）
> 接收方：M1（qx-map 单写者，本机只 pull）
> 目的：老板 2026-08-23 定调「人审两环节」模型，qx-map 权威文档仍为旧口径（三环节/出卡裁决①/DSH 不参与派发）。按单写者纪律本机不改 qx-map，本稿供 M1 落盘。

---

## 一、情况说明

**背景**：CCC 人审环节从三环节合并为**两环节**（2026-08-22 修订，2026-08-23 老板精确化定调）：

- **环节①（老板 + DSH）**：理解意图 → 方案/规划 → 里程碑 → 出卡/制卡 → 开发 → 自测 → 机审 → **已回写**。Claude Code **不参与**。
- **环节②（Claude Code = 2017 管理席）**：**已回写之后** 验收 → 合入 → 提交 → push → 部署（部署须老板确认）。守审核线，有问题打回 DSH。
- **审核红线（硬）**：审核时**绝不信 DSH 执行过程**，只认独立核验证据（git log/diff、卡文件、机审区、维护区四问）。

**现状**：qx-map 三份权威文档仍描述旧口径（三环节、出卡裁决①、DSH 不参与 AUTO 派发），与上述定调矛盾。**2017 执行侧已全部对齐**（`~/.claude/CLAUDE.md` + CCC 仓 `CLAUDE.md`/`AGENTS.md` 已改并推送 commit `84d35a404`）；**qx-map 侧待 M1 落盘**。

**依据**：老板 2026-08-23 定调；`ccc-dsh整合决策定稿-2026-08-22.md` C 组 #6/#7（DSH 执行模型、拆解→开发→自测→机审全进 DSH）；CCC `server/config/executors.json`（DSH 三角色分类=可后台 CLI → AUTO）。

---

## 二、改动清单（3 处必改 + 1 处建议）

### 必改 1 · `qx-map/seats/S116-01-mind.md`（2017 管理席角色心智 · 最核心）

| 位置 | 现状（旧口径） | 应改为 |
|------|----------------|--------|
| 第 10 行 执行链 | `老板意图 →【出卡①·你】→ 真实看板 → Engine → DSH → 待合入 →【审核合入②·你】→ 部署` | `老板意图 →【环节① 老板+DSH：方案→出卡→开发→自测→机审→已回写】→【交接：已回写】→【环节② 你：验收→合入→提交→push→部署（老板确认）】` |
| 第 12-16 行 三个角色 | ①**出卡裁决①**（老板意图→出卡 `dsh-card-maker.sh`→卡进看板）；②审核合入②；③DSH 集群管理 | 改为**两个角色**：①**环节② 审核合入**（已回写后收卡→机审门禁→老板确认→approve-merge→commit+push→部署问老板）；②**DSH 集群管理**（看板+API 轨迹）。**删除「出卡裁决」**——出卡/制卡归环节① DSH 流程，本席不制卡 |
| 第 29 行 红线 | `只做三个角色动作` | `只做环节② + DSH 管理两个角色动作；出卡/方案/开发/机审归环节①，不参与` |
| 第 22-24 行 管理方法论 | 保留（查会话列表/执行轨迹/参考脚本） | **补一句**：DSH API 轨迹只用于**发现问题/监督进度**，不用于证明「完成/通过」 |
| 新增红线 | （无） | **审核红线（硬）**：绝不信 DSH 执行过程，只认独立核验证据 |

### 必改 2 · `qx-map/AGENTS.md`（DSH 行 · 一行）

| 位置 | 现状（旧口径） | 应改为 |
|------|----------------|--------|
| 第 20 行 IDE 工具表 DSH 行 | `DSH … **审计席 / 只读取证 / 运维**：M1 + 2017，人工触发，不参与 AUTO 派发` | `DSH … **执行体 / 机审 / 运维**：M1 + 2017，**参与 AUTO 派发**（2026-08-22 决策：开发/维护/验收三角色，分类=可后台 CLI，见 CCC `server/config/executors.json`）` |

> 依据：executors.json 现行态 DSH 三角色均已 AUTO；AGENTS.md 此行为滞后残留。

### 必改 3 · `qx-map/ide/tool-roles.md`（工具绑定 · 08-22 工具收口未同步）

| 位置 | 现状（旧口径） | 应改为 |
|------|----------------|--------|
| 第 16-20 行 角色绑定表（开发/维护/合入/审核执行体） | OpenCode、Codex、Claude Code 混绑 | 按 **2026-08-22 工具收口**（只用 Claude Code + DSH）：开发/维护/合入执行体 → **DSH**；审核执行体 → DSH 机审 + Claude Code 环节②终审 |
| 第 53 行 Codex 行 | 活跃 · 大脑·管理席 | 标注已禁用/停用（2026-08-22 工具收口，物理卸载待老板） |
| 第 57 行 OpenCode 行 | 活跃 · 开发执行体/合入执行体 | 标注已从 CCC 执行体移除（2026-08-22），仅 M1 备用终端 |

> 依据：`AGENTS.md` 第 15 行「2026-08-22 工具收口红线」已生效，tool-roles.md 未同步。

### 建议 4 · `qx-map/CLAUDE.md`（M1 自身入口 · 可选）

| 位置 | 现状 | 应改为 |
|------|------|--------|
| 第 2 行 我是谁（纯外脑） | 已正确（2026-08-22 定调） | 不动 |
| 「执行席/验收席强制分离」段 | 仍写「终验归 Codex（管理席）」「审核执行体 = Codex、Claude Code 协作」 | 若工具收口后 Codex 已停用，此段应去掉 Codex 或标历史 |

> 此条属 M1 自身工具描述，M1 可自行评估是否需同步。

---

## 三、2017 已完成的改动（供 M1 知悉，无需处理）

- `~/.claude/CLAUDE.md`：角色行 + 审核红线进纪律。
- CCC 仓 `CLAUDE.md` / `AGENTS.md`：双模式表、审核合入段、中枢出卡段对齐两环节；`check-entry-docs.py` 门禁通过。commit `84d35a404`。

## 四、核验建议（M1 落盘后）

1. `git pull --ff-only`（2017 侧确认同步）。
2. 复核 S116-01-mind.md 执行链已无「出卡裁决①」残留（`grep 出卡裁决 seats/S116-01-mind.md` 应为空）。
3. 复核 AGENTS.md DSH 行已含「参与 AUTO 派发」。
