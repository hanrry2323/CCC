# M8 方案（已定稿 v2 · 2026-08-08）

> 北星不变。冻结心智补丁。v2：基线由 300 卡压缩为 100 卡（已发现架构性问题，长跑无意义）。
> 决策主档：qx-map `__archive__/decisions/ccc-loop-engineering-评估与100卡基线计划-2026-08-08.md`。

## 目标

**8 月 8 日 0:00 起**累计跑完 **100 张开发卡**（业务线 xy/hp/mx + 平台 ccc），
50 卡验收点做一次架构性修正，后 50 卡验证效果；100 卡后用数据排四大升级项
（目标驱动闭环 / 自动任务发现 / 技能封装 / 循环健康指标）的优化顺序。预计 2–3 天闭环。

## 范围

1. **继续跑卡不换链**：现有 2017 固定交叉（OpenCode 开发 → Claude Code 机审）与 M1 自验收双轨保持不变。
2. **50/50 分段**：0–50 卡跑基线（每 10 卡轻量快照，只修流程硬伤）；**第 50 卡 = 验收点**，按已记录架构性问题清单做一轮架构修正（修正完成才进第 2 段）；51–100 卡验证；第 100 卡出下一程优化决策。
3. **埋点看板化**：每卡时长/调用/打回率/机审延迟从 metrics.json 出可视图（`server/web/exec_metrics.py` 数据已就绪）。
4. **参考库补录**：loop-engineering 社区 6 月后资料（Claude 4 循环 / Graph 转向 / SlopCodeBench / Memory Governance）入 HP 参考库。

## 验收

| 指标 | 门槛 |
|------|------|
| 累计跑卡 | 8/8 0:00 起 100 张（含各业务线），2–3 天闭环 |
| 指标完整 | 每卡 metrics 存在、可汇总（时长/工具调用/模型头） |
| 检查点 | 第 50 卡架构修正留痕（前 50=基线 / 后 50=验证对比） |
| 升级顺序 | 100 卡后产出「下一程优化决策」，四大项排定优先级 |
| 新 SOP | 0 |

## 不做

新席位/同义句/AGENTS 长禁令；第二套拆卡 LLM；改 2017 开发-机审固定链路；300/500 卡目标（除非修正后数据仍显著波动再议）。
