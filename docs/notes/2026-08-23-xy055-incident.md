# 决策档 · xy055 机审断裂事故记录（ledger 有记录但卡机审区占位仍合入）

> 日期：2026-08-23 · 工具：Claude Code（S116-01@2017 环节② 审核合入席）· 性质：机审真值链断裂复盘
> 关联：ccc-plan-033（人审两环节修订）、042（质量与真伪机械验证）、P0-2（ledger 真值来源自证）
> 范围：xy055 单卡（2026-08-21 合入）

## 一、事故定性

**xy055 机审 ledger 有 `machine_audit_pass` 记录（engine-audit，08-20T18:51Z），但 main 分支卡的 `## 机审区` 是占位文本「（机审方填写）」，仍于 08-21 被合入（commit `6188bd929`）。** 这是机审真值链的**已知断裂**：ledger 与卡文件两个证据源不一致时，当时的合入门禁未拦截。

## 二、事实与证据

### 2.1 ledger 记录（有）

`data/audit/ledger.jsonl`：
```
{"ts": "2026-08-20T18:51:32Z", "action": "machine_audit_pass", "object_id": "xy055", "source": "engine-audit", "detail": "engine 机审通过（work=xy055）", "kind": "approval"}
```

### 2.2 main 卡机审区（占位，不是真机审结论）

main `docs/dispatch/xy/xy055-workflow-visualization.md` `## 机审区`：
```
## 机审区

（机审方填写）
```

### 2.3 合入事实（已合入）

- 合入 commit：`6188bd929`（merge: 合入批准 xy055，2026-08-21）
- 分支信封：`origin/codex/xy055-workflow-visualization` 已删（合入后清理）
- 卡状态：已关闭

## 三、根因（08-22 硬化前）

1. **当时的机审证据检查允许「分支信封含通过」即放行**：`approve-merge.sh` 机审证据逻辑（08-22 前的版本）只要 `git show origin/<branch>:<卡>` 含「机审：通过」文本即 `audit_ok=true`。分支信封中机审区是否被写、ledger 是否落账，不构成硬约束。
2. **ledger 落账与卡文不同步**：engine 机审落账 `machine_audit_pass` 在 08-20，但 main 卡机审区仍是占位——说明机审区文本没被回写，或回写后又被覆盖。
3. **08-22 硬化已封堵此路径**：`approve-merge.sh` 现要求（a）账本有 `machine_audit_pass` 记录（卡文自写不算真值，P0-3 单源化）；（b）分支信封机审区必须含「机审：通过」且与分支绑定；（c）机审后漂移硬拒绝（--close-only 不放行）。

## 四、影响

- **单个历史断裂点**：xy055 是 08-16 ledger 能力后、08-22 硬化前窗口期的断裂——ledger 有记录但卡文机审区缺失。功能代码本身已合入并关闭，未发现实际业务影响。
- **审计口径**：本次事故记录用于**标记该卡为「历史断裂」，避免后续审计误判为「当前机制仍在放行此类情况」**。

## 五、处置结论

- **不追溯修复**：该断裂发生在 08-22 硬化之前，已被现有机制（账本单源化 + 信封证据 + 漂移硬拒绝）堵住，不再追溯修改 xy055 卡或补机审区。
- **证据固化**：本记录 + P0 排查报告（`docs/notes/2026-08-23-p0-audit-gap-report.md` §2.2 抽查表 xy055 行）作为「已知断裂、已堵住」的存档证据。

## 六、证据复现命令

```bash
# 1. ledger 记录
grep '"xy055"' /Users/fan/program/CCC/data/audit/ledger.jsonl | grep machine_audit_pass
# 2. main 卡机审区（当前仍为占位）
sed -n '/## 机审区/,/## 执行提示/p' /Users/fan/program/CCC/docs/dispatch/xy/xy055-workflow-visualization.md
# 3. 合入 commit
git -C /Users/fan/program/CCC show --stat 6188bd929
```
