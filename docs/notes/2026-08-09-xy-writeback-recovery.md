# 复盘 · xy 空回写卡恢复（2026-08-09）

> 作者：CCC 中枢 · 日期：2026-08-09
> 属性：**临时复盘记录（7 天内进权威或归档）** · 涉及项目：xy（xianyu 业务驱动卡）

## 一、背景

对 xy 项目全部 30 张 dispatch 卡逐一核实，发现 13 张卡已 `merge: 合入批准` 关闭，但**卡文件回写区为空**（只有 `**执行体**：OpenCode · 日期：` 空壳）。

## 二、根因（已代码级定性）

- 这 13 张卡的「合入批准」提交全部是**单父提交**（非真 merge），只在 main 上改写卡头状态为「已关闭」，并未并入分支上已写好的**回写区 + 机审区**内容。
- 完整凭证仍保留在 `origin/codex/xyXXX-*` 分支上，**可恢复**。

## 三、恢复动作（F1/F2 + F3/F5）

| 卡 | 处置 |
|----|------|
| xy016, xy017, xy019, xy020, xy021, xy022, xy023, xy025, xy028, xy029, xy030, xy031 | 从 `origin/codex/<xyXXX-*>` 分支恢复完整回写区 + 机审区（保留 main「已关闭」卡头）；12 张已合并落盘。 |
| **xy006** | 无分支。经 **ssh 只读 2017 业务仓**核验 commit `56a6262` 后重建回写区（机审区 main 已具） |
| **xy003** | main 与分支均无机审区、分支已删 → **无法恢复机审区**，已标注「需机审席补审」 |
| xy012 | 卡头被机审锚点注入错乱 + `2027` 笔误，已修复（回归标准卡头 L3） |
| xy027 | 有 `## 验收区` + 完整回写（升级后新格式，正常，不属问题） |

## 四、遗留待办

1. **xy003**：`## 机审区` 缺失，需 **2017 机审席补审**后补写审计证据（不可从本仓恢复）。
2. **PRM 关卡数据化核验**：xy021-031 卡虽已关闭，但 PRM 五关卡为数据化总闸门（grep 硬编码=0 / pytest 全绿 / 真出片老板满意），**需运维侧独立核验**方判达标；launchd 重建仍在全部达标后另行出卡。

## 五、流程问题记录（需权威层固化）

- **卡级纪律未硬编码**：DOC-PROTOCOL §2.3 无「回写区必填 push 证据/实现/测试」权威条款，仅每卡 `## 回写要求` 节口头要求 → **建议在 DOC-PROTOCOL 固化「回写凭证必填」**，呼应 ccc-plan-004 批次5「空回写卡=0」。
- 本记录 7 天内应：进权威（如并入 `ccc-plan-004` 复盘 / `docs/archive/`）或归档。
