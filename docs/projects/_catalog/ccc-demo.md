# ccc-demo（pending）

- **路径**：Mac2017 `/Users/fan/program/apps/ccc-demo`
- **status**：`pending` — seed 已有，PREFIXES 尚无专用前缀
- **taskable**：否（出卡前须先在 `registry.yaml` 定前缀并改 PREFIXES）
- **勿**：口头用 `cd` 出卡而不改 registry
