# 方案 · 意图开发定位强化 + DeepSeek Harness 接入

> 项目：ccc · 编号：ccc-plan-029 · 状态：已确认 · 作者：老板 · 工具：Claude Code
> 创建：2026-08-14 · 更新：2026-08-14
> 关联卡：无
> 关联方案：ccc-plan-027（功能卡两级模型）
> 里程碑：无（方向性方案；可作新里程碑种子）
> 决策源：qx-map `__archive__/decisions/意图开发定位与DeepSeekHarness协同-2026-08-14.md`

## 目标

把 CCC 的定位钉死为**意图开发平台**，明确「不做工具、只做编排」的边界，确立与执行层工具（重点：DeepSeek Harness）的协同方式，并把「一切皆插件」理念吸收进意图层流程设计。

## 背景

DeepSeek Harness（DSH）2026-08-13 发布开源（MIT · 一切皆插件 · 基于 Cordis）。它是 DeepSeek 首款 Agent 框架，定位执行层。对谈后老板定调：

1. 之前定位正确——**CCC 不做 IDE/工具，做编排**。
2. CCC 的外部层（文档 / 项目管理 / 进程管理 / 项目周期管理）是给 Harness 这类工具补的，这类能力 Harness 天生没有。
3. **「意图开发」是 CCC 的重要定位**，需要加固。
4. DSH 是下一步重点接入的 IDE 工具。
5. 学习 DSH「一切皆插件」——CCC 的 Skill / Worker / 节点其实也是插件。

## 方案内容

### 1. 定位钉死（已同步 PRIME-DIRECTIVE §0 + INDEX §0）

**CCC = 意图开发平台，不做工具。** 意图开发四要素：孕育（草案/线路图）→ 澄清（对话收敛）→ 固化（功能卡+验收点）→ 闭环（看板+Loop）。

**执行层 = 可替换执行体槽位**：DSH（headless）/ Claude Code / OpenCode 按 `executors.json` 接入，退出码收单。执行层永远不决定意图。

### 2. DeepSeek Harness 接入（下一步重点）

| 项 | 内容 |
|----|------|
| 定位 | 重点接入的 IDE 工具 / 执行层执行体候选 |
| 优势 | 模型同源（DeepSeek 系）；headless = one-shot runner 契合薄驱动 Engine 契约；Trajectory 可作验收证据原料 |
| 动作 | ① 实测 `dsh headless` 跑一张探针卡验证接入可行性；② 通过则入 `executors.json` 注册表；③ Trajectory 评估接入 card-evidence |
| 门槛 | Developer Preview，兼容性可能被破坏——接入前先实测，不预判 |

### 3. 一切皆插件（意图层吸收）

不照搬 Cordis，吸收「可替换性」作为流程设计默认取向：

- **Skill** → 独立加载/卸载，按角色动态注入（role-skills 已做一半，继续补齐）
- **Worker / 执行体** → 注册表驱动、可替换（已是插件语义，保持）
- **节点 / 门禁** → 每道闸是可替换策略（transfer_gate / 机审 / 合入批准）
- **回写 / 观测** → 每类回写是可插拔钩子（Doc-Gate / 四问 / 证据链）

### 4. 外部层能力独占（加固）

文档（DOC-PROTOCOL / registry / 三层金字塔）、项目管理（里程碑进度）、进程管理（Engine 派发收单）、项目周期管理（看板+Loop 反哺）——**这是 CCC 对 Harness 的护城河**，持续做扎实。

## 验收标准

- [ ] PRIME-DIRECTIVE §0 定位声明已写入，三层金字塔未被破坏
- [ ] INDEX §0 已加定位指针，决策源可回链
- [ ] qx-map 决策已归档（`意图开发定位与DeepSeekHarness协同-2026-08-14.md`）
- [ ] 卡1：Run Code 模式约定已写入 executors 文档 + 内存脚本辅助可用（临时目录隔离+超时），实测对比耗时与残留
- [ ] 卡2：executors.json 真实注册表被 Engine 读取 + DSH headless 槽位登记 + 探针卡实测结论落卡
- [ ] 卡3：DSH 只读取证席登记（只读约束）+ 审计契约文档化 + 一单真实扫描结果经人工核实

## 功能卡

> 一个功能一张卡。M1 主窗口直接开发 + 异席机审（ccc 平台自研红线，不走 Engine 自动出卡）。三卡按价值排序：吸收执行模式 → 落地注册表 → 启用席位。

### 卡1 · 吸收 Run Code 模式（执行层内存临时 worker 约定）

> 状态：**完成 2026-08-15** —— `scripts/ccc-run-inline` 辅助脚本 + `docs/product/run-code-convention.md` 约定；实测 node/py/stdin/超时/零残留/语义正确性全过（sed 盲替换会把冷冻节点端口改错，内存脚本一次做对）。

目标：把 DSH 的 Run Code 执行模式吸收进 CCC 执行层——执行体处理复杂元数据/文本改动时，**写内存临时脚本（Node/Python）替代 sed/awk 拼装**，快、稳、零项目残留。

实现：① 在 executors 参数模板/开发约定中写明该模式；② 提供一个轻量辅助（`scripts/ccc-run-inline`，支持 `node -e`/`python -c` 风格的内存脚本执行，临时目录隔离 + 超时）；③ 用真实场景（改 JSON 种子/Markdown 元数据）验证速度与零残留。

验收：① 约定已写进 executors 文档；② 辅助脚本可用且带临时目录隔离+超时约束；③ 实测一个复杂改动的内存脚本执行，与 sed/awk 对比记录耗时与残留。

### 卡2 · executors.json 落地 + DSH headless 槽位

> 状态：**注册表落地完成 2026-08-15** —— example 模板 F5 对齐（去 Claude 开发槽）+ DSH 只读取证/审计槽已提交；麦克2017 运行时 `executors.json` 已建（`EXECUTOR_REGISTRY_PATH` 本就指向它），6 槽可读、服务健康。
> 待办：**dsh headless 探针实测**——dsh CLI 不在 PATH（web 为主形态），headless 具体参数未定稿；按验收"跑不通则记录原因"，标记挂账。

目标：把 `server/config/executors.example.json` 变为真实注册表 `server/config/executors.json`，登记 DSH headless 为执行体槽位，验证 6102 code 档接入。

实现：① 评估 example 各槽位（OpenCode 开发 / 维护 / 管理席）实际启用情况，落地为真实注册表；② 加 DSH headless 条目（one-shot runner，退出码收单，与 OpenCode 同位可切换）；③ 实测 `dsh headless` 跑一张纯文档探针卡，记录「拉起→执行→退出码→回写」。

验收：① executors.json 为真实文件并被 Engine 读取；② DSH 槽位已登记；③ 探针卡实测结论落卡（能跑通给出接入方案 / 跑不通记录原因）。

### 卡3 · DSH 只读取证席启用

> 状态：**契约已定，待跑首单** —— DSH 只读取证/审计槽已登记于卡2（executors.json）；审计输出契约 + 红线扫描清单 + DSH 触发模板已定（见下）；首单扫描由老板经 DSH 触发后人工核实。

目标：启用 DSH 的只读取证/合规扫描席位（今天三轮实测的最强项），跑一单真实审计验证流程。

#### 审计输出契约（证据格式，卡3 定稿）

```
发现清单表格：| 面 | 位置 file:行号 | 现象 | 证据(原文+命令输出) | 建议处置(改/删/留) | 置信度(高/中/低) |
硬要求：
- 每条发现必须带实际读到的原文 + 验证命令及输出（如适用）
- 「查过、无异常」项也要附验证命令输出（证明真查了）
- 覆盖度自评：读了哪些 / 没读哪些 / 哪些是推断
- 禁止：凭记忆断言、未核实当事实、臆测
```

#### 合规红线扫描清单（首单扫什么）

1. 开发/机审分离：OpenCode=开发、Claude Code=机审、DSH=只读取证；禁交叉越权、禁自审自放行
2. 中转站：唯一出口 6100/6102；4000/4100/4102 不得以"现行"出现（只许历史标注）
3. 文档落点：落点表外禁新建；registry.yaml 唯一事实源；禁双写
4. 卡命名：`prefixNNN-slug.md` 定死，禁发明
5. git 纪律：禁 `git add -A`；显式路径提交
6. 密钥红线：不碰密钥明文，只占位引用
7. 红线一票否决：业务意图违背 / 系统性越界 / 安全漏洞

#### DSH 触发模板（老板粘贴到 DSH 使用）

```
# 任务：合规红线扫描（只读 · 审计证据契约）
运行节点：麦克2017（192.168.3.116）——必须在麦克2017 上运行，路径 /Users/fan/... 才是真实路径
你是 CCC 的只读取证/审计执行体。只读扫描目标（如 /Users/fan/program/CCC 与 /Users/fan/qx-map），
对照红线清单找违规/残留/过时/遗漏。输出严格按审计输出契约。
硬约束：全程只读（不写/不删/不改，除起手 git pull）；每条断言带命令输出；
无法核实标「未核实」；禁止臆测；报告输出在对话。
红线清单：开发机审分离 / 中转站 6100/6102 / 文档落点与 registry 唯一真值 /
卡命名 prefixNNN / 禁 git add -A / 密钥红线 / 红线一票否决。
```

实现：① 在 executors / role 映射中登记「只读取证」角色（只读沙箱、禁写）；② 定义审计输出契约（发现清单 + file:行号 + 证据 + 置信度）；③ 用 DSH 对 CCC/qx-map 跑一单合规红线扫描（对照红线清单）。

验收：① 只读取证角色已登记且约束为只读；② 审计契约文档化；③ 跑出一单真实扫描结果，人工核实命中/误报。

## 机审记录（2026-08-15 异席机审）

> 结论：**三卡有条件通过**。2 个问题已修，其余挂账。机审核实真伪：dispatch.py decide()「可后台 CLI → AUTO」会拉起不存在的 dsh（中高风险，已修 DSH 槽分类→「—」）；README 缺 DSH 行（已补）；卡1「沙箱」表述不符实现（已改「临时目录+超时」）；headless 参数未定稿落注册表（已标占位）。

**挂账**：
1. dsh headless 探针实测（CLI 不在 PATH、参数未定稿；挂账期间 DSH 槽不参与 Engine 派发）
2. 首单合规扫描（DSH 只读取证触发模板已就绪，老板在麦克2017 触发后人工核实）
3. 机审三卡改动需老板裁决（本机审即异席审）

## 备注

- **红线**：ccc 平台自研，本方案不走 Engine 自动出卡，由 M1 直接开发 + 异席机审（与 ccc-plan-027 同规则）。
- **方向约束（2026-08-15 老板强调）**：CCC 处于开发初期但已有债务；本次吸收必须朝「可插拔平台」大方向做——新增构建要可替换、可组合、无特权核心，不为了快而堆临时补丁。
- **吸收原则**：优先吸收模型无关的架构模式（Run Code / 注册表 / 轨迹证据），DSH 具体的模型/人格缺陷（谄媚、断言不核实）用指令纪律与模型选型管住，不混入架构层。
- DSH 为 Developer Preview，一切以实测为准，不写死接入承诺。
- 未来方向三线：意图层加深 / 执行层扩展（DSH）/ 插件化改造（Skill/Worker/节点）。
