# 方案 · 机审区格式契约统一——解析器双保险 + 规范成文

> 项目：ccc · 编号：ccc-plan-035 · 状态：待排期 · 作者：OpenCode（W4） · 工具：OpenCode
> 创建：2026-08-17 · 更新：2026-08-17
> 关联卡：无（平台自研红线：ccc 禁出卡，不走 engine 自动流程；交给 M1 主窗口直接开发）
> 关联方案：无（独立立项）
> 里程碑：流程契约加固
> 决策源：2026-08-17 底座复盘（qx-map `sync/notes/ccc-base-review-2026-08-17.md`）F1 + hp044 实测踩坑（`sync/notes/hp044-merged-2026-08-17.md`）

## 目标

统一机审区格式契约，杜绝「审计 Agent 输出格式与解析器假设不符 → 机审通过但落盘失败 → 死循环」类问题，让机审区写入/解析/门禁三段对同一标准。

## 背景（证据）

2026-08-17 三起同源事故：

1. **机审落盘失败死循环**：`server/board/models.py:94` 解析器读取 `## 机审区` 时忽略所有 `###` 开头行，而 2017 审计 Agent 输出 `### 机审：通过` → 误判无结论 → 机审通过但落盘失败 → 冷却死循环，hp032/hp038 卡死无法合入（已热修：忽略 `###` 时排查是否含「机审/结论/结果」字样）。
2. **parse_metadata 全文扫 `>` 行**：机审区 `> 状态：机审通过` 被 `parse_metadata` 全文扫 `>` 行误当成卡头状态（`meta["状态"]` 后写覆盖）→ loader 测试失败 `state='机审通过'`（hp044 实测踩坑，已改为 `> 结论：`）。
3. **无成文规范**：审计 Agent 输出格式靠经验，无权威契约文档；DSH 审计执行体（ccc-plan-029 定位）接入时同样面临格式对齐问题。

## 方案内容

### 一、机审区格式权威标准（契约正文）

机审区内固定两类行，其余自由：

- **结论行**（解析器唯一依据）：`> 结论：通过` / `> 结论：不通过`（权威写法）。**禁止** `> 结论：机审通过`（冒号后须直接是「通过/不通过」，中间隔「机审」则判定器不认 → 死循环根因）；`**机审：通过**` / `### 机审：通过` / `结果：通过` 为兼容写法。详见 onboarding §6.6。
- **元数据行**：一律 `> 结论：` 前缀，**禁止 `> 状态：` 前缀**（会被 parse_metadata 误当卡头状态覆盖真值）。
- **层级标题**：允许 `### 机审：通过` 等 `###` 行，解析器已兼容（含结论字样不忽略）。

### 二、代码改动

1. 机审区格式校验器：审计落盘前校验结论行格式，非法格式当场报错打回（防静默降级）。
2. `parse_metadata` 加固：仅解析卡头元信息区，不全文扫 `>` 行（或对 `## 机审区` 之后的内容明确豁免）。

### 三、文档改动

1. 契约正文写入 `docs/projects/onboarding.md` 机审区规范段 + 本方案登记为权威源。
2. 2017 审计 Agent / DSH 审计执行体的机审输出模板对齐契约。

## 验收标准

- [ ] 机审区结论行/元数据行格式有权威契约（onboarding 成文）。
- [ ] 非法机审区格式（如 `> 状态：`）在落盘时被校验器拦截并报错，不再静默污染卡头状态。
- [ ] `> 结论：通过` 权威写法被正确解析；`> 结论：机审通过`（带「机审」）被校验器拦截（hp032/hp038 死循环根因）；`### 机审：通过` 兼容写法被正确解析，单测覆盖。
- [ ] hp032/hp038 死循环类问题无复发路径（解析器双保险 + 校验器前置）。

## 功能卡

> 平台自研（ccc taskable:false），本节为**开发分工范围**，非实际任务卡。

### F1 机审区格式校验器
实现：落盘前校验机审区结论行格式，非法即打回。
验收：非法格式落盘被拦；合法格式全通过。

### F2 parse_metadata 卡头隔离
实现：机审区内容不再参与卡头元信息解析。
验收：机审区 `> 状态：` 不再覆盖卡头真值；loader 测试通过。

### F3 契约成文 + 审计 Agent 模板对齐
实现：onboarding 机审区规范段 + 2017/DSH 模板对齐。
验收：审计输出默认符合契约，无需人工修正。

## 转卡计划

**平台自研（ccc taskable:false），不转卡、不进 engine 自动流程。** M1 主窗口直接开发 + 直接测试 + 异席机审（2026-08-10 平台自研红线）。

## 备注

- 关联教训：机审区元数据行禁用 `> 状态：`（hp044 踩坑，教训已入业务仓 `docs/lessons.md`）。
- 防复发：解析器双保险（T1 热修）为兜底，校验器前置为拦截，两层缺一不可。

## 机审记录（2026-08-19 · M1 机审证据席）

> 机审只出证据，不裁决；终验归 Codex（管理席）。本卡平台自研无任务卡，机审记录落本方案档。

### 机审结论：通过（待异席终验）

三道防线已落地且零回归。机审处置 4 项问题后达可合入标准。

### 4 项问题处置

| # | 问题 | 处置 | 证据 |
|---|------|------|------|
| 1 | 19 个 `docs/dispatch/xy/xy0{33..51}*.md` 脏工作树（别的已关闭任务回写） | 提交显式 `git add` 6 文件，未夹带 xy 文档 | `git status` 仅含本卡 6 文件 |
| 2 | 原开发自述「2 fail=预存 Self（3.9）」 | 本机 Python 3.14 实测 **0 fail / 2 skip**；2 skip 是 `test_http_api.py:1980/2008`（数据跳过）与 Self 无关；`card_header.py:14` 的 `from typing import Self` 确系预存（本次 hunk 在 47/157 行后） | `pytest server/tests/ -q --tb=no` |
| 3 | F1 挂钩无集成测试 | 新增 `test_append_rejects_invalid_audit_section`（monkeypatch 校验器返非法 → 断言返 False 且卡内容不变），钉死「挂钩存在且生效」 | `test_engine_audit_backfill.py` 末尾 |
| 4 | 契约写法不一致 | 统一 `> 结论：通过` 为权威；删校验器 `_AUDIT_VERDICT_RE` 冗余 `(?:机审)?` 对齐判定器 `models.py:108` | 实测见下 |

### 新发现根因（实测确认，非推理）

原方案档第 28/46 行把 `> 结论：机审通过` 当「合法/正确解析」写法——**实测判定器不认它**：

```
写法                 判定器 models.py   校验器（删 (?:机审)? 后）
> 结论：通过          通过              合法     ← 权威写法
> 结论：机审通过      无结论（不认）     拦截     ← 禁止写法（两器一致不认）
### 机审：通过         通过              合法     ← 兼容写法
```

冒号后隔「机审」二字 → 判定器正则 `结论：通过` 不匹配 → 判无结论。**这正是 hp032/hp038 死循环真实根因之一**：Agent 写 `> 结论：机审通过`，原校验器（带 `(?:机审)?`）放行、判定器却判无结论 → 机审通过但落盘判定失败 → 死循环。删 `(?:机审)?` 让校验器对齐判定器，落盘前拦截，堵死根因。契约已据此修正（onboarding §6.6 + 本档 §一/验收标准）。

### 零回归证据（可复现）

- 相关 4 测试文件：**34 点全过**（原 32 + 新增挂钩测试 + 禁止写法测试）
- 全量回归：与基线一致。`test_isolation_fields_parsed` 1 个预存 flaky fail（`isolation_max_concurrent` 运行时推导，registry.yaml 无此字段，测试硬编码 `==1` 脆弱），**基线（stash 掉本卡全部改动）同样 fail**，与本卡无关
- 复现：`git stash push -u && pytest server/tests/test_project_registry.py::test_isolation_fields_parsed`（基线 fail）→ `git stash pop`

### 提交清单（6 文件，显式 add 禁 -A）

```
server/board/card_header.py
server/engine/main.py
server/tests/test_engine_audit_backfill.py
server/tests/test_audit_format_contract.py  (新)
docs/projects/onboarding.md
docs/projects/ccc/plans/035-audit-section-format-contract.md
```

## 终验结论（Codex 异席终验 · 2026-08-19）

**总判：可合。** 三道防线逻辑正确、正则对齐、测试覆盖充分、无回归。F1/F2/F3 三层职责清晰互不依赖，任一失效不导致死循环；权威写法 `> 结论：通过` 成文，禁止写法 `> 结论：机审通过` 有校验器前置拦截；旧格式存量卡判定器仍识别不被误杀。

### 开发者核实纠正（机审席/本次开发者 · 防幻觉不附和）

Codex 终验报告有 2 处与事实出入，已实测核实：

1. **测试点数**：Codex 报告"28 点全过"，实测 **34 passed**（test_audit_format_contract 17 + test_engine_audit_backfill 7 + test_card_header 6 + test_board_column_audit 4）。不影响"全过"结论，但数字偏少。
2. **合入状态**：Codex 称"建议合入、走合入流程"——但 `f3f571e7` 是 `origin/main` 最新提交，**代码已在 main**。本卡平台自研（ccc taskable:false），按方案档 §转卡计划"M1 主窗口直接开发，不走 engine 合入流程"，合入在 push 时已完成，无待合入分支、无合入动作。

### 收尾状态

- 代码：`f3f571e7` 已在 origin/main（平台自研直接进 main）
- 全流程实跑验证：见 qx-map `sync/notes/ccc-fullflow-verify-2026-08-19.md`（读路径/旧格式存量卡/死循环根因路径/异常卡诊断 4 项全过）
- 待老板对"已在 main 的本次改动"认可收尾