# 方案 · DSH 调研收获固化 + 审计席部署 + 任务编排借用 CodeRun（重点突破方向）

> 项目：ccc · 编号：ccc-plan-034 · 状态：草稿（待老板确认） · 作者：老板 · 工具：Claude Code
> 创建：2026-08-16 · 更新：2026-08-16
> 关联卡：无
> 关联方案：ccc-plan-029（意图开发定位 + DSH 接入）
> 里程碑：暂无（可作为「任务编排能力」新里程碑种子）
> 决策源：qx-map `__archive__/research/dsh-eval-experiments-2026-08-16.md`（老板决策 §八）

## 目标

把 DSH 深度调研（25 实验 + 卡2/卡3）的收获固化收尾：**DSH 部署为独立审计席（稳妥）**，开发体替换**谨慎待 DSH 成熟**；并立项 **「任务编排/多任务执行借用 CodeRun」**为 CCC 下一步突破重点方向——CC 规范化执行流程已打通，缺的是多步骤/长任务编排能力。

## 背景

- DSH（DeepSeek Harness）CodeRun 范式（模型写代码、代码内调工具）已完成深度调研：源码 11 章报告 + 25 实验 + 卡2/卡3 落地（2026-08-16，Claude Code）。
- 关键实证：DSH 只读取证/审计能力强（卡3 首单 3 实锤）；CodeRun 对多工具编排任务提效明确（一轮=多子调度并发）；无沙箱（内部可信环境直接使用，老板定）；description 漏参 57%（模型纪律短板）。
- CCC 现状：**任务编排、规范化执行流程已打通**（看板 → 出卡 → Engine 派发 → 合入门禁），但**多步骤、长任务、任务编排能力尚未正式投入开发**——这是老板认定的下一步突破重点。
- CodeRun 的「程序化编排 + curated result」正是补这块能力的范式参考。

## 方案内容

### 1. DSH 部署定位（老板定，落地）
- **现役**：DSH 只做**独立审计/取证席**（卡3 已验证：只读取证、零残留、带证据）。部署于 Mac2017，走 headless 只读，不参与 Engine 自动派发（executors.json 分类「—」）。
- **谨慎**：**不做 OpenCode 开发体替换**。CCC 沿用当前流程（OpenCode 开发），等 DSH 成熟后再评估替换，不预判。
- 隔离沙箱不投入（老板定：集群内可信环境直接使用）。

### 2. 调研收获固化（收尾）
- 25 实验 + 卡2/卡3 已落档：qx-map `__archive__/research/`（报告 + 汇总）+ CCC 分支 `research/dsh-eval`（experiments/）。
- 收获要点沉淀为可复用的三条（见 §4 借鉴）。

### 3. 任务编排借用 CodeRun（重点研究立项）
- **方向**：研究 CodeRun 模式解决 CCC 的「多步骤/长任务/任务编排」——把「一次卡任务 = 多步骤流水」从逐轮人工/工具编排，变为**程序化编排（一段编排程序内并发/串行调度多个子步骤）**。
- **假设**（待研究验证）：① 多步骤编排提效（少模型往返）；② 长任务上下文更省（curated result）；③ 低档模型可承担部分编排（配合边界约束）。
- **范围**：本方案只做**研究立项**（探针 + 概念验证），不做生产改造。成熟后再考虑与 Engine 编排对接。

### 4. CodeRun 借鉴点落卡（机制层，模型无关）
1. **curated result**：执行体回写只带结论、详情进证据链——对齐 CCC 审计契约。
2. **程序化并发编排**（Promise.all 并行）——临时 Worker 语义。
3. **fail-closed 默认**：无审批不升级、无通道即拒。

## 验收标准

- [ ] DSH 审计席部署就绪：只读取证走通 ≥1 单真实审计（卡3 已过首单，复测一次）
- [ ] 任务编排研究立项：CodeRun 编排探针 ≥1 个概念验证（多步骤串/并联演示）
- [ ] 3 条借鉴点写成 CCC 约定（回写/执行体边界文档）
- [ ] 不触碰 OpenCode 开发体替换（沿用当前流程）

## 功能卡

> 方案 = 一个子项目（任务编排能力），节点② 老板确认后按子项目转卡。三卡按价值排序。

### 卡1 · DSH 审计席部署运行

目标：把 DSH 只读取证席从「实验验证」变「日常可用」——独立跑合规/巡检扫描，与开发/机审三席分离。

实现：① 确认 executors.json DSH 槽参数（已定稿）；② 定义 DSH 审计触发入口（headless runner，只读）；③ 接板一次真实巡检（可复用卡3 审计模板）。

验收：DSH 独立审计席可被老板按模板触发一单真实扫描，结果落档。

### 卡2 · 任务编排借用 CodeRun（研究探针）

目标：概念验证「一段编排程序调度多步骤」在 CCC 任务形态下是否提效。

实现：① 选一个真实多步骤任务（如「出卡 → 审卡 → 回写」或一次多文件改动）；② 用 CodeRun 写成编排程序（并发只读 + 串行变更 + 条件分支）；③ 对比逐轮工具调用的轮次/耗时/上下文占用。

验收：探针产出「编排 vs 逐轮」对比数据，结论落档（提效是否成立 + 适用任务类型）。

### 卡3 · CodeRun 借鉴点落 CCC 约定

目标：把 3 条机制借鉴写进 CCC 执行体约定。

实现：① curated result → 回写契约「只带结论、详情进证据链」；② 并发编排 → 执行体边界文档；③ fail-closed → 执行体默认。

验收：3 条写入对应文档（executors / 回写 SOP / 边界），validate-plans 通过。

## 备注

- **红线**：ccc 平台自研，本方案走 M1 直接开发 + 异席机审（与 ccc-plan-027/029 同规则），不走 Engine 自动出卡。
- **方向约束（老板 2026-08-16 强调）**：DSH 只做审计、开发体替换谨慎、任务编排是重点突破方向——按此定优先级。
- 任务编排研究是**探索性**的，产出可能是「方向验证 + 落卡建议」，不承诺生产改造。
