# 方案 · 内容产线架构：从「技术规格达标」到「内容质量可度量」（M5.3+ / 新里程碑）

> 项目：xy · 编号：xy-plan-011 · 状态：待排期（含 2 个拍板点，拍板前不动手） · 作者：外脑（S140-01@M1 · 老板 2026-09-18 直派） · 工具：Claude Code
> 创建：2026-09-18 · 更新：2026-09-18
> 关联卡：无（拍板前不动手；实施按「功能卡」拆解，见 §七）
> 关联方案：xy-plan-008（M5 高表现力二期，其 5.3 A/B 质量评估由本方案接手重定义）· xy-plan-006（质量量化，11 项技术规格门，本方案不推翻、在其上加内容维）· xy-plan-010（M7 发布闭环，**本方案不触碰，仍等 Cookie 前置**）· ccc-plan-055（底座缺口登记，见 §六）
> 取证基线：CCC 主仓 @ `f2e6c71b8`（2026-09-18）；业务仓 `~/program/apps/xianyu` @ `bc75287`。全程只读核实，零代码/配置改动。本方案所有诊断结论均经外脑亲验，不采信子任务自报（两处自报已在 §二更正）。
> 授权声明：外脑按老板 2026-09-18 指令「由你这边来驱动」执行；本方案为**规划产物**，实施前须经老板拍板 §八 两个决策点。外脑不因本方案获得任何生产写权。

---

## 目标

把闲鱼产线从「**技术规格达标即算通过**」升级为「**内容质量可度量、且失败不会伪装成成功**」的自动产线。

一句话判据（可机器验证）：**任一环节产出失败时，产线必须报错而不是报成功；内容质量必须有一个可复现的数字分数，且该分数由机审独立算出、不由产出方自述。**

终点形态：一条 24 小时运行的产线，每日自动出「选题 → 文案 → 视频 → 四平台适配稿」，每份产出带一份**含内容维度的质量分数**与**可复现证据**；低于及格线的不进库、不发布；连续失败的自动熔断并挂人工。

---

## 一、为什么是 2 分：这不是渲染问题

**先说清楚一件事：渲染本身没有毛病。** 实测链路：PIL 逐帧画 PNG（`scene/generator.py:18-19`，1080×1920 / 30fps）→ ffmpeg 编码（`compose/generator.py`，`-b:v 5000k` + `-nal-hrd cbr` + `-preset slow` + `aacc 192k`）→ **edge-tts 真神经网络配音**（`tts/generator.py` 四音色按情绪标签选：serious=YunjianNeural / humorous=Yunxi / marketing=Xiaoxiao / emotional=Xiaoyi，`SubMaker()` 出 SRT）→ 字幕（FontSize 40 / Outline 2）+ BGM（音量 0.3）。技术规格 9 项全达标。

**2 分的真正来源是三层断链**：

### 断链 1：内容输入是关键词复读（垃圾进）

视频文案由 `video-pipeline/stages/script/generator.py` 的 f-string 模板拼装，模板要读 `data/content-sources.json` 取 `hook`/`title`/`core_selling`——**该文件不存在**（`find` 零命中），于是永远走 fallback，把主题字符串同时塞进三个空位。实测产物 `script.json`（主题「AI视频生成技术原理」）：

- 场景1（28s）：「挑战摆在面前：**AI视频生成技术原理** 很多人卡在这一步。」
- 场景2（26.8s）：「突破来自一个简单但深刻的洞察——**AI视频生成技术原理**。这个思路改变了游戏规则。」
- 场景3（25.2s）：「带来的价值是实实在在的。。如果你也在为**AI视频生成技术原理**头疼，不妨试试这个方案。**一分钱不花，三分钟搞定。**」

`word_count: 119`，3 场景撑 80 秒 = 每秒 1.5 字；主题名复读 3 次；有重复句号「。。」；情绪标签 `humorous` 但文本无任何幽默要素（误判）。

**决定性发现——难的活已经干完了，只是没接线。** 业务仓 `src/xianyu/content/` 里存在完整的 LLM 文案系统：`writer.py`（走 3456 通道写 350 字闲鱼垂类文案，四段结构锁死：3 秒钩子≤2 句 → 痛点场景 → 3 条干货（每条含数字/案例）→ 行动号召；300-400 字自校正，字数超标重生成一次）、`rewriter.py`（一份稿改写为 bilibili 800-1500 字口语化 / toutiao 300-500 字信息流 / weibo 140 字 + #话题# / **video_script = 50-60 秒短视频口播稿**）、`ab_writer.py`（A/B 双版本）、`router.py`（关键词→平台路由）。

**`video-pipeline/` 这条产线与这套 LLM 系统之间零接口。** 实测 `grep "writer\|rewriter" video-pipeline/stages/script/generator.py` 零命中。视频产线缺的正是 `video_script` 口播稿，而它就在隔壁仓库躺着。

### 断链 2：质量门是假的（假验收）

`scripts/check_video_quality.py` 共 11 项，**逐条全是技术规格，零内容质量**：分辨率 / 码率≥3.5Mbps / h264 High Level≥4.0 / 时长 60-90s / 文件大小 / 有 audio stream / srt 非空 / config 有 bgm_path / 5 帧灰度 diff>1.5 / CLIP≥0.25 / cosine<0.70。

**skip 不扣分**——这是最致命的一处（`render_all_templates.py` 的 `parse_quality_json()`）：

```python
fail_count = sum(1 for c in checks if c["ok"] is False)
skip_count = sum(1 for c in checks if c["ok"] is None)
return {"checks": checks, "pass": fail_count == 0, ...}
```

`ok=None` 完全不进 `fail_count`。而第 10、11 项在真实环境**永远返回 `ok=None`**，所以 `pass=True` 实际只证明「9 项技术参数达标」。

**外脑更正子任务自报**：子任务称「sentence-transformers 未装导致永久 skip」。**不实**。本机实测 `python3 -c "import sentence_transformers"` → `INSTALLED 2.7.0`。真因是模型缓存缺失叠加受限网络：`_load_sentence_transformers()`（`check_video_quality.py:362-377`）先以 `local_files_only=True` 加载 `clip-ViT-B-32`，而 `~/.cache/huggingface/hub/` 里只有 `models--BAAI--bge-m3`、**没有 clip-ViT-B-32** → 抛异常 → 回退联网加载 → 失败 → `except: return None`。

**这意味着内容质量检查不是架构死角，是缺一个模型文件 + 缺一行容错。** 唯一能判断「内容对不对题」的检查从未执行过一次。

**人工打分表也从未执行**：`ab-report-20260914-103950/scorecard.md` 四维度（结构/画面/文案适配/整体）分值全是 ` /5` 空值，备注列全空，结论三行（综合最佳模板 / 主要差距 / 后续动作）全空。「人工打分」这一环只交付了空表格。

### 断链 3：失败伪装成成功（污染一切度量）

`src/xianyu/content/video.py:1086-1098` 的 `_write_mock()`：全管线失败时写出一个文本占位文件并 `return` 路径；调用方 `_mock_result()`（:1100-1106）返回 `WorkerResult(success=True)`。

实测后果：`data/videos/` 共 154 个 `.mp4`，其中 **152 个是 47-57 字节的文本占位文件**（hexdump 实证：`[MOCK VIDEO] subject=识别与清理 No image paths`）；`workspace/outputs/video/` 三个任务目录里两个 `final.mp4` 为 **0 字节**，仅一个是真视频。

**这是整个诊断里最重的一条。** 产线跑了很多次，产出几乎全是占位符，而每次都被记为成功。只要这条不改，后面任何质量数字都被假成功污染——**必须先修它，再谈优化**。

---

## 二、两条产线分裂（附带结构问题）

| | `video-pipeline/` | `src/xianyu/` worker 池 |
|---|---|---|
| 文案 | f-string 模板关键词复读 | **LLM 350 字 + 四平台改写** |
| 渲染 | PIL 逐帧 + ffmpeg | Playwright 帧捕获 + HyperFrames |
| 触发 | **无任何定时**，靠 DSH 卡手动跑 | `com.xianyu.worker.plist` `RunAtLoad+KeepAlive`，PID 817 在跑 |
| 实际行为 | 手动脚本 | `main()` 只 `init_db` → 打印已注册 Worker → `await asyncio.Event().wait()`，**注释自述「调度由 openclaw 接管，xianyu 仅作为工具池」** |

两条线互不衔接、无数据接口。

**自动化脚本写了但没接**：`scripts/daily/` 下 `run_daily_video.sh` / `run_pipeline_cron.sh` / `daily_publish_douyin.sh` / `generate_video.py` 均在位，但 `~/Library/LaunchAgents/` 里**没有任何对应 plist**（只有 `com.xianyu.worker.plist`）。即「每天自动产出」的代码写完了，调度器一直没挂上——这是纯接入工作，不是开发工作。

**交付物未进主线**：xy077 的 commit `4b2bd3a` 只在 worktree 分支，业务仓主仓 HEAD 停在 `bc75287`；`templates/ab_scorecard.md` 主仓不存在；`video-pipeline/output/` 被 `.gitignore:80-81` 忽略。台账记「已完成」，代码没进 main。这与 006 质量量化方案「进度 100%」的口径矛盾属同类债（roadmap 尾部已挂 8 条「方案进度不一致」治理债）。

---

## 三、质量差根因排序（按证据强度）

| # | 根因 | 证据 | 性质 |
|---|---|---|---|
| 1 | **失败伪装成成功** | 152/154 mp4 是文本占位；`_write_mock` 返 `success=True` | **P0 必须先修** |
| 2 | 质量门 skip 不扣分 + 唯一内容检查从未运行 | `fail_count` 只数 `ok is False`；clip 模型缓存缺失 | P1 |
| 3 | 质量门只验技术规格、零内容维 | 11 项逐条列出 | P1（结构性） |
| 4 | 人工打分表从未执行 | scorecard 四维全空 | P1 |
| 5 | 内容输入是关键词复读 | 主题复读 3 次、119 字撑 80s、「。。」 | P2 |
| 6 | LLM 文案系统与视频产线零接口 | grep 零命中；`content-sources.json` 不存在 | P2（**接上即可**） |
| 7 | 两条产线分裂 | 见 §二 | P3 |
| 8 | 定时脚本存在但无 plist | daily/ 脚本在、launchd 无对应项 | P3（纯接入） |
| 9 | 交付物未进 main | `4b2bd3a` 仅在 worktree | P3 |

**关键判断**：#1 和 #2 必须先于任何「优化质量」的动作。理由：#1 不改则所有度量数字污染；#2 不改则无法判断优化是否有效（没有可测的内容分）。**反过来做，就是把 2 分的垃圾批量化——这正是老板要的「不要」**。

---

## 四、目标架构

四层，每层之间设质量闸，任一闸不过即停在该层不往下传。

```
① 选题层   关键词池/热点 → 选题 → 平台路由(router.py)          ← 新增：选题可追溯
                │
                ▼  [闸 A：选题合法性——红线词/重复选题去重/垂类归属]
② 内容层   writer.py 350字四段 → rewriter.py 四平台稿(含 video_script)  ← 已存在，接通即可
                │
                ▼  [闸 B：内容质量分 —— 见 §五，机审独立算，不出自产出方]
③ 渲染层   口播稿 → 场景 → edge-tts 配音 → PIL 帧 → ffmpeg → 字幕+BGM  ← 现有，无病
                │
                ▼  [闸 C：技术规格 9 项 + 视听一致(CLIP 实跑) + 差异化]
④ 入库层   产物 + 质量分 + 证据包 → 内容库(带指纹/可复现命令)          ← 新增
                │
                ▼  [闸 D：及格线判定 —— 不过不进库；发布仍等 M7/Cookie]
```

**三层职责分离（照搬量化侧已验证的模式）**：产出方（DSH/PI 执行体）→ **不自己打分**；质量席（CC CLI 机审，独立会话零共享上下文，CCC phase2 现成能力）→ 独立复算质量分；调度席（外脑）→ 只看台账与熔断，**永不自验收自己派单**。

---

## 五、质量度量口径（核心新增，唯一权威定义）

现状：两仓**均无任何 `/100` 或 `2/100` 打分记录**（搜过 CCC `docs/`+`data/`、xianyu 全仓 `*.py/*.md/*.json`、`~/.ccc/data/board-events.jsonl`）。唯一带 `quality_score` 的审计条目是**代码复杂度分**（changed_files / complexity_avg / mypy_errors / assert_per_test），与内容无关。

所以「2 分」是人工主观判断，无记录出处。以下为本方案提出的口径——**首版需实测一次才有基线，本方案不预填分数**（预填即制造假基线，同断链 1 的病）。

| 维度 | 分值 | 判据（机器可算） | 现有能力 |
|---|---|---|---|
| **内容实质** | 30 | 关键词复读率、每段信息密度、是否含数字/案例、字数 300-400 | 需新增；`structural_problems()`/`count_words()` 已有可复用 |
| **结构完整** | 15 | 四段标记齐全、3 秒钩子≤2 句 | `writer.py:97 validate_copy_structure` **已存在** |
| **平台适配** | 15 | 各平台字数区间 + 语气特征词命中 | `rewriter.py` **已存在**，缺判定器 |
| **视听一致** | 15 | CLIP 画面-话题相关度 ≥0.25 | `check_clip_alignment` **已存在**，缺模型文件 |
| **技术规格** | 10 | 现有 9 项 | 已有 |
| **差异化** | 10 | 视频间 cosine <0.70 | `check_uniqueness` 已有，<2 视频时 N/A |

**三条口径铁律（防本方案自己长成断链 2）**：

1. **skip 必须显式可见，禁止静默不扣分。** 改法不是把 skip 直接算 fail（CLIP 模型缺失会让全产线熔断），而是：`pass = (fail_count == 0 AND skip_count == 0)`，同时输出 `pass_partial`（仅 fail 判定）与 `pass_full`（fail+skip 判定）两个字段，入库只认 `pass_full`。口径来源必须能区分「9 项达标」和「11 项全过」。
2. **内容维分数必须由质量席独立算，产出方无权自述。** 产出方回执里的质量分一律不采信，以闸 B 机审复算值为准。
3. **分数必须随句标口径**（引擎版本 / 模型版本 / 样本段 / 是否全量），单窗观察不构成过门证据——照搬量化侧 `caliber-authority-spec` 的模式。

**及格线建议：60 分**（内容实质 ≥18/30 + 结构满分 + 平台适配 ≥9/15 + 技术规格满分）。**目标线 75 分。** 低于及格线不进库。

> ⚠️ 此建议待 §八 拍板点 2 确认。

---

## 六、CCC 底座缺口（登记，不在本方案内修）

底座盘点结论：CCC 引擎是「代码任务的执行+审核+合入+部署机器」，可靠性隔离做得足（INFRA 指数退避封顶 1800s 不计预算 / BUSINESS·PROTOCOL 双预算+达限挂人工 / merge 冲突两次熔断 / push 失败保已回写态进冷却 / 短命会话熔断 / 配额预检 / 静默故障落盘 `engine-pipeline.json`），**这些全部可复用**。

面向内容生产缺三件：

1. **周期触发**：`observer.py:1690 trigger_scheduled_ops` 只支持「卡头 `派发：scheduler` + `定时：HH:MM`」的**一次性**触发，需卡先存在、无 cron 表达式解析、到期不重生卡。`scheduler.py` 框架有 `TASK_TYPE_CHANGE` 定义但 `_default_registry` 里**零变更类任务**（README:31 明写「只读巡检 + 变更类走卡（不绕过 Engine）」）。
   → 缺口：没有「每天自动生成一批新选题卡」的调度器。属**新开发**，登记 `ccc-plan-055`，不在本方案落地。
2. **产出物入库/发布**：`deploy_and_probe`（`phase2.py:954`）只干一件事——拉起 CCC 自己的 `server.web.server` 并探 `/health`。全仓无内容入库/媒体上传实现。
   → 缺口：内容库登记 + 指纹 + 可复现证据包。业务仓侧做，不动底座。
3. **内容质量评估**：底座有 `scripts/quality-score.py`（代码圈复杂度/mypy/断言密度增量不可劣化门禁），内容质量零实现。
   → 缺口：内容分计算与闸 B。业务仓侧做，底座只借相位审席位。

**底座纪律不可破**：任何自动产内容动作都必须先落卡，不能绕过 Engine 直接写。这是 `scheduler.py` 明写的架构纪律，本方案遵守。

---

## 七、落地阶段（顺序有依赖，不得跳序）

### 阶段 0 · 让失败显形（P0，1 卡）

**唯一动作**：`_write_mock()` / `_mock_result()` 的 `success=True` 改为失败态，产线对占位文件与 0 字节产物 fail-closed。

- 理由：不改则后续所有度量被污染。这是全方案最高杠杆的一条改动。
- 风险：会立刻暴露大量历史失败（152/154 全是占位），台账数字会「变差」。**这是正确的**——变差是真值，之前的好是假象。
- 与量化侧模式对应：「失败不伪造成功」+「fail-closed 而非伪装 0 条」。

### 阶段 1 · 内容质量可度量（P1，2 卡）

- 卡 1：补齐 `clip-ViT-B-32` 模型缓存（一次下载），`_load_sentence_transformers()` 加容错（缓存缺失时明确报错而非静默 None），让第 10 项真正运行。
- 卡 2：`parse_quality_json()` 改 `pass_full` / `pass_partial` 双口径（§五 铁律 1）；内容维 6 项打分器实现（3 项可复用现成函数）。
- 验收：**用现有已产出的真实样本跑一次 6 项打分，产出首版真实基线分数**。本方案不预填该分数。

### 阶段 2 · 接通内容输入（P2，2 卡）

- 卡 1：`video-pipeline` 的 script 阶段接 `src/xianyu/content/writer.py` → 产出 `content-sources.json` → 消费 `video_script` 口播稿。f-string 模板降级为兜底（不是主路径）。
- 卡 2：接通后重跑同一主题，对比阶段 1 基线与阶段 2 分数。**这是本方案的第一次真实质量验证**——若分数不动，说明接通没通，打回。

### 阶段 3 · 自动化调度（P3，2 卡，**须阶段 0-2 全过闸才开工**）

- 卡 1：`scripts/daily/` 已有脚本挂 launchd plist + 错峰验证（量化侧判例：cron 分钟交集=0；建时在册≠会触发，须 `nextRunAt` + 首拍产物双验）。
- 卡 2：内容库入库（产物 + 质量分 + 指纹 + 可复现命令），配产线断供哨兵（>3 天无产物 CRITICAL，照搬量化侧 `research_product_stall`）。

### 阶段 4 · 发布闭环（**不做**）

M7 仍等 Cookie 前置（2026-08-17 老板定）。本方案产出止步于入库，不碰外部平台发布。

---

## 八、拍板点（2 个）

**拍板点 1：开工顺序。** 推荐按 §七 严格顺序，阶段 0 先行。代价：阶段 0 落地当天台账会显示大量历史失败，观感是「产线变差了」，但那是真值第一次显形。**不推荐**先做阶段 3 自动化——那会把 2 分的垃圾批量化，且因阶段 1 无可测内容分而无法判断优化是否有效。

**拍板点 2：质量口径与及格线。** 推荐 6 维 100 分制、及格 60 / 目标 75（§五），口径三条铁律强制。代价：内容实质 30 分的判据需要实现一个打分器（约 200 行），且首版阈值需跑 10-20 组样本校准。**未核实标注**：首版真实基线分数本方案不预填，阶段 1 实测后才知当前真实分数落在何处。

---

## 九、从量化侧借鉴的模式（已在量化仓验证有效）

| 模式 | 量化侧位置 | 本方案落点 | 迁移成本 |
|---|---|---|---|
| 失败不伪造成功（fail-closed） | `dsh_runner.sh`；P2-060「读失败抛原异常而非伪装 0 条」 | 阶段 0 | 极低 |
| DSH 失败不重试 + 连续 3 次标 CRITICAL | `dsh_runner.sh:151-162` | 阶段 3 熔断 | 低 |
| 哨兵对账（产线断供哨兵） | `research_product_stall.sh`；`check_daily_fetch_freshness.sh` | 阶段 3 | 低 |
| 错峰心跳 + 建后双验 | 运转-sop §1.2/1.4 | 阶段 3 launchd | 低 |
| §66 独立复算（复算 vs 自报对照列） | charter-v4.0:351-356 | 闸 B 内容分 | 中 |
| 口径权威单一源 + 报数必带口径 | `caliber-authority-spec-2026-09-16.md` | §五 铁律 3 | 中 |
| 回执证据门禁（声称↔diff 机器校验） | `check_receipt_evidence.py` | 闸 D 入库证据包 | 中 |
| 队列状态=派生数据（不靠人记改） | `tools/queue-derive.py` | 内容库状态 | 中 |
| Hook 三层机械护栏 | `guard-policy-2026-09-17.md` | 阶段 3 | 极低 |
| 执行席/验收席分离 | `.claude/agents/质量官.md` | 闸 B（CCC phase2 现成） | 高 |

---

## 十、不做什么（边界，明确写死）

1. **不碰 M7 / Cookie / 外部平台发布**（2026-08-17 老板定，前置未就绪）。
2. **不碰 CCC 底座的定时框架纪律**——自动产内容必须先落卡，不绕过 Engine。底座缺口登记 ccc-plan-055，不在本方案内修。
3. **不推翻 xy-plan-006 的 11 项技术规格门**——在其上**加**内容维，不是替换。技术规格仍须全过。
4. **不预填任何质量分数**——首版基线由阶段 1 实测产出。
5. **不删历史占位文件**（152 个 mock mp4）——它们是阶段 0 的证据，先留档再清。
6. **不动 config.env 任何值**，密钥只允许占位引用。

---

## 十一、验收标准

阶段 1 完成时：
1. `check_video_quality.py` 第 10 项在真实环境返回数值而非 `skipped`（证据：报告 JSON 中该项 `detail` 不含 "not installed" 且 `value` 为数字）。
2. 报告输出含 `pass_full` 与 `pass_partial` 两个字段，二者不等时明确标注哪些项被 skip。
3. **首版内容质量基线分数实测产出**（对既有真实样本，6 维分项 + 总分），写入本方案「首版基线」节。本方案交稿时该节为空，属预期。

阶段 2 完成时：
4. 同一主题接入 LLM 文案后，关键词复读率下降至 0（脚本可核：主题字符串在口播稿中出现次数 ≤1）；总分较阶段 1 基线提升（提升幅度待实测，不预填）。

阶段 3 完成时：
5. launchd 首拍产物双验通过（`nextRunAt` 在档 + 首次真实产物落盘）；断供哨兵 >3 天无产物时报 CRITICAL。
6. 内容库中每条产物带：质量分（含口径标注）+ 指纹 + 可复现命令。低于及格线的不入库，且入库记录可审计。

全程：
7. 失败不再被标记为成功——`_mock_result()` 路径返回失败态（可断言的测试用例覆盖）。
8. 未触碰 `config.env`、未触碰 M7 相关任何文件、未产生任何外部平台发布动作。

---

## 备注

- 本方案与 `ccc-plan-053`（平台自身全链自动化）不重叠：053 是「造代码的产线」，本方案是「造内容的产线」。053 提供调度底座复用，本方案不重写它。
- 本方案与 `ccc-plan-055`（底座缺口登记）配套：本方案是业务侧内容产线，055 是平台侧定时/入库/评估三缺口的登记单。
- xy077 的两处口径失实已按当前信封重刷（CCC 主仓 `dce6231ed`），机审区仍为历史「不通过」结论——其正式收口依赖引擎真机审重跑（需重启常驻服务，待老板批准），属独立阻塞项，不阻塞本方案阶段 0-2。
- 本方案诊断中两处子任务自报不实已更正：①「sentence-transformers 未装」→ 实际已装 2.7.0，真因是模型缓存缺失+受限网络；②「永久 skip」→ 实际为可修。
