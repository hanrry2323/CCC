# 方案 · <人读标题>

> 项目：<prefix> · 编号：<prefix>-plan-<NNN> · 状态：已确认 · 作者：<作者> · 工具：<工具名>
> 创建：YYYY-MM-DD · 更新：YYYY-MM-DD
> 关联卡：无
> 关联方案：无
> 里程碑：<里程碑标题，可选；来自该项目 roadmap.md 的 ### 标题>
> 子项目：<子项目 ID 标题，可选；来自 roadmap.md 里程碑下的子项目，新模型方案必填>
> 环境准备：<所需环境准备 + 是否已具备，可选；子项目方案必填，转卡门禁会校验>

## 目标

<一句话说清要达成什么>

## 背景

<为什么需要这个方案，解决什么问题>

## 方案内容

<具体怎么做，分步骤写>

## 验收标准

- [ ] <可验证的完成条件 1>
- [ ] <可验证的完成条件 2>

## 功能卡

> 一个功能一张卡（ccc-plan-027 拆卡原则）。2026-08-16 子项目层起：**方案 = 一个子项目**（里程碑下 1:1 生成），
> 节点② 老板确认清单后**按子项目逐步转卡**（可指定 slices 子集），不再整方案一次全转。
> 每张功能卡必须声明**开发卡三要素**：颗粒度 / 依赖 / 架构位置（validate-plans.sh 校验存在性；依赖为硬约束）。

### <功能卡标题——一个独立功能>

目标：<2-3 句人话，一眼看懂这一步做什么>

实现：<详细实现，可选；将注入卡文件 ## 实现 段>

验收：<验收点，可选>

颗粒度：<本卡改动范围说明——平衡粒度，非越小越好也非越大越好>

依赖：<依赖的功能卡标题（同方案）或卡 ID（跨方案），逗号分隔；无则「无」>

架构位置：<本功能在系统架构图中的位置，如「ingest/chunker → knowledge_search 语义检索链路」>

### <第二张功能卡>

目标：<……>

## 转卡计划

<兼容旧格式：每行一张卡标题。新方案优先用上面的「## 功能卡」段，转卡脚本自动识别。

## 备注

<风险、依赖、排期考虑等>