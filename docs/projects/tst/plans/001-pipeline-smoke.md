# 管线自检 · 冒烟验证

> 项目：tst · 编号：tst-plan-001 · 状态：已确定 · 作者：管理席 ox-alpha · 工具：DSH · 创建：2026-08-23 · 更新：2026-08-24 · 关联卡：tst002, tst003, tst004
> 进度：1/1 (100%)（作废 2）

## 目标

验证「真实看板出卡 → Engine 全自动派发 → DSH 开发执行 → 机审」链路在 tst 自检项目上可用；改动仅落在任务卡文件自身。

## 背景

2026-08-23 老板拍板启用 tst 前缀（指令 A 第 1 步），替代隔离目录出卡。本方案为首个自检样例。
注：方案编号 001 保留卡号 tst001（plan_reservations 机制），故首张实卡自动编号为 tst002。

## 功能卡

### 管线冒烟·本卡末尾追加独立行 tst-001-smoke 并回写

目标：执行体在本卡文件末尾追加一行与标记串完全一致的独立行并完成回写，验证派发/回写/机审全链路。标记串：`tst-001-smoke`。

实现：白名单仅本卡文件自身；追加行须整行精确等于标记串；完成后按模板填回写区与维护区四问。

颗粒度：单卡单改动（仅卡文件自身）
依赖：无
架构位置：任务卡流程面，零业务代码、零测试仓代码改动

## 验收标准

- [ ] tst002 卡经 Engine 自动派发后由执行体回写，卡头状态=已回写
- [ ] 卡文件存在且仅存在一行整行精确匹配的冒烟标记行（`grep -cx tst-001-smoke` = 1）
- [ ] 机审通过并写入「## 机审区」，台账有 machine_audit_pass 记录

## 备注

标记串在功能卡标题/实现节内联出现（不占整行），`grep -c`=3 属预期，验收只认 `grep -cx`=1 的整行匹配。
