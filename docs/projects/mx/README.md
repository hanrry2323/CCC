# medio-0（前缀 mx）

## 是什么

2017 上的独立业务仓；经 CCC 出卡驱动开发。

## 路径

| 机 | 路径 |
|----|------|
| M1 | 无 |
| Mac2017 | `/Users/fan/program/apps/medio-0` |

## 在 CCC 怎么动

- **前缀**：`mx`（UI id：`medio-0`）→ `docs/dispatch/mx/`
- **taskable**：是
- **出卡**：`scripts/new-card.sh --project mx --title "..."`

## 线路 / 近况

- 业务深文在 medio-0 仓；CCC 只留本页 + 任务卡
- 近况见看板未关闭 `mx*` 卡

## 禁区

- 前缀是 `mx` 不是 `medio`；卡文件名必须 `mxNNN-…`
- 禁止在 CCC 建业务深文档目录
