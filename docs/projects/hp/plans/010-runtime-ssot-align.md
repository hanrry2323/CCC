# 方案 · 运行时↔SSOT 对齐（M2）

> 项目：hp · 编号：hp-plan-010 · 状态：已确认 · 作者：Claude（中枢） · 工具：Claude Code
> 创建：2026-08-16 · 更新：2026-08-16
> 关联卡：无
> 关联方案：无
> 里程碑：M2 · 稳控与可恢复
> 子项目：2.3 运行时↔SSOT 对齐
> 环境准备：hp 节点服务访问 + mac2017 SSOT 可写

## 目标

运行时 mcp_server（含 kb_status）回灌 SSOT；SSOT 领先部分回灌运行时；diff 确认两端一致。

## 背景

运行时比 SSOT 还新（kb_status 等改进未回灌），两端源码不一致。

## 功能卡

### 实施「运行时↔SSOT 对齐」
目标：完成子项目 2.3 运行时↔SSOT 对齐，交付可验收产物。
颗粒度：子项目级（1-2 卡）。
依赖：无
架构位置：mcp-server（运行时）↔ SSOT 仓

## 验收标准

- [ ] 运行时↔SSOT 对齐完成，验收点可复核（命令/可观察结果）

## 备注

前置子项目（依赖）：2.1, 2.2——按依赖顺序逐步转卡，前置卡完成后本子项目才能独立验收。
